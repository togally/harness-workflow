# {{REPO_NAME}} 文档入口

最后更新时间：{{DATE}}

## 1. 文档目标

`workflow/` 目录是面向 Agent 协作的正式文档入口。

## 2. 推荐阅读顺序

1. `memory/constitution.md`
2. `context/experience/index.md`
3. `context/hooks/README.md`
4. `context/rules/agent-workflow.md`
5. 与当前任务相关的其他上下文文件

## 3. 目录说明

- `context/team/`：团队级通用规范
- `context/project/`：当前项目事实与架构
- `context/experience/`：经验索引与踩坑记录
- `context/hooks/`：按调用时机组织的 Hook 文档与子目录
- `context/rules/`：详细执行规则
- `versions/active/<version>/`：当前版本下的需求、变更与计划容器
- `versions/archive/`：历史版本归档
- `decisions/`：技术与架构决策
- `runbooks/`：启动、排障、恢复与发布手册
- `templates/`：可复用模板
- `memory/`：长期有效记忆与项目原则

推荐优先补齐：

- `context/project/project-overview.md`
- `context/team/development-standards.md`
- `context/rules/risk-rules.md`

## 4. 存量文档

如果仓库仍存在旧的 `doc/` 目录，应在此建立桥接索引，并采用渐进迁移方式处理。
