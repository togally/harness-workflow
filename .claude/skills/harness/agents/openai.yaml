interface:
  display_name: "Harness"
  short_description: "Initialize repo and manage requirement/change/version"
  default_prompt: "Use $harness requirement to create and refine a new requirement, then split it into changes in the harness workflow."
