# 实施说明：chg-03（历史漂移 reconcile（live → scaffold_v2 mirror 27+ 文件 + B 类 mirror → live 2 文件））

## 实际做了什么

- Step 1（STEP-1）：预置验证通过——工作树 4 行 unstaged（feedback.jsonl / runtime.yaml / untracked state + sessions，均属白名单），A17 由 chg-02 闭合（diff 零差异），`development-standards.md` 存在（B2 拉回零行为影响）。
- Step 2（STEP-2）：A 类 25 项 live → mirror 批量 cp（A17 由 chg-02 已闭合跳过），含新建目录（regression/ 4 个 reg-*.md；tools/catalog/api-document-upload.md）。
- Step 3（STEP-3）：A27 特殊处理——`printf 'queries: []\n'` 重置 mirror `missing-log.yaml` 为模板态（live 保持累积不变）。
- Step 4（STEP-4）：B 类 2 项反向 cp（mirror → live）：B1 `role-inheritance-checklist.md` + B2 `development-standards.default.md`。
- Step 5（STEP-5）：zero-drift 自证——`diff -rq .workflow/ src/harness_workflow/assets/scaffold_v2/.workflow/` 过滤后仅余 10 行"Only in .workflow"路径（均为 archive/backup/stage/flow 等白名单目录），无真实 drift 行。

## 未做与原因

- A17 `context/roles/harness-manager.md` 跳过（由 chg-02 同 commit 闭合，per plan.md 约定）。
- missing-log.yaml（A27）reconcile 后 live vs mirror 有意不一致（live 累积 / mirror 模板态）；已加入 diff grep 白名单。

## 关键文件变更

- `src/harness_workflow/assets/scaffold_v2/.workflow/` 下：25 个 A 类文件改写（含 A3-A6 新建 regression/reg-*.md + A24 新建 tools/catalog/api-document-upload.md）+ 1 个 A27 重置（missing-log.yaml → `queries: []`）= 26 文件。
- `.workflow/context/checklists/role-inheritance-checklist.md`（B1，mirror → live，新建）。
- `.workflow/context/team/development-standards.default.md`（B2，mirror → live，新建）。

## 已知限制

- zero-drift 自证使用了扩展白名单 grep（含 `tools/index/missing-log\.yaml`），与 requirement.md §8 附录命令 B 略有差异；已在 chg-03 plan.md STEP-4 及 audit.md § 1 元信息中显式标注。
- B 类 2 项拉回 live 后，已在 `.workflow/context/team/development-standards.md`（非 default）存在的情况下，stage-role.md 加载顺序无变化（default 仅为 fallback，不影响实际加载）。
