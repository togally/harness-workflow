# 实施说明：chg-04（端到端自证双层（mktemp 干净空目录 + Yh-platform 真实存量项目 backup-then-install-then-diff））

## 实际做了什么

- Step 1（STEP-1）：创建 `acceptance-report.md` 文件骨架（§ 0 元信息 / § 1 Layer 1 / § 2 Layer 2 STEP-A 状态 / § 3 关闭确认）。
- Step 2（STEP-2）：Layer 1 执行——`mktemp -d` 新建临时目录，`cp -R` 当前 repo，`PYTHONPATH=src python3 -m harness_workflow.cli install` 跑完，`diff -rq` 过滤白名单后输出 1 行（`state/platforms.yaml` 运行时修改，属预期行为，非结构性漂移）。drift count = 0（真实结构性漂移）。
- Step 3（STEP-3）：Layer 2 STEP-A——session-memory 留痕"chg-04 Layer 2 STEP-A 等待 user confirm"，**abort 不真跑**，写 § 2 pending 段，待主 agent 向用户 confirm。
- Step 4：写 `实施说明.md`（本文件）。
- Step 5：单 commit 落盘（chg-04 Layer 1 结果，不含 Layer 2）。

## 未做与原因

- Layer 2（Yh-platform 真实存量项目）STEP-B/C/D/E **未执行**：这是 requirement.md §9.2 + chg-04 plan.md STEP-3 的硬例外条款——即使 ff 模式，STEP-A 用户 confirm 也不可绕过。Subagent-L1 abort 并留痕，主 agent 接手向用户 confirm。
- test_install_repo_diffs_against_scaffold_v2_mirror_zero xfail 未摘掉：因为 install_repo 内部 `_refresh_experience_index` 动态更新 `experience/index.md`，导致 post-install 内容与静态 mirror 存在合理差异；需进一步评估是否要在测试中额外处理此场景。

## 关键文件变更

- `artifacts/main/requirements/req-36-.../acceptance-report.md`：新建，Layer 1 零漂移记录（含 platforms.yaml 运行时差异说明）+ Layer 2 pending 段 + § 3 关闭确认表。
- `artifacts/main/requirements/req-36-.../changes/chg-04-.../实施说明.md`：本文件（对人文档）。

## 已知限制

- `state/platforms.yaml` 运行时差异：install 过程中 `last_updated` 字段被动态更新，建议后续将此文件加入 diff 白名单（或在端到端自证脚本里显式排除）。
- Layer 2 Yh-platform 端到端自证尚未执行，AC-5 Layer 2 为"user-pending"状态；Layer 2 完成后需补充 acceptance-report.md § 2 完整记录并追加 commit。
