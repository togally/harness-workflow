# 实施说明：chg-02（install_repo sync 契约扩展（覆盖 context/tools/evaluation/flow，加 self-audit + 单测）+ harness-manager 硬门禁五保护面扩展）

## 实际做了什么

- Step 1（STEP-1）：确认 `_scaffold_v2_file_contents` 已是 `SCAFFOLD_V2_ROOT.rglob("*")` 全量遍历（仅过滤 `/requirements/`），sync 范围无需改动。
- Step 2（STEP-2）：在 `src/harness_workflow/workflow_helpers.py` 文件末尾新增 `_install_self_audit(root)` 函数（约 60 行），实现：触发面判定（HARNESS_DEV_REPO_ROOT env 或 pyproject.toml name 正则匹配）/ 白名单过滤（12 条 + A27 missing-log.yaml）/ live vs mirror 双向 diff / stderr 逐条告警 + WARNING 汇总。在 `install_repo()` 的 `return 0` 前插入调用。
- Step 3（STEP-3）：新建 `tests/test_install_repo_sync_contract.py`（含 2 个测试）：① test_install_repo_diffs_against_scaffold_v2_mirror_zero（标 xfail strict=False，依赖 chg-03 reconcile 完成后摘掉）② test_install_self_audit_reports_drift_to_stderr（AC-4，mock drift 触发 stderr 告警，当前 PASS）。
- Step 4（STEP-4）：扩展 `.workflow/context/roles/harness-manager.md` 硬门禁五（live）：保护面从 3 类文件扩展到 5 大子树（context / tools / evaluation / flow / state/experience）+ 扩展白名单 5 条 + 更新"违反处理"grep 命令 + 追加正反例各 1。同 commit `cp` 到 mirror（硬门禁五自身约束触发）。
- Step 5（STEP-5）：pytest 验证零新增失败（3 failed 均为 pre-existing，289 passed 含 1 个新 PASS + 1 xfailed）。

## 未做与原因

- 测试 1（test_install_repo_diffs_against_scaffold_v2_mirror_zero）标 `xfail(strict=False)`：依赖 chg-03（历史漂移 reconcile）完成后才能期望零差异，chg-02 单独 merge 时历史漂移仍存在。chg-03 完成后摘掉 xfail。
- `tomllib` 版本方案放弃：改用正则解析 `name = "harness-workflow"` 字面（R-E 缓解，零新增依赖，兼容 Python 3.10+）。

## 关键文件变更

- `src/harness_workflow/workflow_helpers.py`：新增 `_install_self_audit()` 函数（约 60 行），`install_repo()` 末尾插入调用。
- `.workflow/context/roles/harness-manager.md`（live）：硬门禁五保护面扩展（5 大子树 + 正反例）。
- `src/harness_workflow/assets/scaffold_v2/.workflow/context/roles/harness-manager.md`（mirror）：与 live 同步（cp 同 commit）。
- `tests/test_install_repo_sync_contract.py`：新建，2 个测试（1 PASS + 1 xfail）。

## 已知限制

- test_install_repo_diffs_against_scaffold_v2_mirror_zero 在 chg-03 完成前会 xfail（strict=False），不算 FAIL；chg-03 合入后需摘掉 xfail 标记并补 commit。
- `_install_self_audit()` 触发面判定依赖 pyproject.toml 存在且包含 `name = "harness-workflow"`；若本仓库重命名则需同步更新锚点。
