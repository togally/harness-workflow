# 实施说明：chg-01（audit 报告（Yh-platform + 本仓库 live vs scaffold_v2 全量 drift 清单））

## 实际做了什么

- Step 1（STEP-1）：创建 `audit.md` 文件骨架，包含 § 1 元信息 / § 2 总览 / § 3 A 类 / § 4 B 类 / § 5 C 类白名单 / § 6 Yh-platform / § 7-9 附录共 9 个章节。
- Step 2（STEP-2）：填充 § 3 A 类清单 27 项（含字段：路径 / 漂移形态 / 推断成因 / reconcile 方向 / 关联 chg），A27 特殊标注"mirror 重置为 `queries: []`"。
- Step 3（STEP-3）：填充 § 4 B 类清单 2 项（B1 role-inheritance-checklist.md / B2 development-standards.default.md），方向 mirror → live。
- Step 4（STEP-4 + STEP-5）：填充 § 5 C 类白名单 12 条（含斜体注解）+ § 6 Yh-platform 证据（§6.1 缺失 2 + §6.2 旧版 ~17 + §6.3 独有 + §6.4 结论）+ § 7-9 三段附录（实跑命令 / Yh-platform 命令模板 / 白名单 12 条逐条理由）。
- Step 5（STEP-7）：单 commit 落盘（`audit.md` + 本 `实施说明.md`，message 引 `req-36 / chg-01`）。

## 未做与原因

- STEP-2 中执行了预置验证命令（`diff -rq` + 行数统计），实测命中 39 行（含 grep 未完全拦截的白名单内路径格式行）；真实 drift = 29 项（A 类 27 + B 类 2），与 requirement.md §2.3 一致。
- 未动任何代码 / scaffold_v2 mirror / live `.workflow/` 文件（均落在 chg-02 / chg-03）。

## 关键文件变更

- `artifacts/main/requirements/req-36-.../audit.md`：新建，212 行，包含三类完整审计清单 + Yh-platform 证据 + 三段附录。
- `artifacts/main/requirements/req-36-.../changes/chg-01-.../实施说明.md`：本文件（对人文档）。

## 已知限制

- `grep -vE` 白名单模式为子串匹配，`Only in .workflow: archive` 等"仅路径名"格式行在某些终端实现中可能不被完整过滤；audit.md § 1 已标注"实测 39 行，真实 drift 29"，不影响 AC-1 判定。
- A27（`tools/index/missing-log.yaml`）的 reconcile 方向与其余 A 类不同（mirror 重置为空模板态，非 live 内容覆写），已在 § 3 末行特殊标注，需在 chg-03 / chg-04 端到端自证时把此文件加入白名单 grep 排除。
