# 实施说明：chg-05（端到端自证（api-document-upload tools-manager 匹配 + project-reporter 从 scaffold_v2 可召唤））

## 实际做了什么

- STEP-1：python3 脚本模拟 tools-manager keywords.yaml 匹配，query=`上传 API 文档 apifox 同步`，api-document-upload top-1 overlap=5（PASS）
- STEP-2：`cat .mcp.json 2>/dev/null` → 不存在；`mcp__apifox*` 不可见 → DEGRADED（E-4 授权，不阻塞）
- STEP-3：6 条 scaffold_v2 project-reporter 存在性/行数/bytes/role-model-map/index/harness-manager §3.5.1 全部 PASS
- STEP-4：追加"端到端自证 req-34"段到 `.workflow/state/sessions/req-34/session-memory.md`（~40 行，含 AC 汇总）

## 未做与原因

- STEP-5（pytest 全量）：本 chg 未跑全量，由 testing stage 负责；做了 smoke collect（338 collected，pre-existing 2 failures 确认）

## 关键文件变更

- `.workflow/state/sessions/req-34/session-memory.md`：追加端到端自证段（~40 行），含 4 步留痕 + AC 命中汇总
- 各 chg 下 `实施说明.md`：本 chg 一并产出 chg-01~05 共 5 份对人文档

## 已知限制

- apifox MCP 真实联调（STEP-2 非 DEGRADED）需用户先 `claude mcp add apifox ...` 后重跑本 STEP
