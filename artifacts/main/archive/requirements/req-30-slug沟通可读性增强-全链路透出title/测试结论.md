# 测试结论：req-30 slug 沟通可读性增强：全链路透出 title

> 本文档首次提到 req-30（slug 沟通可读性增强：全链路透出 title）和 chg-01（state schema — title 冗余字段）/ chg-02（CLI 渲染 — render_work_item_id helper）/ chg-03（角色契约 — id + title 硬门禁）/ chg-04（归档 meta — title 落盘）；后续沿用简写 id（契约 7 — AC-10 自证）。

## 通过/失败统计

- 用例总数：220 collected（基线 188 + 新增 32）
- 通过：183
- 失败：1（pre-existing：`test_smoke_req29::test_human_docs_checklist_for_req29`，req-29 归档目录缺失，与 req-30 无关）
- 跳过：36（维持）
- **本次新增 32 条单测 100% 通过（chg-01 × 12 + chg-02 × 12 + chg-03 × 8），零回归**

## 关键失败根因

无本次引入失败。唯一失败项为 req-29 归档清理导致的 pre-existing smoke，与本需求无关。

## 未覆盖场景

- **AC-07（归档目录 `_meta.yaml`）**：chg-04 延期，未在本周期实测归档后 `_meta.yaml` 落盘路径；现状归档目录名 slug + `需求摘要.md` 首行模板部分覆盖但非硬约束。
- **Agent 真实会话中契约 7 遵守度**：契约文档 + 9 个角色文件样本已就位，但"agent 在真实任务中是否自动按 `{id}（{title}）` 格式首次引用"属于 agent 运行时行为，本次静态断言（样本文档 grep 通过）不等于运行时验证。
- **legacy yaml 非标准空串兜底**：若未来出现手写 `title: ''` 字面量（被 yaml 解析为 `"''"` 字符串）导致 `render_work_item_id` 显示 `（''）`，当前无 strip 兜底。
- **大规模 state/requirements 下的性能**：当前文件数 < 30，未做百级 id lookup 的压测。
- **regression id（reg-NN）**：目前走 `(no title)` 降级（reg 无独立 state yaml），sug 登记后处理，非本 AC 要求。

## 风险评估

- **风险判定：可上线（可推进 acceptance）**。
- 核心 AC-01~06 / AC-08~10 全部通过；AC-07 延期已三处一致登记为"不阻塞主线"（需求 §8 / planning / executing 决策）。
- 32 条新增单测零回归，CLI smoke 端到端通过，契约文档覆盖全部 9 个角色文件。
- 建议 done 阶段决定 AC-07 是否登 sug（"归档 `_meta.yaml` 落盘"），维持 req-30 干净完成。
