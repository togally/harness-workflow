# 交付总结：req-30 slug 沟通可读性增强：全链路透出 title

## 需求是什么

- 让任何人 / agent 在任何场景看到工作项 id 时，不额外跳转就知道它在说什么功能——以 `{id}（{title}）` 形态贯穿四层（汇报模板、CLI stdout、runtime/state yaml 字段、归档/索引）。

## 交付了什么

- **chg-01（state schema — title 冗余字段）**：`runtime.yaml` 新增 3 个 `*_title` 字段 + 11 处写入点统一同步 + `_resolve_title_for_id` helper + 12 条单测。
- **chg-02（CLI 渲染 — render_work_item_id helper）**：新渲染 helper + 覆盖 `harness status / next / ff / suggest --list` 四条命令 + sug 多级 fallback + 12 条单测。
- **chg-03（角色契约 — id + title 硬门禁）**：`stage-role.md` 契约 7 + 9 个角色文件（7 stage + director + stage-role）汇报模板更新 + `experience/index.md` 来源列校验规则 + `planning.md` 经验示范回填 + 8 条契约测试。
- **chg-04（归档 meta — title 落盘）**：📌 optional 延期，转 sug-22（归档 `_meta.yaml` 落盘）。
- **自证样本**：requirement.md / 需求摘要.md / 4 change.md / 4 plan.md / 3 实施说明.md / 3 变更简报.md / test-evidence.md / 测试结论.md / acceptance-report.md / session-memory.md 全部按契约 7 写作。

## 结果是什么

- ✅ **验收通过**（无条件）：AC-01~AC-10 中 9 ✅ + 1 📌 计划内延期（AC-07 转 sug）。
- 32 条新增单测 100% 通过，全量 pytest **220 collected / 183 passed / 36 skipped / 1 pre-existing failure**（`test_smoke_req29`，与本 req 无关），零回归。
- 影响面：仅扩展 + 向后兼容；旧 `runtime.yaml` 缺 `*_title` 字段时默认空串不炸。

## 后续建议

- **sug-22**（归档 `_meta.yaml` 落盘，AC-07 延期主体）：待优先级排期。
- **sug-25**（`harness status --lint` 自动化契约 7 校验）：可显著降低人工审核成本。
- **sug-26**（辅助角色契约 7 扩展 — harness-manager / tools-manager / reviewer）：完善契约覆盖面。
