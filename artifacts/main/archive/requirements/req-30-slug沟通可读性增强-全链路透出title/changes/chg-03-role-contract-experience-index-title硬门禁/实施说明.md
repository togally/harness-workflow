# 实施说明：chg-03 角色契约 — id + title 硬门禁

> 承接 req-30（slug 沟通可读性增强：全链路透出 title），本次实施 chg-03（角色契约 — id + title 硬门禁），后续沿用简写 id。

## 实际做了什么
- Step 1：`.workflow/context/roles/stage-role.md` 新增 `### 契约 7：id + title 硬门禁`，含规则 / 校验方式 / fallback 三段；契约 7 开头显式写"与契约 1-6 并列生效不覆盖"（R3 缓解）。
- Step 2：7 个 stage 角色文件（requirement-review / planning / executing / testing / acceptance / regression / done）在"对人文档输出"章节各增加 1 段"契约 7 硬门禁"注脚 + 汇报样本（首次引用 req-30 即带完整 title）。`done.md` 额外追加 checklist 项"契约 7：首次引用工作项 id 时均带 title（grep 校验通过）"。
- Step 3：`.workflow/context/roles/directors/technical-director.md` 的 Step 4 派发段落新增"Subagent briefing 模板（契约 7 硬门禁）"，含 `current_requirement_title` 双字段示范 + 正文形如 `{id}（{title}）的 XXX 工作`。
- Step 4：`.workflow/state/experience/index.md` 新增 `### 来源字段校验规则（契约 7）`，含规则 + 正例（`req-29（批量建议合集 2 条）`）+ 反例（裸 `req-29 chg-04`）+ 生效时机（仅新增/修改）。
- Step 5：示范性回填 `.workflow/context/experience/roles/planning.md` 的"经验一"来源段，从 `req-29 — sug-01 + sug-08` 改为 `req-29（批量建议合集 2 条）— sug-01...` 粒度。
- Step 6：新增 `tests/test_chg03_title_contract.py` 共 8 条用例（3 类：契约 7 章节 / 7 stage + director 样本 / experience 校验规则 + 自证样本）。

## 未做与原因
- 未对历史 `requirement.md` / `change.md` / `plan.md` 做 title 回填（req-30 AC-08 明确"新增时校验、存量按需补"）。自证样本测试聚焦 executing 阶段新产出的 `实施说明.md`。
- 未开发 `harness status --lint` 工具（plan 明确作为后续 sug 登记）。
- 未改 `harness-manager.md` / `tools-manager.md` / reviewer 模板（scope 之外；plan 明确转 sug）。

## 关键文件变更
- `.workflow/context/roles/stage-role.md`：新增契约 7 章节。
- `.workflow/context/roles/{requirement-review, planning, executing, testing, acceptance, regression, done}.md`：各加 1 段契约 7 注脚 + 样本。
- `.workflow/context/roles/directors/technical-director.md`：Step 4 加 briefing 模板章节。
- `.workflow/state/experience/index.md`：新增来源字段校验规则。
- `.workflow/context/experience/roles/planning.md`：经验一来源段回填为带 title 格式。
- `tests/test_chg03_title_contract.py`（新增）：8 条文本校验用例。

## 已知限制
- 契约 7 的 grep 校验需要 done 阶段 reviewer 人工复核"首次引用"判定（R6 已标注）；后续可在 sug 池登记"自动化 lint 工具"。
- 自证样本测试对 `实施说明.md` 的首次引用判定采用"含 req-30 后紧跟 `（` 或 `-slug` 或模板标题格式"启发式，极端 edge case 可能误判；当前 3 份实施说明均通过。
