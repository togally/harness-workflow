# 验收摘要：req-31（批量建议合集（20条））

> 契约 7 自证：本文件首次引用 req-31（批量建议合集（20条））/ chg-01（契约自动化 + apply-all bug）/ chg-02（工作流推进 + ff 机制）/ chg-03（CLI / helper 剩余修复）/ chg-04（归档迁移 + 数据管道）/ chg-05（legacy yaml strip 兜底）时均带 title。

## AC 核对结果

| AC 编号 | 通过 | 证据 / 备注 |
|--------|------|-----------|
| AC-01（`harness status --lint` CLI） | ✅ | `tests/test_contract7_lint.py` 10 用例绿；fixture 违规 / 合规 exit 码就位 |
| AC-02（产出后自检 `harness validate --contract`） | ✅ | `validate_contract.py` + 三 `--contract` 分支；stage-role.md / regression.md 条款就位 |
| AC-03（辅助角色契约 7 扩展） | ✅ | `tests/test_assistant_role_contract7.py` 3 用例绿；3 份角色 / 清单文件新节命中 |
| AC-04（regression 契约 + sug frontmatter 五字段） | ✅ | `tests/test_create_suggestion_frontmatter.py` 3 + `test_regression_contract.py` 2 全绿 |
| AC-05（`harness next --execute`） | ✅ | 3 用例绿；fixture stdout 含 subagent-briefing JSON fence |
| AC-06（`stage_timestamps` 完整性） | ✅ | 6 用例绿（含 regression --testing + bugfix ff 全程） |
| AC-07（`ff_mode` 自动关 + subagent timeout） | ✅ | 7 + 3 用例绿；runtime 自证待 archive 触发（路径已落地） |
| AC-08（helper 去重 + hash 竞争 + 覆盖 + apply-all） | ✅ | 8 用例绿；3 份测试文件覆盖 |
| AC-09（auto-locate + ID 扫归档） | ✅ | 6 用例绿；fixture 子目录启动 + 归档 id 不复用 |
| AC-10（归档 `_meta.yaml` + migrate 扁平） | ✅ | 8 用例绿；`_meta.yaml` 4 字段落盘。**小瑕疵**：CLI `migrate` subparser `choices=["requirements"]` 不含 `archive`，底层支持（非阻塞，衍生 sug） |
| AC-11（feedback 提示 + regression 独立 title） | ✅ | 4 用例绿；fixture `current_regression_title` 独立 |
| AC-12（legacy yaml strip 兜底） | ✅ | 5 用例绿（含内部引号保留反例） |
| AC-综合（测试 + 零回归） | ✅ | 独立复跑 `pytest` = **252 passed / 36 skipped / 0 failed**（与 testing 一致） |
| AC-自证（契约 7） | ⚠️ | `harness status --lint` 扫 req-31 artifacts **171 条违规**（多 sug 并列枚举行）；按契约 7 legacy fallback + requirement.md §6 表格 title 列已提供上下文，**判定可接受**；衍生 sug "lint 规则增强（表格/列表上下文识别）" |

## 是否通过

**⚠️ 有条件通过（建议推进 done）**：硬指标全绿（252 passed / 14 AC 全部有 ✅ 或合理 ⚠️ 判定），仅 3 项条件：(1) 10 份《变更简报.md》《实施说明.md》对人文档按 ff 模式与 executing 预先知情延期，尚未落盘，建议 done 阶段或 archive 前补齐；(2) AC-10 CLI subparser 小瑕疵记 sug 候选；(3) AC-自证 legacy fallback 接受 + lint 增强记 sug 候选。ff 模式下主 agent 可自动推 done。

## 未达项处理建议

| 未达项 | 下一步 | 谁做 | 预期完成时间 |
|-------|-------|------|-----------|
| 10 份对人文档缺失（5 × 变更简报 + 5 × 实施说明）| 由主 agent 在 done 阶段派发独立 subagent 统一补齐；或放入下一 requirement scope | 主 agent（done 阶段）/ 后续 executing subagent | req-31 archive 前 |
| AC-10 CLI subparser 补 `archive` choice | 登记为衍生 sug（low 优先级）；下一轮消化 | suggest 池 → 后续 change | 下一 requirement 周期 |
| lint 规则增强（表格/列表上下文）| 登记为衍生 sug（medium 优先级）；避免此类假阳性重复出现 | suggest 池 → 后续 change | 下一 requirement 周期 |
| acceptance 硬门禁与 ff 模式冲突的"软阻塞"策略 | 登记为衍生 sug（medium 优先级）| suggest 池 | 下一 requirement 周期 |

---

**详细逐 AC 三元组核查（需求条款 → 实现点 → 测试证据）**：见 `.workflow/state/sessions/req-31/acceptance-report.md`。
