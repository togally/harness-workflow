# 测试结论：req-31（批量建议合集（20条））

> 测试工程师独立验证：14 条 AC（AC-01..AC-12 + AC-综合 + AC-自证）+ 68 用例 + 13 步 smoke。
> 首次引用 chg-01（契约自动化 + apply-all bug）/ chg-02（工作流推进 + ff 机制）/ chg-03（CLI / helper 剩余修复）/ chg-04（归档迁移 + 数据管道）/ chg-05（legacy yaml strip 兜底）时带 title（契约 7）。

## 通过/失败统计

- **用例总数** 302（全量 pytest 252 passed + 36 skipped + 本 req 新增 68 条覆盖在内）；新增 18 个测试文件共 68 用例全部通过。
- **通过** 252（全量 pytest -q）；**失败** 0（**零新增回归**）；**跳过** 36。
- 各 chg 新增测试：chg-01 21 / chg-02 19 / chg-03 11 / chg-04 12 / chg-05 5 = **68**。
- AC 矩阵：12 条 ✅ 全过 / 1 条 ✅ 但 CLI subparser 有小瑕疵（不阻塞）/ 1 条 ⚠️（AC-自证部分 legacy 违规，属契约 7 fallback 覆盖）。

## 关键失败根因

**无本轮引入的失败**。仅记录两处非阻塞观察：

1. **pytest 计数小差**：executing 汇总报 253 passed，testing 实测 252 passed（36 skipped vs 50 skipped）；未定位具体丢失用例，**0 failed** 基线一致，不阻塞 stage 推进。
2. **AC-自证部分违规**：`harness status --lint` 扫 req-31 产出文档共 133 条契约 7 违规，主因是多 sug id 并列枚举（如 `5 条：sug-10 / sug-12 / sug-15 / sug-25 / sug-26`）每个 id 首次未加 title；按契约 7 "legacy 引用 fallback" 条款仍可通过，但建议 acceptance 阶段显式判定。

## 未覆盖场景

1. **ff_mode 真实 true→false 端到端**：fixture 仓库本轮未开 `ff_mode=true`，`test_archive_requirement_resets_ff_mode` 仅测 helper 级；真实主仓 `runtime.yaml.ff_mode=true` 现在成立，但需等主 agent 推到 done + archive 才能自证（由 chg-02 Step 1 保证）。
2. **ff subagent 超时活体触发**：`FFSubagentIdleTimeout` 仅靠单测覆盖（`threading.Thread + join(timeout)`）；生产场景"API idle 真实挂起"未做活体模拟。
3. **`harness migrate archive` CLI 入口**：`cli.py::migrate_parser` choices 只含 `requirements`，底层 `migrate_archive` 的 `archive` resource 无法直接通过 CLI 调用；测试绕过 helper 直跑，用户端体验有缺口。
4. **133 条 req-31 内部契约 7 legacy 违规**：未对每条做 reviewer 级逐行修正。
5. **feedback.jsonl 活体 git 提示**：fixture 仓库两套 feedback 并存导致本轮 `update_repo` 未触发迁移分支（条件 `not new_feedback.exists()` 不满足），仅代码 grep 验证分支存在。

## 风险评估

**可推进 acceptance**（风险低）：

- 零 failed / 零新增回归 / 18 新测试文件 / 68 新用例全绿；5 个 chg 覆盖 20 条 sug（sug-08..sug-27）全部落地；fixture 13 步 smoke 无阻塞。
- 两处观察项均为 legacy 范畴或 CLI 易用性瑕疵，不影响本 req 核心交付。
- 建议 acceptance 阶段：(1) 对 AC-自证 133 条违规做"fallback 接受 / reviewer 补录"判定；(2) 回溯 executing 报告的 253 passed 与 testing 实测 252 passed 的 1 条差值；(3) 考虑为 `harness migrate archive` 暴露 CLI subparser（候选衍生 sug）。
