# 需求摘要：req-31（批量建议合集（20条））

## 目标

- 打包处理 bugfix-3（apply-all path-slug bug 批次）+ req-29（批量建议合集（2条））+ req-30（slug 沟通可读性增强：全链路透出 title）三轮沉淀的 20 条建议（sug-08..sug-27），按 A-G 主题分组逐批消化，维持 harness workflow 自身的健康与自我改进节奏。

## 范围

- **包含**：§6 的 20 条 sug 全部纳入；按主题拆为 4-5 个 change（A 契约自检 + D apply-all / B+C ff 与 stage_timestamps / D 剩余 CLI 修复 / E+F 归档与数据管道 / G legacy yaml strip）；本 req 自身产出作为契约 7（id + title 硬门禁）自证样本；合入 `apply-all` path-slug bug 修复（与 sug-11 同源）。
- **不做**：不恢复已被删除的 sug body（`.gitignore` 范围内无 git 历史，不现实）；不引入新 sug；不改 slug / id 编号规则；不动 req-30 已交付部分；不扩 `ff --auto` 到 acceptance 之后；不做 Python 包发布。

## 验收要点

- **AC-01 / AC-02 / AC-03 / AC-04（A 组 + sug-11）**：`harness status --lint` 契约 7 自动校验 + stage 产出后自动 `harness validate` + 辅助角色契约扩展 + regression《回归简报.md》契约补强 + `create_suggestion` frontmatter 五字段全量。
- **AC-05 / AC-06 / AC-07（B+C 组）**：`harness next --execute` 能触发下一 stage 实际工作；`stage_timestamps` 在 regression/bugfix ff 路径下无缺字段；`ff_mode` 在 done + archive 后自动关（本 req 完成后 `runtime.yaml.ff_mode == false` 自证）。
- **AC-08 / AC-09（D 组）**：`_path_slug` helper 统一 + 多生成器 hash 竞争保护 + `adopt-as-managed` 覆盖保护 + `apply-all` path-slug bug 修复 + CLI auto-locate repo root + ID 分配器扫归档树。
- **AC-10 / AC-11 / AC-12（E+F+G 组）**：legacy archive 迁移 + 归档 `_meta.yaml` 落盘 + feedback.jsonl 迁移提示 + regression reg-NN 独立 title 源 + `render_work_item_id` strip legacy yaml 脏数据。
- **AC-综合 / AC-自证**：每 change ≥ 2 单测；全量 `pytest` 零回归（≥ 183 passed）；req-31 自身所有产出首次引用 id 带 title（契约 7 自证）。

## 风险

- **R1（body 丢失传导）**：sug-08..sug-27 body 文件已被 `apply-all` path-slug bug 物理删除且在 `.gitignore` 范围内不可恢复，planning 阶段仅能靠 title + action-log + git log + session-memory 推断细节，可能漏掉原 sug 中的具体 trade-off / 实现提示——需要在各 `plan.md` 显式列出"已知不全"条目并保留"发现遗漏时补 sug"的回退路径。
- **R2（apply-all bug 复发）**：若 chg-01 修复 `apply-all` path-slug bug 前又有人触发 `harness suggest --apply-all`，可能二次删除新登记的 sug。缓解：在 chg-01 未落地前**禁止**执行 `--apply-all`（改用单条 `--apply`），并在 `apply_all_suggestions` 入口临时加 warning。
