# 交付总结：req-31 批量建议合集（20条）

## 需求是什么

- 打包处理 bugfix-3（install/update 单 agent 作用域）/ req-29（批量建议合集（2条））/ req-30（slug 沟通可读性增强：全链路透出 title）三轮沉淀的 **20 条 sug**，按 A-G 主题分组拆为 5 个 change 逐批消化，维持 harness workflow 自身健康与自我改进节奏。

## 交付了什么

- **chg-01（契约自动化 + apply-all bug）**：apply-all 原子化 + `create_suggestion` 五字段（契约 6）+ `harness status --lint` + `validate_contract.py` + 辅助角色契约 7 + regression 退出条件 + **6 覆盖 sug（sug-10/11/12/15/25/26）+ 21 单测**
- **chg-02（工作流推进 + ff 机制）**：`harness next --execute` + `_sync_stage_to_state_yaml` 补齐 + ff subagent timeout + **ff_mode 自动关**（AC-07 自证触发点）+ **5 覆盖 sug + 19 单测**
- **chg-03（CLI / helper 剩余修复）**：update_repo hash 竞争 + adopt-as-managed 保护 + CLI auto-locate + ID 分配扫归档 + **4 覆盖 sug（sug-13/14/17/19）+ 11 单测**
- **chg-04（归档/迁移 + 数据管道）**：归档 `_meta.yaml` 落盘 + legacy archive 迁移 + feedback 迁移提示 + regression title 独立源 + **4 覆盖 sug（sug-08/20/22/24）+ 12 单测**
- **chg-05（legacy yaml strip）**：`render_work_item_id` 读 title 时 strip legacy yaml 的引号/空格 + **1 覆盖 sug（sug-23）+ 5 单测**
- **自证样本**：requirement.md / 需求摘要.md / 5 × change.md / 5 × plan.md / 测试结论.md / 验收摘要.md / done-report.md 本对人文档首次引用工作项 id 均带 title（契约 7 自证）

## 结果是什么

- ⚠️ **验收有条件通过**（无阻断）：AC-01..AC-12 + 综合 + 自证 共 **14 AC = 12 ✅ + 1 ✅（UX 小瑕疵）+ 1 ⚠️（legacy fallback）**
- **pytest 190 → 253 passed**（+63 累计，36 skipped，0 failed），零新增回归
- **apply-all 数据丢失事故** 在 chg-01 Step 1 根治（原子写 + unlink 前校验 + `_path_slug` 清洗 + OSError 保护单测），再次调用安全
- **ff_mode 自动关** 将在 `harness archive req-31` 时触发（AC-07 最终 end-to-end 自证）
- 3 条非阻塞差异：(1) 10 份对人文档（5 变更简报 + 5 实施说明）延期；(2) AC-10 CLI `migrate archive` 未暴露（底层功能完整）；(3) testing/acceptance 252 vs executing 253 pytest（0 failed 一致）

## 后续建议

- **sug-31**（`harness status --lint` 规则增强）：priority=medium，识别表格/列表上下文消 133 条假阳性
- **sug-33**（acceptance 与 ff "软阻塞"策略）：priority=medium，明确对人文档延期边界
- **sug-35**（10 份对人文档补齐）：priority=medium，archive 前决定做 / 延期 / 放弃
