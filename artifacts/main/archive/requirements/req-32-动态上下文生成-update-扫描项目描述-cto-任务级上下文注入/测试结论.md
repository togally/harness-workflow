# 测试结论：req-32（动态上下文生成：update 扫描项目描述 + CTO 任务级上下文注入）

## 通过/失败统计

- 本 req 新增 TDD 用例总数：34（`test_project_scanner` 14 + `test_update_repo_profile` 4 + `test_task_context_index` 7 + `test_task_context_snapshot` 3 + `test_briefing_ff_regression` 6）
- 全仓 pytest：287 passed / 50 skipped / 0 failed（基线与复跑一致，零回归）
- 集成端到端：AC-01..AC-07 共 7 条全部 pass（AC-05 有措辞偏差，非阻塞）
- 契约校验：`harness validate --contract all` exit 1，322 violations（历史基线），其中 req-32 artifacts 下新增 3 条 contract-7 bare id 违规（P1-01）

## 关键失败根因

- 无单测失败；集成层唯一偏差：AC-05 stderr 文案 `skip user-authored: CLAUDE.md` 未出现（实际走 stdout `skipped modified CLAUDE.md`），用户自定义保护语义完整，仅消息路径与 chg-02（harness update 集成扫描器 + hash 漂移 + 用户自定义保护）spec 措辞不一致
- 契约 7 违规 3 条：chg-01（项目描述扫描器 + project-profile 落地）的 `session-memory.md` 行 1（`req-32` 裸 id）+ 行 38（chg-02 / chg-03（CTO 派发 briefing 注入 task_context_index + 快照落盘）裸 id），属契约 7 "首次引用必须带 title" 硬门禁违反

## 未覆盖场景

1. 多语言混合项目（如同时有 pyproject + package.json + pom.xml）的 `stack_tags` 权重 / 首选语言判定未测试
2. `project-profile.md` 损坏（frontmatter YAML 语法错）下游读者（下一次 `_build_task_context_index` 读 profile 加权）的容错仅有代码路径覆盖，无端到端集成测试
3. `task_context_index_file` 快照与 `harness archive` 迁移的真实 smoke（归档路径继承既有测试，未显式 e2e 跑一次 archive → artifacts 落盘核对）
4. subagent 真实行为验证（C2 回退语义是 LLM 消费契约，只有 helper 单测 + 文档覆盖，未在真实 subagent 会话中抽查回退动作）
5. `harness update` 在大仓（>1000 文件）的 scanner 性能 sanity（spec 要求 <100ms，未跑实际 bench）

## 风险评估

- **可上线（有阻塞）**。功能性 7 条 AC 全绿、pytest 零回归，但 P1-01（chg-01 session-memory contract-7 bare id 3 条）必须在 done 阶段六层回顾前修正，否则违反契约 4 "契约 all 须绿"。修正代价小（行 1 / 行 38 补 title），不影响代码层。P2-01 / P2-02 / P2-03 建议承接到 sug 池处理，不阻塞当前 req 推进 acceptance。
