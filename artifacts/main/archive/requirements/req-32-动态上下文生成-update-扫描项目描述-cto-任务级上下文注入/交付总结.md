# 交付总结：req-32（动态上下文生成：update 扫描项目描述 + CTO 任务级上下文注入）

## 需求是什么

把 harness workflow 从"静态 scaffold 同步"升级为"按任务内容动态生成上下文索引 + 项目画像"——`harness update` 扫项目描述生成 profile，CTO 派发 subagent 时按任务裁剪 task_context_index 并落快照。

## 交付了什么

- chg-01（项目描述扫描器 + project-profile 落地）：`src/harness_workflow/project_scanner.py` 新模块（509 行），覆盖 pyproject / package / pom / go / cargo / README / CLAUDE.md / AGENTS.md 8 类描述文件静态解析，产出 `.workflow/context/project-profile.md`（结构化字段 + LLM 占位 + sha256 hash + 时间戳）
- chg-02（harness update 集成扫描器 + hash 漂移 + 用户自定义保护）：`harness update` 接入扫描器，hash 漂移三态 stderr 反馈；复用 req-31（批量建议合集（20条））sug-14（用户自定义 CLAUDE/AGENTS/SKILL 保护）防覆盖
- chg-03（CTO 派发 briefing 注入 task_context_index + 快照落盘）：`_build_subagent_briefing` 扩展，索引构建器（硬上限 8 + 截断 warn）+ 快照写盘（`.workflow/state/sessions/{req-id}/task-context/{stage}-{seq}.md`），派发路径覆盖 next --execute / ff --auto / regression
- 对人文档链：需求摘要 / 3 份变更简报 / 测试结论 / 验收摘要 / 本交付总结俱全（契约 6 / 契约 7 绿）

## 结果是什么

- 7 条 AC 全 PASS；Scope 不包含项零越界；3 条 Risks 防御（索引上限 / hash 漂移 / 用户保护）全落地
- 12 个 commit（每 chg TDD 红绿独立），`pytest -q` 288 passed / 50 skipped（+35 新 TDD，零回归）
- 本 req 流程全程自证：testing / acceptance 派发产出的 briefing 实盘触发 9→8 截断 warn 与快照写盘，机制活用
- 遗留 1 次 runtime.yaml 意外污染（chg-02 subagent `git stash pop + git checkout HEAD --` 组合触发，已即时修复，并转 sug-36（executing 禁 stash/checkout 硬条款）沉淀为 executing 硬条款

## 后续建议

- 本 req 产出 10 条改进建议（编号 36 至 45）已入建议池，优先级分 high/medium/low：high 含 executing 禁 stash/checkout 硬条款 与 前置 review 阶段 stage_timestamps 白名单补全；medium 覆盖 contract scope flag / AC-05 spec 对齐 / tests conftest / next 语义文档；low 覆盖 CLI 便利 / gitignore 策略 / 多语言权重 / regression 默认 index
- 建议优先打包两条 high 建议下一个 req 落地（一条硬条款 + 一条 state 补全），保本轮经验即刻转化
- 本仓 artifacts/ 内对人文档链因 `/artifacts/` gitignore 规则未入 git，建议与 gitignore 策略决策一并评估
