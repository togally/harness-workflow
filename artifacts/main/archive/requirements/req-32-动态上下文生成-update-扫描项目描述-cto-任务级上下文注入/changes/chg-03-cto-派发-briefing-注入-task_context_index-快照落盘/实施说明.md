# 实施说明：chg-03（CTO 派发 briefing 注入 task_context_index + 快照落盘）

## 实际做了什么

- **Step 1-2**：扩展 `workflow_helpers._build_subagent_briefing` 签名，新增两个 kw-only 参数 `task_context_index` / `task_context_index_file`，在 JSON fence 中追加两字段；未传时缺省为空数组 / 空串，向后兼容既有 briefing 调用。
- **Step 3**：新增 `_build_task_context_index(root, stage, req_id)` helper，按优先级构造候选（role / stage-role / base-role / experience / project-profile / constraints / checklist），仅保留存在的文件，硬上限 8 条，超限 stderr warn `task_context_index truncated from N to 8`。
- **Step 4**：新增 `_write_task_context_snapshot(root, req_id, stage, index)` + `_next_task_context_seq`，落盘到 `.workflow/state/sessions/{req_id}/task-context/{stage}-{seq:03d}.md`；frontmatter 四字段 `req_id` / `stage` / `ts` / `index_count`；seq 按同 stage 已有快照数递增（三位零填）。
- **Step 5**：`workflow_next(execute=True)` 派发点串接：构建索引 → 写快照 → briefing 两字段；不同 stage seq 独立计数。新增 `_resolve_task_context_paths` helper（C2 回退语义，AC-04）。
- **Step 6**：`.workflow/context/roles/stage-role.md` + `assets/scaffold_v2/.../stage-role.md` 新增"task_context_index 回退语义"小节，约定 subagent 路径失效时静默 fallback。

## 未做与原因

- 未改 `project_scanner.py`（chg-01（项目描述扫描器 + project-profile 落地）底座）与 `update_repo`（chg-02（harness update 集成扫描器 + hash 漂移 + 用户自定义保护）底座），按 change.md Scope 排除。
- regression 派发路径（`regression_action`）当前无 briefing 输出，本 chg 未扩展以避免范围蔓延；作为 follow-up 留给后续 chg / req。
- `ff_auto._advance_to_stage_before_acceptance` 不经由 `_build_subagent_briefing`（ff auto 仅推进 stage + 落决策汇总，不打 briefing），故本 chg 不改。

## 关键文件变更

- `src/harness_workflow/workflow_helpers.py`：`_build_subagent_briefing` 签名扩展 + 新增 `_build_task_context_index` / `_next_task_context_seq` / `_write_task_context_snapshot` / `_resolve_task_context_paths` / `_stage_role_name` helper + `workflow_next` 派发点串接，约 215 行新增。
- `.workflow/context/roles/stage-role.md` + `src/harness_workflow/assets/scaffold_v2/.workflow/context/roles/stage-role.md`：同步新增"task_context_index 回退语义"小节。
- `tests/test_task_context_index.py`（7 条）+ `tests/test_task_context_snapshot.py`（3 条）：覆盖默认集 / 8 条上限截断 warn / profile 加权 / 快照 frontmatter + 正文 / seq 递增 / 不同 stage seq 独立 / next --execute 集成。

## 已知限制

- 领域加权目前只做"profile 存在即追加 project-profile.md 一条"；未基于 `stack_tags` 做深度匹配（如 python+poetry → experience/tool/python.md），留后续增量。
- 快照归档依赖既有 `harness archive` 对 `.workflow/state/sessions/{req-id}/` 整体迁入，本 chg 未单测归档路径（已在 chg-01 / chg-02 的 archive 测试覆盖）。
