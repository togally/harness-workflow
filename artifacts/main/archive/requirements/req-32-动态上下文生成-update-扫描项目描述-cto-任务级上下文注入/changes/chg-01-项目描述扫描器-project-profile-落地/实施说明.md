# 实施说明：chg-01（项目描述扫描器 + project-profile 落地）

## 实际做了什么
- Step 1：新建 `src/harness_workflow/project_scanner.py`，实现 `ProjectProfile` 数据类与 `build_project_profile(root)` 入口；落地 pyproject.toml / package.json 解析分支（含 `python+pyproject` / `python+poetry` / `nodejs` / `node+ts` stack tag）。
- Step 2：补齐 pom.xml（xml.etree 标准库 + Maven 命名空间处理）/ go.mod（正则 module + require 块）/ Cargo.toml（tomllib）/ README.md / CLAUDE.md / AGENTS.md（首 H1 作为 project_headline）6 个解析分支。
- Step 3：新增 `render_project_profile(profile, *, now)`，输出 YAML frontmatter（`generated_at` / `content_hash` / `schema: project-profile/v1`）+ 三段正文（`## 结构化字段` / `## 项目用途（LLM 填充）` / `## 项目规范（LLM 填充）`）；`content_hash` 复用 `workflow_helpers._managed_hash`；`now` 依赖注入支持测试 mock。
- Step 4：新增 `load_project_profile(path)`（剥 frontmatter → 解析 `## 结构化字段` 段还原字段；文件不存在返回 `None`）与 `write_project_profile(root, *, now)` 顶层入口（扫 → 渲染 → 写盘到 `.workflow/context/project-profile.md`，自动建父目录）。
- 测试共 14 条全绿，全量 pytest 267 passed / 50 skipped（基线 253 passed / +14 新增），零回归。

## 未做与原因
- 不接入 `harness update`：属 chg-02（harness update 集成扫描器 + hash 漂移 + 用户自定义保护）范围。
- 不构造 `task_context_index` / 不做 briefing 注入：属 chg-03（CTO 派发 briefing 注入 task_context_index + 快照落盘）范围。
- 不调用 LLM：按 B3 决策仅留占位 section，由主 agent/CTO 后续填充。

## 关键文件变更
- `src/harness_workflow/project_scanner.py`（新增 ~400 行）：scanner 模块主体（ProjectProfile / 各解析分支 / render / load / write 顶层入口）。
- `tests/test_project_scanner.py`（新增 ~350 行，14 条用例）：覆盖每种描述文件、render 快照、hash 稳定性、round-trip、空仓兜底、write 入口。

## 已知限制
- pom.xml 解析仅取 artifactId 与 direct dependencies（不解析 parent / dependencyManagement），深层 Maven 项目可能漏依赖。
- README/CLAUDE/AGENTS 的 headline 只取首 H1；非常规 markdown 结构（仅二级标题起头）将返回空。
