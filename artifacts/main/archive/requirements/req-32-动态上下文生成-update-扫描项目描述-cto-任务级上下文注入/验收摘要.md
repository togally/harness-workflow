# 验收摘要：req-32（动态上下文生成：update 扫描项目描述 + CTO 任务级上下文注入）

## AC 核对结果

| AC | 通过 | 证据 / 备注 |
| --- | --- | --- |
| AC-01 | [x] | `.workflow/context/project-profile.md` 产出：结构化字段 + LLM 占位 + generated_at/content_hash/schema 齐 |
| AC-02 | [x] | 二次 update hash 稳定；tmp 仓 `50be7e0→dea9f05` stderr `project-profile 已刷新（hash 漂移）` 可复现 |
| AC-03 | [x] | 本次 acceptance 派发 briefing 含 8 条 `{path, reason}` + `task_context_index_file`；next/ff/regression 三路径端到端 |
| AC-04 | [x] | `_resolve_task_context_paths` missing 分流 + `stage-role.md` 回退语义文档 + 单测 |
| AC-05 | [x] | chg-02（harness update 集成扫描器 + hash 漂移 + 用户自定义保护）user-authored + user-modified 双 stderr 分支均已落地（test-report P2-01 已在本轮前修复） |
| AC-06 | [x] | pytest 287 passed / 50 skipped 零回归；`harness validate --contract 7` 对 req-32 范围 grep 为空 |
| AC-07 | [x] | `sessions/req-32/task-context/testing-001.md` + `acceptance-001.md` 均 frontmatter 四字段 + 8 行正文 |

## 是否通过

- **通过**。建议直接进入 done 阶段归档。

## 未达项处理建议

- 无阻塞项。done 阶段需补 `交付总结.md` 把 `harness validate --human-docs` 补至 10/10。衍生建议 5 条（AC-05 spec 措辞回流 / conftest sys.path 注入 / `--scan-profile-only` flag / project-profile commit 策略 / 多语言混合 stack_tags 权重单测补齐）转 sug 池由 harness-manager 分配编号，不阻塞本需求。
