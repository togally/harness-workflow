# 测试结论：req-28 harness CLI 闭环修复 + 对人文档 SOP 收束

## 通过/失败统计
- 用例总数 137 / 通过 101 / 失败 0 / 跳过 36（均为 legacy version-flow 遗留，已由注释声明"外于本需求范围"）。
- 新增独立补测 3 条（`tests/test_req28_independent.py`）全部通过：5 次 round-trip 懒回填、`--force-done` 对已 done bugfix 幂等、`create_suggestion` 跨 archive+current 混合 5 档历史取全集最大 +1。

## 关键失败根因
- 无。

## 未覆盖场景
- AC-14 sweep 实际落盘路径偏差：本次 bugfix-3/4/5/6 一次性 sweep 的归档产物落在 legacy `.workflow/flow/archive/main/bugfixes/`，而非 AC-14 文本要求的 `artifacts/main/archive/bugfixes/`。根因：`resolve_archive_root`（workflow_helpers.py:4280）在 legacy 路径已有历史内容（req-01~req-26 旧归档仍在 `.workflow/flow/archive/main/`）时降级返回 legacy。chg-03 单测用 tempdir 空 legacy 目录无法暴露，需补一条"legacy 非空时新 bugfix 仍应落新路径"或提供 `harness migrate` 迁移子命令。建议作为 P1 偏差新开 sug 承接，不阻塞本需求验收。
- 真 `harness` CLI 子进程端到端 smoke 缺失：所有测试在 helper 层 + argparse 层覆盖，CLI 进程层（子进程隔离、真实 runtime 读写）未跑，由 briefing 硬约束决定。
- bugfix 归档后 `generate_requirement_artifact` 不产制品摘要 md（chg-03 已知限制）。
- sug frontmatter 硬门禁仅在 SOP 文档层声明，`create_suggestion` 写入时未 raise 校验，依赖 reviewer / done 阶段软拦截（chg-01 已知限制）。

## 风险评估
- 总体风险：**可上线**（档 1 四条闭环 bug 全部有独立单测覆盖且绿，档 2 三条 SOP/文档补齐均已落盘）。唯一需在验收阶段重点核对的 P1 偏差是 AC-14 实际 archive 路径落到 legacy 的事实；建议 acceptance 判定为"功能满足但路径需后续 sug 迁移"，或要求起 sug 承接后再进入 done，二选一由人工决策。
