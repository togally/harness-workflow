# 交付总结：req-28 harness CLI 闭环修复 + 对人文档 SOP 收束

## 需求是什么

- 修完 harness CLI 在 bugfix / archive / suggest 三条链路上的 4 个闭环 bug（档 1 硬阻塞），并补齐对人文档 SOP 的 3 个遗留（档 2 落盘校验 / 下游模板刷新 / 本 req 自身作为首次完整示范）。

## 交付了什么

- chg-01：修复 `harness suggest --apply / --delete / --archive` filename fallback + `create_suggestion` 跨 archive+current 编号单调递增 + done sug frontmatter 硬门禁。
- chg-02：修复 `harness bugfix` 不写 `operation_type` / `operation_target` 到 runtime.yaml 的问题，load 侧懒回填 + save 白名单显式保留。
- chg-03：`harness archive` 支持 bugfix 目录并一次性 sweep bugfix-3/4/5/6，active_requirements 同步清空。
- chg-04：恢复 `harness_workflow` cycle-detection 7 个符号，`tests/test_cycle_detection.py` 从 smoke 恢复为 24 条完整用例。
- chg-05：新增 `harness validate --human-docs` CLI + stage-role.md 契约 6 sug frontmatter 硬门禁。
- chg-06：README.md + README.zh.md 增补 `pip install -U harness-workflow` 下游模板刷新提示。
- chg-07：对人文档端到端 smoke（FullLifecycleSmokeTest）+ req-28 自身 18/18 首次完整示范。

## 结果是什么

- **整体验收：有条件通过**。7 AC 中 5 条（AC-09/10/12/13/15）完全通过，2 条（AC-11/14）有条件通过。
- **测试**：137/137 全过，跳过 36（legacy version-flow 遗留，不在本 req 范围）；新增独立补测 3 条全绿。
- **对人文档**：18/18 全部落盘（需求摘要 + 测试结论 + 验收摘要 + 交付总结 + 7×变更简报 + 7×实施说明），AC-11 首次完整示范闭环。
- **遗留**：AC-14 物理归档落 legacy `.workflow/flow/archive/main/bugfixes/` 而非 AC-14 文本要求的 `artifacts/main/archive/bugfixes/`，sug-08 已承接。

## 后续建议

- 新开 bugfix-7 消费 sug-08，修 `resolve_archive_root` 判据（legacy 非空时新 bugfix 仍应落 primary 路径），可选加 `harness migrate --archive` 子命令。
- sug-01-ff-auto（ff --auto 模式）待下一轮 apply 消费，与本需求无硬依赖。
- 下游仓库用户如需用最新 `harness change / plan` 模板：`pip install -U harness-workflow` 即可，无需额外 harness CLI 子命令。
