# 需求摘要：req-28 harness CLI 闭环修复 + 对人文档 SOP 收束

## 目标

- 修完 harness CLI 闭环的 4 个 bug（bugfix / archive / suggest 路径），并补齐对人文档 SOP 的 3 个遗留（落盘验证、下游模板刷新、首次完整示范）。

## 范围

- **做**：sug-12 bugfix 在 runtime.yaml 写入并持久化 `operation_type` / `operation_target`。
- **做**：sug-13 恢复 cycle-detection 6 个符号与完整测试用例。
- **做**：sug-14 `harness archive` 支持 bugfix 目录并清理 active 列表。
- **做**：sug-15 `harness suggest --delete/--apply` 补 filename fallback + done 阶段 sug frontmatter 硬门禁。
- **做**：sug-09 / sug-10 / sug-11 对人文档落盘校验、下游刷新策略、本 req 自身作为首次完整示范。
- **不做**：sug-08 回溯（已现场清理）、bugfix-3/4/5/6 主动归档 sweep、下游真实远程推送。

## 验收要点

- **AC-12 / AC-13 / AC-14 / AC-15**（档 1，硬阻塞）：CLI 四条闭环 bug 全部修复，`harness bugfix → next → archive → suggest` 打通，cycle-detection 测试从 smoke 恢复为完整用例。
- **AC-09**（档 2）：提供可跑的对人文档落盘校验（CLI / smoke / checklist 任一）。
- **AC-10**（档 2）：产出下游仓库 `harness change` 模板刷新指南，并视情况新增 `harness update` 开关。
- **AC-11**（档 2）：req-28 自身全程产出 6 份对人文档，并由 AC-09 校验反向覆盖。

## 风险

- 档 1 的 sug-12 / sug-14 / sug-15 都涉及 CLI 持久化与目录扫描逻辑，修复点分散且互相耦合，planning 阶段需明确拆分粒度以避免回归。
- AC-11 依赖 AC-09 的校验机制先行可用，如 sug-09 延后，AC-11 只能降级为人工检视，影响"首次完整示范"的可信度。
