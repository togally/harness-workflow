# 验收摘要：req-28 harness CLI 闭环修复 + 对人文档 SOP 收束

## AC 核对结果

| AC 编号 | 通过 | 证据 / 备注 |
|--------|------|------------|
| AC-09 | 通过 | `harness validate --human-docs --requirement req-28` CLI 存在且能运行；当前输出 16/18（缺 验收摘要 / 交付总结，符合 acceptance 阶段预期）；校验失败明确列出缺失文件路径。tests/test_validate_human_docs.py 8 条单测全绿。 |
| AC-10 | 通过 | README.md:64 + README.zh.md:77 均含 `pip install -U harness-workflow` 提示；tests/test_smoke_req28.py 的 `test_readme_has_pip_upgrade_hint` 已覆盖。 |
| AC-11 | 有条件通过 | req-28 作为首次示范：需求级 2/4（需求摘要 / 测试结论），本阶段 +1 = 3/4；change 级 14/14 全部产出。剩 1 份（交付总结）由 done 阶段补齐后达 18/18。 |
| AC-12 | 通过 | create_bugfix 写入 operation_type/operation_target；load_requirement_runtime 懒回填（round-trip 5 次验证）；save 白名单显式保留。tests/test_bugfix_runtime.py 4/4 + tests/test_req28_independent.py 独立补测过。 |
| AC-13 | 通过 | 7 符号（CallChainNode / CycleDetector / CycleDetectionResult / detect_subagent_cycle / report_cycle_detection / get_cycle_detector / reset_cycle_detector）全部导出；tests/test_cycle_detection.py 从 smoke 恢复为 24 条完整用例，全绿。 |
| AC-14 | 有条件通过 | 功能目标达成：`harness archive` 支持 bugfix + sweep 成功清理 bugfix-3/4/5/6 + active_requirements 同步清空 + tests/test_archive_bugfix.py 4/4 过。**偏差**：物理归档落到 legacy `.workflow/flow/archive/main/bugfixes/`，而非 AC-14 文本要求的 `artifacts/main/archive/bugfixes/`（resolve_archive_root 在 legacy 非空时降级）。 |
| AC-15 | 通过 | harness suggest --apply / --delete / --archive 三处 filename fallback 齐全；create_suggestion 跨 archive+current 单调递增（本次新建 sug-08 正确递增，验证 bug 已修）。tests/test_suggest_cli.py 3/3 过。 |

## 是否通过

**整体判定：有条件通过**。建议动作：**起 sug-08 承接 AC-14 路径偏差后进入 done 阶段**；AC-11 的"交付总结.md"在 done 阶段同步产出即闭环。

## 未达项处理建议

- **AC-14 archive 路径偏差**：已产出 sug-08-archive-path-legacy-fallback，建议在 req-28 done 后新开 bugfix 承接 resolve_archive_root 改判据 + 可选 `harness migrate --archive` 迁移子命令。owner：下一轮 executing；预期：1 个 bugfix 周期。
- **AC-11 交付总结.md 未产出**：由 done 阶段主 agent 按契约产出，无需额外动作。

## Excluded 反例核查

- commit aa0a8b8 未触碰 `.workflow/flow/archive/` 下任何历史归档（仅新增 bugfix-3/4/5/6 归档目录）。
- 未触碰 req-26 / bugfix-6 已归档产物。
- 结论：**OK**。

## 遗留

- sug-01-ff-auto（ff --auto 模式）仍在池内待消费，与本需求无硬依赖。
- sug-08-archive-path-legacy-fallback（本次新增，承接 AC-14 路径偏差）。
- create_suggestion 生成的 frontmatter 仅含 id/created_at/status 三字段，契约 6 要求的 title/priority 字段缺失，属 chg-01 已知限制，非本 req 验收卡点。
