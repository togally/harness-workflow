# 变更简报：chg-04 恢复 cycle-detection API 与完整测试

## 变更名

- 把 `harness_workflow` 模块对外的 cycle-detection 符号从 smoke 恢复为完整 API，并让 `tests/test_cycle_detection.py` 从 1 条 smoke 恢复为完整用例集。

## 解决什么问题

- 对应 AC-13：模块 re-export 目前缺 `CallChainNode` / `CycleDetector` / `CycleDetectionResult` / `detect_subagent_cycle` / `report_cycle_detection` / `get_cycle_detector` / `reset_cycle_detector` 等关键符号，外部调用方无法正常使用；完整测试已被裁剪为 smoke。

## 怎么做

- 以 bugfix-6 的 `diagnosis.md` / `实施说明.md` 为权威定符号清单。
- 从 git 历史（`5584656^` 附近）取回完整 `cycle_detection.py` 与 `test_cycle_detection.py`，贴回并适配当前代码路径。
- 在 `src/harness_workflow/__init__.py` re-export 全部符号。
- 跑 `pytest tests/test_cycle_detection.py` 直到全绿。

## 影响范围

- 代码：`src/harness_workflow/cycle_detection.py`（新增或恢复）、`src/harness_workflow/__init__.py`、`tests/test_cycle_detection.py`。
- 用户路径：外部 subagent 依赖 `from harness_workflow import ...` 的 cycle-detection 调用。

## 预期验证

- `python -c "from harness_workflow import CycleDetector, detect_subagent_cycle, ..."` 全部 import 成功。
- `pytest tests/test_cycle_detection.py -v` 用例数回到历史水准、全绿。
- 最小调用链：Level0 → Level1 → Level0，`detect_subagent_cycle` 正确返回 cycle 命中结果。
