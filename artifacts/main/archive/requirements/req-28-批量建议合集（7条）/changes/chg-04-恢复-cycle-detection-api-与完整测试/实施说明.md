# 实施说明：chg-04 恢复 cycle-detection API 与完整测试

## 实际做了什么

- 新建 `src/harness_workflow/cycle_detection.py`（225 行），恢复 7 个历史符号：`CallChainNode` / `CycleDetectionResult` / `CycleDetector` / `detect_subagent_cycle` / `report_cycle_detection` / `get_cycle_detector` / `reset_cycle_detector`；判定依据为 `agent_id` 重复（与 `5584656^` 历史语义一致）。
- 在 `src/harness_workflow/__init__.py` 与 `src/harness_workflow/tools/harness_cycle_detector.py` 同时 re-export 上述 7 个符号，兼容 `from harness_workflow import ...` 与旧 CLI 模块路径两种 import。
- 重写 `tests/test_cycle_detection.py`（390 行，24 用例），按 5584656^ 历史覆盖面恢复：dataclass 构造、CycleDetector 单例 get/reset/add/pop/snapshot/clear、`detect_subagent_cycle` 无状态函数（含 dict 链兼容）、`report_cycle_detection` 日志落盘（含 has_cycle=False noop）、AC 场景 A→B→A / A→B→C→A / 兄弟节点不误判，再加一条 "同 role 不同 agent_id 不视为 cycle" 的判定锚点。
- 回归：`python3 -m unittest discover tests` → `Ran 120 tests, OK (skipped=36)`，较基线 97 增加 23 条，0 fail / 0 error。

## 未做与原因

- 未改 `harness_cycle_detector.py` CLI 本身的 JSON 持久化与 `main()` 行为；本 change 仅恢复 in-process API，CLI 语义保持不变。
- 未碰 chg-01/02/03/05/06/07 范围内文件，diff 严格限于 cycle-detection 模块、顶层 `__init__.py`、`tests/test_cycle_detection.py`、本 change 文档。

## 关键文件变更

- `src/harness_workflow/cycle_detection.py`：新增 225 行，定义 7 个符号与单例 plumbing。
- `src/harness_workflow/__init__.py`：re-export 7 个符号并加入 `__all__`。
- `src/harness_workflow/tools/harness_cycle_detector.py`：CLI 顶部从新模块 re-import 同名符号，兼容旧 import 路径。
- `tests/test_cycle_detection.py`：从 1 条 smoke 恢复为 24 条完整用例，覆盖所有 7 个符号。

## 已知限制

- `CycleDetector` 是进程级单例，不持久化到磁盘；需要跨进程/跨命令调用时仍应走 `harness_cycle_detector` CLI 的 `cycle-chain.json`。
- `report_cycle_detection` 在同一进程内毫秒级多次命中时使用带微秒的时间戳避免文件名冲突，但 `ns` 级并发场景未做额外保护。
