# 实施说明：chg-07 对人文档端到端 smoke + req-28 首次完整示范

## 实际做了什么

- 新增 `tests/test_smoke_req28.py`：tempdir 隔离环境下 6 条集成用例，覆盖 AC-07 / 09 / 10 / 11 / 12 / 13 / 14 / 15。
- `test_full_lifecycle_with_bugfix_and_archive` 串起 req 全链路（requirement_review → done → archive）+ bugfix 全链路（BUGFIX_SEQUENCE → archive --force-done）；过程中断言 operation_type 持久化、state yaml stage 同步、archive 路径无双层 branch、active_requirements 清理。
- 另 5 条用例分别独立覆盖 suggest filename fallback、跨 archive 编号单调、cycle-detection 7 符号导出与链式检测、validate_human_docs 区分 ok/missing、README 含 `pip install -U harness-workflow` 刷新模板提示。
- 直接 import `validate_human_docs` 统计 req-28 自身对人文档，作为 AC-11 "首次完整示范"证据：14/18 present（本 `实施说明.md` 落地后为 15/18，剩余 3 份 req-level 由后续 testing/acceptance/done 阶段产出）。

## 未做与原因

- 未跑真 `harness` CLI 子进程（briefing 硬约束，避免污染主仓 runtime / artifacts）。
- 未把 chg-07 本身的 regression 级 `回归简报.md` 纳入 smoke（change.md 风险 C 已写明 V1 不覆盖 regression 级完整校验）。

## 关键文件变更

- `tests/test_smoke_req28.py`（新增，376 行）：6 条集成用例，`python3 -m unittest tests.test_smoke_req28 -v` 全过；全仓测试 134 tests OK（基线 128 + 新增 6），0 fail / 0 error。

## 已知限制

- smoke 用 helper 层函数驱动，未经过 CLI argparse 层；CLI 参数解析已由 chg-01~05 的独立用例覆盖。
- req-28 对人文档仍缺 3 份 req-level（测试结论 / 验收摘要 / 交付总结），由后续 stage 推进时产出，AC-11 示范完整度 ≥ 83%。
