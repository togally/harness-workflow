# 变更简报：chg-07 对人文档端到端 smoke + req-28 首次完整示范

## 变更名

- 交付端到端 smoke（tempdir 跑完整 req + bugfix 生命周期），并把 req-28 自身变成对人文档产出链的首次完整示范。

## 解决什么问题

- 对应 AC-11（首次完整示范）与 AC-07（端到端）：当前 6 份对人文档没有真实跑过的端到端 smoke；req-28 自身是首次有机会在各 stage 真实产出对人文档的需求。

## 怎么做

- 新增 `tests/test_human_docs_e2e.py`，在 tempdir 初始化 harness 仓库，按 stage 顺序推进并在每 stage 产对人文档。
- 在 smoke 中直接 import chg-05 的 `validate_human_docs`，断言全 ok。
- 新增 bugfix 分支 smoke，覆盖 `回归简报.md` + bugfix 的 `实施说明.md` / `交付总结.md`。
- req-28 自身在各 stage 推进时真实产出 6 份对人文档（planning 阶段本 change 同目录的 `变更简报.md` 已产出）。

## 影响范围

- 代码：`tests/test_human_docs_e2e.py`（新文件）、`pytest.ini` / `pyproject.toml` 的 marker 注册。
- 用户路径：端到端 smoke（仅 CI / 手动运行）。
- req-28 的 artifact 树：每个 stage 都会产出对应对人文档。

## 预期验证

- `pytest tests/test_human_docs_e2e.py -v -m slow` 全绿。
- req-28 推进到 done 时 `harness validate --human-docs --requirement req-28` 全 ok。
- 手动检查 `artifacts/main/requirements/req-28-*/` 下 6 份对人文档齐全。
- bugfix 分支 smoke 的 `回归简报.md` / bugfix 的 `实施说明.md` / `交付总结.md` 均能被校验通过。
