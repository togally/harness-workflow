# 实施说明：chg-01 修复 harness suggest CLI filename fallback 与编号单调递增

## 实际做了什么

- Step 2：`apply_suggestion` / `delete_suggestion` / `archive_suggestion` 三函数统一匹配分支，frontmatter `id:` 优先、`sug-NN` filename 前缀 fallback。
- Step 3：`_next_suggestion_id` 同时扫 `.workflow/flow/suggestions/` 与 `archive/` 子目录，取最大 `sug-NN` 编号 +1，防止清空后回滚撞历史。
- Step 4：`done.md` 追加 "sug 文件 frontmatter 硬门禁"章节（id / title / status / created_at / priority），并在禁止行为中加硬条目。
- Step 4：`stage-role.md` 对人文档契约新增"契约 6：sug 文件 frontmatter 统一约定"。
- Step 5：scaffold_v2 对应 `done.md` / `stage-role.md` 同步补丁；diff = 0。
- Step 6：新增 `tests/test_suggest_cli.py`，3 条用例全部通过（delete fallback / apply fallback / create 跨 archive 递增）。

## 未做与原因

- plan.md Step 4 中"create_suggestion 写入时缺 frontmatter 抛异常"未落地：briefing 的核心交付清单把写入门禁限定为 SOP 文档（chg-05 双轨风格），未含代码 raise 校验；保留由下游 reviewer / done 阶段拦截。
- `harness suggest --list` 输出格式未动（Excluded）。

## 关键文件变更

- `src/harness_workflow/workflow_helpers.py` L2920-2938（`_next_suggestion_id` 重写跨 archive 扫描）/ L3050-3078（`apply_suggestion` fallback）/ L3087-3107（`delete_suggestion` fallback）/ L3107-3127（`archive_suggestion` fallback，移除脆弱的 `split("-")`）。
- `.workflow/context/roles/done.md` + `src/harness_workflow/assets/scaffold_v2/.workflow/context/roles/done.md`：追加 sug frontmatter 硬门禁章节与禁止行为条目。
- `.workflow/context/roles/stage-role.md` + scaffold_v2 镜像：追加契约 6。
- `tests/test_suggest_cli.py`：新增 3 条用例。

## 已知限制

- 存量无 frontmatter 历史 sug 不会被自动回填，仅保证 CLI 可操作；如需回填由后续 sug 专项处理。
- fallback 依赖文件名严格以 `sug-NN` 开头，异常命名仍会被忽略（风险 A，已在 change.md 中声明）。
