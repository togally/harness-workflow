# 实施说明：chg-06 下游模板刷新提示文档

## 实际做了什么
- Step 1/2：在 `README.md`（英文）`Installation` 节的 `--force` 说明之后，新增 "Refreshing change / plan templates" 小节，共 9 行（含空行），包含 `pip install -U harness-workflow` 升级命令与"历史 change 是一次性快照不更新"的说明。
- Step 1/2：在 `README.zh.md`（中文）`安装` 节的 `harness install` 之后，同步新增"刷新 change / plan 模板到最新版本"小节，共 9 行，内容与英文版对齐。
- Step 4：本 change 仅改文档，无新增单测；以 `grep "pip install -U harness-workflow" README*.md` 静态验证，两处命中（README.md:64、README.zh.md:77）。

## 未做与原因
- Step 3（可选同步 CLI help text）未执行：硬约束明确"不碰任何代码 / CLI"，且 Step 3 本身就是 plan 中的可选步骤，跳过。

## 关键文件变更
- `README.md`：新增 "Refreshing change / plan templates" 小节（位于 Installation 节末尾、Core Commands 节之前）。
- `README.zh.md`：新增 "刷新 change / plan 模板到最新版本" 小节（位于 安装 节末尾、核心命令 节之前）。

## 已知限制
- 未覆盖从源码（editable mode）安装场景的说明，按 change.md Scope Excluded 留作未来 sug。
- 未同步 `harness update --help` 文案；若后续希望 CLI help 与 README 一致，需另开 change 触碰代码。
