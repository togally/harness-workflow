# 实施说明：chg-02 修复 harness bugfix / next 闭环（operation_type 持久化，AC-12）

## 实际做了什么

- Step 2/3：在 `save_requirement_runtime` 的 `ordered_keys` 白名单中加入 `operation_type` / `operation_target` / `stage_entered_at`，避免 `create_bugfix` 刚写入的这两个字段在首次 save→load 往返中被 `save_simple_yaml` 按白名单裁剪。
- Step 2：在 `load_requirement_runtime` 增加懒回填，若 `current_requirement` 以 `bugfix-` / `sug-` 开头而 `operation_type` 为空，自动推断为 `bugfix` / `suggestion`（其余按 `requirement`），并用 `current_requirement` 回填 `operation_target`。显式值绝不被覆盖。
- Step 3：`create_bugfix`（workflow_helpers.py:3375-3376）本已写入 operation_type，本次修复让其真正被持久化。
- Step 6：新增 `tests/test_bugfix_runtime.py`，4 条用例，全部通过。

## 未做与原因

- 未新增独立的 `scripts/backfill_bugfix_runtime.py`：采用 plan.md 给出的"更简单方案——懒回填"。load 层自愈足以覆盖历史 bugfix-3/4/5/6 的缺字段情况，避免一次性脚本的命令入口维护成本。

## 关键文件变更

- `src/harness_workflow/workflow_helpers.py:338-387`：`load_requirement_runtime` 增加懒回填；`save_requirement_runtime` 扩展 `ordered_keys` 白名单。
- `tests/test_bugfix_runtime.py`：新增，覆盖 create / round-trip / stage sync / 懒回填四条路径。
- 未触碰 chg-01 的 suggest CLI 代码；未触碰 `.workflow/flow/` 与 `artifacts/bugfixes/` 历史数据。

## 已知限制

- 懒回填基于 id 前缀，若未来新增 `operation_type`（如 `experiment`）需同步扩展前缀映射。
- 仍依赖 chg-03 sweep 修正 bugfix-3/4/5 的 `stage=done/force-done` 归档，本 change 不处理历史 stage 回推。
