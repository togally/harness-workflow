# 测试结论：req-29 批量建议合集（2条）

## 通过/失败统计

- 全仓用例总数：171
- 通过：135（OK）
- 失败：0
- 跳过：36（与 req-29 无关的平台/环境跳过用例）
- req-29 新增模块专项（test_archive_path / test_migrate_archive / test_decision_log / test_ff_auto / test_smoke_req29）：36/36 全过。

## 关键失败根因

- 无。

## 未覆盖场景

- `harness ff --auto` 在真实仓端到端跑通（briefing 明令禁止，已由 tempdir smoke 代偿）。
- `harness migrate archive` 在真实本仓的 legacy 数据上落盘（仅 tempdir 验证，真迁移留给主 agent 手动执行）。
- 跨设备 `shutil.move` 中断留下半成品状态的回滚路径（chg-02 已知限制，依赖人工 reconcile）。
- `--auto-accept` 在真 TTY 环境下的交互 prompt 拼接文案（纯 mock input/output，未覆盖真实终端）。
- `决策汇总.md` 的 slug 兜底在多 req 目录名冲突时的选择策略（当前仅做目录优先匹配，无多冲突用例）。

## 风险评估

可上线。AC-01~04 均有对应单元+集成测试覆盖，smoke 测试 171/171 零回归；主要已知限制集中在"真实仓执行"和"跨设备 IO"两类工程外沿，不影响默认路径正确性。

## AC 覆盖评价（一行一条）

- **AC-01**（ff --auto 主路径）：`test_ff_auto.py` 7 条 + `test_smoke_req29.py::test_ff_auto_advances_to_before_acceptance` 整合覆盖，stage 推进与 acceptance 前停均断言到位。
- **AC-02**（决策点数据契约）：`test_decision_log.py` 14 条覆盖 append/roundtrip/三档分组/写盘，字段契约完整。
- **AC-03**（判据修复）：`test_archive_path.py::ResolveArchiveRootTest` + `ArchiveIntegrationTest` + smoke 3 条用例覆盖"legacy 非空仍落 primary"。
- **AC-04**（一次性迁移）：`test_migrate_archive.py` 5 条（happy path/bugfix/dry-run/冲突/幂等）+ smoke 验证迁后无警告。
