# 验收摘要：req-29 批量建议合集（2条）

## AC 核对结果

| AC 编号 | 通过 | 证据 / 备注 |
|---------|------|-------------|
| AC-01（ff --auto 主路径 + acceptance 前停 + 批量确认） | [x] | `src/harness_workflow/ff_auto.py` 的 `workflow_ff_auto` 按 stage sequence 推进且 acceptance 前停；`tests/test_ff_auto.py` 7 条 + smoke 集成断言；CLI 层 `--auto-accept` 必须配合 `--auto` 有 argparse 校验。 |
| AC-02（决策点数据契约） | [x] | `src/harness_workflow/decision_log.py` 定义 `DecisionPoint`（id/ts/stage/risk/options/choice/reason/extra）；日志 fenced YAML block 追加；`tests/test_decision_log.py` 14 条覆盖；`stage-role.md` 契约 3 已增"决策汇总.md"行。 |
| AC-03（resolve_archive_root 判据修复） | [x] | `workflow_helpers.py::resolve_archive_root` 默认 primary 优先，opt-in 走 `prefer_legacy` 参数或 `HARNESS_ARCHIVE_LEGACY=1`；`tests/test_archive_path.py` 新增 3 条用例全过。 |
| AC-04（一次性迁移命令） | [x] | `harness migrate archive [--dry-run]` 已并入 `tools/harness_migrate.py` argparse choices；`workflow_helpers.py::migrate_archive` 实现扫描 + 搬迁；`tests/test_migrate_archive.py` 5 条（happy/bugfix/dry-run/冲突/幂等）全过。 |
| §5.1 阻塞类别（8 条） | [x] | `decision_log.is_blocking_decision` 对 rm -rf / force push / 跨 req / archive 删 / 凭证 / 敏感配置 / 依赖变更 / schema 做 keyword 命中；smoke 验证 `rm -rf + risk=low` 仍拦截。 |
| §5.2 双轨日志 + 汇总 | [x] | flow 侧 `.workflow/flow/requirements/{req-id}/decisions-log.md` + artifacts 侧 `决策汇总.md` 分别有 append/write 实现，stage-role 契约已增行。 |
| §5.3 --auto-accept 三档语义 | [x] | `ff_auto.py::_should_auto_ack` 按 None/low/all 三档处理；阻塞类别强制交互（即使 all）；`test_ff_auto.py` 三档 ack 用例全过。 |

## 是否通过

- **通过**。建议动作：进入 done 阶段归档，后续由主 agent 手动执行 `harness migrate archive` 清理本仓 legacy 归档。

## Excluded 反例

- git log 显示 req-29 的 commit（015b9d3）仅触碰 `src/harness_workflow/{cli,decision_log,ff_auto,workflow_helpers,tools/harness_migrate}.py` + 5 个 tests + stage-role.md（本仓 + scaffold）+ runtime.yaml + feedback.jsonl，完全落在 chg-01~05 的 Included 范围内。
- 未改 `resolve_requirement_root` / `resolve_bugfix_root`（chg-01 Excluded 承诺）。
- 未回溯修改 req-26/27/28 / bugfix-3/4/5/6 的归档内容（requirement.md §3.2 承诺）。
- 未发布 Python 包、未扩展 `--auto` 到 acceptance 之后（§3.2 承诺）。
- 工作区 diff 仅 `.workflow/state/runtime.yaml` + `.harness/feedback.jsonl`（stage 切换副产物）。

## 未达项处理建议

- 无未达 AC。两项遗留工程项（主 agent 在真实本仓跑 `harness migrate archive` 收敛 legacy；真 TTY 下手测 `--auto-accept` 交互）建议 done 阶段作为 follow-up 记入 done-report。

## harness validate --human-docs 落盘核查

- 当前 12/14（缺 验收摘要.md、交付总结.md）；本 stage 产出本文件后为 13/14，done 阶段产出 交付总结.md 后达 14/14。
