# 交付总结：req-29 批量建议合集（2条）

## 需求是什么

为 `harness ff` 新增 `--auto` 自主推进模式（requirement_review → testing 全流程，acceptance 前停下批量 ack），并修复 `resolve_archive_root` 在 legacy 非空时错误降级的 bug、提供 `harness migrate archive` 一次性迁移历史归档到 primary。

## 交付了什么

- **chg-01**：`resolve_archive_root` 改为 primary 始终优先，`prefer_legacy` / `HARNESS_ARCHIVE_LEGACY=1` 双 opt-in；新增 3 条单测。
- **chg-02**：新增 `migrate_archive` helper + `harness migrate archive [--dry-run]` CLI 子命令，5 条单测（含幂等/冲突）。
- **chg-03**：新建 `decision_log.py`，定义 `DecisionPoint` 数据模型、日志 append/load/summary 渲染、阻塞检测；14 条单测；`stage-role.md`（本仓 + scaffold）契约 3 增"决策汇总.md"行。
- **chg-04**：`cli.py` 的 ff 子命令加 `--auto` + `--auto-accept {low,all}`；新建 `ff_auto.py` 实现主循环、三档 ack、阻塞强制交互、acceptance 前汇总落盘；7 条单测。
- **chg-05**：新增 `tests/test_smoke_req29.py` 5 条 smoke，整合验证 AC-01~04 + §5.1 阻塞 + 对人文档 checklist。
- **对人文档**：5 份《变更简报.md》+ 5 份《实施说明.md》+《需求摘要.md》+《测试结论.md》+《验收摘要.md》+《交付总结.md》= 14 份全量落盘。

## 结果是什么

- 验收：全部 AC（AC-01~04）+ §5.1/5.2/5.3 规约逐条 [x] 通过；整体判定"通过"。
- 测试：全仓 `python3 -m unittest discover tests` 171/171 OK（36 skipped），零回归；req-29 专项 36/36 全绿。
- 影响面：`harness ff --auto` 新增自主推进能力；归档默认落 primary，老仓升级需用户主动跑 `harness migrate archive` 收敛 legacy。
- 遗留：主 agent 需在本仓真跑一次 `harness migrate archive` 完成 legacy → primary 物理迁移；真 TTY 下手测 `--auto-accept` 交互文案。

## 后续建议

- **建议 1**：产出新 sug 扩展 §5.1 阻塞清单（当前仅 8 类 keyword 命中，建议补充语义级规则库和白名单机制）。
- **建议 2**：把 `append_decision` 自动钩入 subagent 产出决策点的标准工具路径，避免依赖人类记忆手动调用。
- **建议 3**：为 `shutil.move` 跨设备中断设计幂等回滚（当前依赖人工 reconcile，与 migrate_requirements 同风险级别）。
