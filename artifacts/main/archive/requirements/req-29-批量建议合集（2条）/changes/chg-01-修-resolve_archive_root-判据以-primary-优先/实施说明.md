# 实施说明：chg-01 修 resolve_archive_root 判据以 primary 优先

## 实际做了什么
- Step 1：扫描 `resolve_archive_root` 调用点，确认仅 `archive_requirement`（`workflow_helpers.py:4518`）一处，无 tests 依赖老降级语义。
- Step 2：改判据为"primary 始终优先"；新增关键字参数 `prefer_legacy: bool = False` 与环境变量 `HARNESS_ARCHIVE_LEGACY=1` 双 opt-in，默认路径下把老的 `using legacy archive path` warning 改为 `notice: using primary archive path; legacy at ... has data, run harness migrate archive to consolidate` 一次性迁移提示；opt-in 分支保留原 warning。
- Step 3：补导入 `os`；扩展 `tests/test_archive_path.py` 新增 3 条用例（默认 primary、参数/环境变量 opt-in、legacy 非空时新归档仍落 primary 的集成）。
- Step 4：跑 `python3 -m unittest tests.test_archive_path -v` → 5/5 通过；`python3 -m unittest discover -s tests` → 140/140 通过（36 skipped）。

## 未做与原因
- 不改 `resolve_requirement_root` / `resolve_bugfix_root`（scope 外，按 change.md Excluded）。
- 不实施 `harness migrate archive` 子命令（chg-02 负责）。
- 不迁移 legacy 下已有归档数据（由 chg-02 的 migrate 完成）。

## 关键文件变更
- `src/harness_workflow/workflow_helpers.py`：新增 `import os`；`resolve_archive_root`（原 4280-4305 行）重写判据，默认 primary 优先，opt-in 才走 legacy。
- `tests/test_archive_path.py`：头部注释补充 AC-03 覆盖说明；新增 `ResolveArchiveRootTest`（2 条）+ `ArchiveIntegrationTest`（1 条）。

## 已知限制
- 老仓升级后会进入"老归档在 legacy、新归档在 primary"的双轨过渡态，需用户在 chg-02 合入后运行 `harness migrate archive` 收敛；本 change 只打迁移提示，不做自动搬迁。
- opt-in 同时支持参数和环境变量，调用者需避免同时设置引发歧义；二者任一命中即视为 opt-in。
