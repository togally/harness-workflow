# 实施说明：chg-05 端到端 smoke 测试与对人文档示范

## 实际做了什么

- Step 1~4：新增 `tests/test_smoke_req29.py`，用 tempdir 隔离 + `mock.patch _get_git_branch` 固定 `main`，跑 5 条 smoke 用例：archive 默认落 primary（AC-03）、migrate 把 legacy 搬到 primary 且幂等（AC-04）、`workflow_ff_auto(auto_accept="all")` 推进 stage 到 acceptance 前停并产出三档决策汇总（AC-01 + AC-02）、`is_blocking_decision` 对 `rm -rf` + `risk=low` 仍返回 True（§5.1 阻塞清单）。
- Step 5：产出本《实施说明.md》作为 AC-11 对人文档示范；与之同时，smoke 里新增 `test_human_docs_checklist_for_req29` 只读检查 req-29 artifacts 目录，断言《需求摘要.md》+ 5 份《变更简报.md》+ 5 份《实施说明.md》齐全。
- 验证：`python3 -m unittest tests.test_smoke_req29 -v` 5/5 通过；全量 `python3 -m unittest discover tests` 171/171 通过，零回归。

## 未做与原因

- 未在本仓真跑 `harness ff --auto` / `harness migrate archive`：briefing 硬约束，所有断言都在 tempdir。
- 未改 chg-01 ~ chg-04 的生产代码或测试：本 change 是收尾 smoke，只新增 `tests/test_smoke_req29.py` 与本《实施说明.md》。

## 关键文件变更

- `tests/test_smoke_req29.py`（新增）：5 条端到端 smoke，覆盖 AC-01 ~ AC-04 整合 + §5.1 阻塞 + AC-11 对人文档 checklist。
- `artifacts/main/requirements/req-29-批量建议合集（2条）/changes/chg-05-端到端-smoke-测试与对人文档示范/实施说明.md`（新增，本件）：executing 阶段对人文档，补齐 req-29 五份《实施说明.md》的最后一份。

## 已知限制

- AC-11 checklist 用相对路径定位 `REPO_ROOT / artifacts/main/requirements/req-29-…`，仅在本仓内有效；新仓复用时需同步改目录名。
- `test_archive_lands_in_primary_by_default` 内置 `archive_requirement` 的 Git 自动提交会被 `builtins.input` mock 吃掉交互，但若未来归档流程改为无交互分支，patch 可移除。
