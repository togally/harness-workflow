# 交付总结：bugfix-3 install/update 仅更新当前选定 agent + `.harness/feedback.jsonl` 落层归位

## 需求是什么
用户两条并发诉求：
1. `harness install --agent <X>` 选定后，之后的 `install` / `update` 应**只作用于** `.X/` 目录，不再全量刷新 `.codex/ .claude/ .qoder/ .kimi/`——要求通过状态层字段记录。
2. `/Users/jiazhiwei/claudeProject/PetMallPlatform/.harness/feedback.jsonl` 游离于六层架构外，应归位到合适的层（或证实无用后删除）。

## 交付了什么
- **状态层字段新增**：`.workflow/state/platforms.yaml` 引入 `active_agent` 字段（与 `enabled[]` 解耦），`install_agent` 自动持久化。
- **全链路 agent 作用域收敛**：`_project_skill_targets` / `_managed_file_contents` / `install_local_skills` / `_sync_requirement_workflow_managed_files` / `update_repo` / `install_agent` 同时接 `active_agent`；CLI 新增 `--agent X` 一次性覆盖 + `--all-platforms` escape hatch；无 `active_agent` 时回退 `enabled[]` 兼容模式 + warning，不破坏老仓。
- **feedback.jsonl 归位**：常量 `FEEDBACK_DIR = .workflow/state/feedback/`；`update_repo` 一次性迁移旧 `.harness/feedback.jsonl → .workflow/state/feedback/feedback.jsonl` + 空目录 rmdir（数据连续、不覆盖已有）；消费者 `harness_export_feedback` + scaffold_v2 两处文档同步更新。
- **TDD 6 条用例**：3 条 executing 核心（持久化 / 作用域 / 落层） + 3 条 testing 边界（compat warning / 迁移不覆盖 / 一次性 flag 不持久化）。
- **对人文档 5/5 ✓**：回归简报 / 实施说明 / 测试结论 / 验收摘要 / 交付总结。
- **4 条衍生 sug**（sug-18~21）+ 2 条经验沉淀（executing.md 经验九、十）。

## 结果是什么
- **验收 pass**：bugfix.md 5 项 Validation Criteria 全通过；全量 `pytest` **152 passed / 50 skipped / 1 pre-existing failure**（`test_smoke_req29`，HEAD baseline 已独立认证）；烟测 6 步端到端（install → update → `.harness/` rmdir → feedback 迁移 → `--all-platforms` escape → `harness feedback` 读新位置）全绿。
- **影响面**：
  - 所有 harness 用户：下次 `harness update` 自动归位 `.harness/feedback.jsonl`，后续 install/update 自动单 agent 作用域；多 agent 并用的用户可加 `--all-platforms` 保持旧行为。
  - 主仓自己：182 行 feedback.jsonl 将在下次真实 update 迁移，git diff 会是 `rm` + `add` 组合，属预期。
- **遗留**：bugfix-3 ID 与上一轮冲突（CLI 分配器只扫活跃目录），Q4 按约定另立 sug-19 跟踪；testing subagent 超时暴露 ff 粒度问题（sug-18）。

## 后续建议
1. 下次 `harness update` 后检查主仓 `.harness/` 是否已清空，git 提交迁移 diff。
2. 推进 sug-18（ff 子任务拆分粒度），避免未来 subagent 长任务再次超时。
3. 推进 sug-19（bugfix ID 分配扫归档树），彻底消除 ID 复用。
