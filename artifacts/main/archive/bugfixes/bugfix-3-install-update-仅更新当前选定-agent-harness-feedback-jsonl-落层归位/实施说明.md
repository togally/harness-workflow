# 实施说明：bugfix-3 install/update 仅更新当前选定 agent + .harness/feedback.jsonl 落层归位

## 实际做了什么
- 新增 `platforms.yaml.active_agent` 字段 + `read_active_agent` / `write_active_agent` helper，`install_agent` 末尾持久化选定 agent；`update_repo` 读 `active_agent` 后只刷新对应 agent 的 skill/commands，`_managed_file_contents` / `install_local_skills` / `_project_skill_targets` 全部接 `active_agent` 参数；CLI 新增 `--agent X` 一次性覆盖 + `--all-platforms` escape hatch。
- 常量 `FEEDBACK_DIR` 从 `Path(".harness")` 改到 `Path(".workflow") / "state" / "feedback"`，`record_feedback_event` 自动走新路径；`update_repo` 开头加一次性 `shutil.move` 旧 `.harness/feedback.jsonl → .workflow/state/feedback/feedback.jsonl`，迁移后 `.harness/` 空目录 `rmdir`；`harness_export_feedback.py` 同步读新路径。
- scaffold_v2 文档 2 处路径文案更新（`harness-manager.md:537` / `harness-export-feedback.md:8`）。
- 新增 3 条 TDD 红用例（`tests/test_active_agent_and_feedback_relocation.py`），红阶段先 FAIL 在根因上，绿阶段全部 PASS；同步调整 2 条既有 `test_cli.py` 用例用 `--all-platforms` 保留原测试意图（experience 七"同步行为变更的老断言主动改"）。
- 全量 pytest 149 passed / 50 skipped / 1 pre-existing failure（`test_smoke_req29`，零新增回归）；手动烟测 PetMall 副本 `install --agent claude + update` 路径全部符合预期。

## 未做与原因
- **bugfix-3 ID 复用问题**：Q4=A 约定另立独立缺陷，不在本轮范围。
- **PetMallPlatform 主仓数据**：仅用只读临时副本烟测，已 rm -rf；主仓 `.harness/feedback.jsonl`（182 行）迁移由下次真实 `harness update` 自动触发，代码层已就绪。
- **git commit / push / harness next**：按 briefing 要求不执行，留给主 agent 推进。

## 关键文件变更
- `src/harness_workflow/workflow_helpers.py`：常量 `FEEDBACK_DIR`/`FEEDBACK_LOG` 改指 `.workflow/state/feedback/`；新增 `LEGACY_FEEDBACK_DIR`/`LEGACY_FEEDBACK_LOG` 供迁移；新增 `read_active_agent` / `write_active_agent` / `_AGENT_TO_PLATFORM_KEY` / `_AGENT_TO_SKILL_DIR`；`_project_skill_targets` / `_managed_file_contents` / `install_local_skills` / `_sync_requirement_workflow_managed_files` / `update_repo` / `install_agent` 全链路接 `active_agent`；`update_repo` 加一次性数据迁移 + `force_all_platforms` / `agent_override` 参数；硬编码 "refreshed .codex/..." 改为按实际写入路径动态输出。
- `src/harness_workflow/tools/harness_update.py`：CLI 新增 `--agent` / `--all-platforms`，传入 `update_repo`。
- `src/harness_workflow/cli.py`：`update` 子命令新增 `--agent` / `--all-platforms` + 透传到 `harness_update.py`。
- `src/harness_workflow/tools/harness_export_feedback.py`：`log_path` 改指 `.workflow/state/feedback/feedback.jsonl`。
- `src/harness_workflow/assets/scaffold_v2/.workflow/context/roles/harness-manager.md`：路径文案同步。
- `src/harness_workflow/assets/scaffold_v2/.workflow/tools/catalog/harness-export-feedback.md`：路径文案同步。
- `tests/test_active_agent_and_feedback_relocation.py`：新增 3 条红用例（install_agent 持久化 / update 按 active agent 收敛 / feedback.jsonl 落层 + 迁移 + 消费）。
- `tests/test_cli.py`：2 条既有用例改用 `--all-platforms` 保留旧意图。

## 已知限制
- 旧仓（无 `active_agent`）首次 `harness update` 会打印 "active agent not set; refreshing enabled set (compat mode)" 并走 `enabled[]` 旧路径；下次 `harness install --agent X` 自动补齐 `active_agent` 后才进入单 agent 模式，符合 Q1=A 迁移友好约定。
- `_scaffold_v2_file_contents` 对 `CLAUDE.md` / `AGENTS.md` 的写入由 `include_claude` / `include_agents` 控制，不随 `active_agent` 收敛（根文档属于入口文件，不算 agent-private）；若未来需要按 agent 精确管控根 md，需另起变更。
