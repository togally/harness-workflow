# 验收摘要：bugfix-3 install/update 仅更新当前选定 agent + feedback.jsonl 落层归位

## AC 核对结果

| AC | 通过 | 证据 / 备注 |
|---|---|---|
| 1. 3 条原红用例全绿 | ✓ | 独立复跑 3 passed in 1.52s |
| 2. 全量 pytest 零新增回归 | ✓ | 152 passed；唯一 failure `test_smoke_req29` 经 git stash HEAD baseline 复跑仍 FAIL，认证 pre-existing |
| 3. install/update 单 agent 收敛 + feedback 迁移 | ✓ | 烟测 `/tmp/petmall-bugfix3-acceptance-smoke/`：`active_agent: claude` 落盘；`.harness/` rmdir；`.workflow/state/feedback/feedback.jsonl` 28 行 3748 bytes 零损耗迁移；`.codex/.qoder/.kimi` mtime 未变 |
| 4. `--all-platforms` escape hatch 兼容 | ✓ | `test_cli.py` 2 条既有用例改用 `--all-platforms` 全绿；testing 阶段烟测已端到端验证 |
| 5. `harness feedback` 从新路径读 | ✓ | testing 烟测步 d 导出成功；本次 acceptance 新路径保留 28 行连续数据 |

## 是否通过

**通过**，建议 ff 模式自主推进至 done 阶段归档。

## 未达项处理建议

| 项 | 处理建议 |
|---|---|
| `回归简报.md` 缺失（validate 2/5→3/5 后仍不足 4/5）| 本 bugfix 走 ff 跳过 regression 产出步，上报主 agent 判断是否豁免或由 done 阶段按 `regression/diagnosis.md` 内容补产 |
| 主仓 `.harness/feedback.jsonl`（182 行）未真实迁移 | 实施说明已交代由下次真实 `harness update` 自动触发；建议 done 阶段或后续任一 update 触发后核查新路径行数 ≥ 182 + 新增事件 |
