# 测试结论：bugfix-3 install/update 仅更新当前选定 agent + feedback.jsonl 落层归位

## 独立复跑结果
- **3 条原红用例**（`tests/test_active_agent_and_feedback_relocation.py`）：**3 passed in 1.49s**
  - `test_install_agent_persists_active_agent` ✓
  - `test_update_repo_only_refreshes_active_agent` ✓
  - `test_feedback_jsonl_writes_under_state_feedback` ✓
- **3 条边界用例**（`tests/test_active_agent_feedback_edge_cases.py`，testing 阶段补强）：**3 passed**
  - `test_update_repo_compat_mode_warning_when_active_agent_missing` — 旧仓无 active_agent 时 update 打 warning + 走 enabled[] 兼容路径 ✓
  - `test_feedback_migration_does_not_overwrite_existing_new_path` — 新位置已有数据时拒绝覆盖迁移 ✓
  - `test_update_repo_agent_override_is_one_shot_not_persisted` — `--agent X` 一次性覆盖不持久化 active_agent ✓
- **全量 pytest**：`PYTHONPATH=src python3 -m pytest -q --no-header` → **152 passed / 50 skipped / 1 pre-existing failure**（`test_smoke_req29::HumanDocsChecklistTest`，HEAD baseline 已知 / 零新增回归）

## 独立烟测结果（`/tmp/petmall-bugfix3-testing-smoke/`）
| 步 | 动作 | 结果 |
|---|---|---|
| a | `harness install --agent claude` | `platforms.yaml` 写入 `active_agent: claude` ✓ |
| b | `harness update` | `.claude/` refresh；`.codex/ .qoder/ .kimi/` mtime 未变 ✓ |
| b' | `.harness/feedback.jsonl` 迁移 | 旧目录 rmdir + 新位置 `.workflow/state/feedback/feedback.jsonl`（3748 bytes = 28 行数据连续）✓ |
| c | `harness update --all-platforms` | stdout `refreshed .codex/.claude/.qoder` 全部出现 ✓ |
| d | `harness feedback` 从新位置读 | 导出 `harness-feedback.json` 成功 ✓ |
| e | `rm -rf` 清理 | OK |

## 边界用例补强
testing 阶段在 executing 的 3 条基础上追加 3 条，覆盖 executing 未覆盖的风险点：
- **compat mode**：老仓（platforms.yaml 无 active_agent）不应破坏，回退 enabled[] 且打 warning
- **迁移安全带**：目标项目新位置已有 feedback 时不覆盖（防数据丢失）
- **一次性覆盖 flag 边界**：`--agent X` 是单次 run 覆盖，不应误写 active_agent

## 衍生问题 / 风险（上报主 agent）
- 主仓自己的 `.harness/feedback.jsonl`（182 行）迁移需要下次真实 `harness update` 触发；git 层会产生 `rm .harness/feedback.jsonl` + `add .workflow/state/feedback/feedback.jsonl`，属预期。
- bugfix-3 ID 复用（Q4）按约定另立独立缺陷，不在本轮（建议 done 阶段起 sug）。

## 结论
**testing pass**——产品 bug 全部修复且边界覆盖充分，零新增回归，烟测端到端通过。可推进 acceptance。
