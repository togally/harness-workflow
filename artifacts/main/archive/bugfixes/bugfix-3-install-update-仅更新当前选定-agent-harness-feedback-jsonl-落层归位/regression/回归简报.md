# 回归简报：bugfix-3 install/update agent 作用域 + .harness/feedback.jsonl 落层归位

## 问题
- install/update 跨 agent 全量刷新（用户选 claude，但 codex/qoder/kimi 仍被同步）；`.harness/feedback.jsonl` 落在六层架构之外。

## 根因
- 状态层缺"active agent"字段；`update_repo` 与 `_managed_file_contents` 写入范围由硬编码 + `platforms.yaml.enabled` 决定，不感知用户实际使用的 agent。`FEEDBACK_DIR = Path(".harness")` 早于六层架构成型，从未纳入 `.workflow/` 统一归位规则。

## 影响
- 命令：`harness install`、`harness update`、`harness next`、`harness ff`、`harness regression`、`harness feedback`
- 文件：`workflow_helpers.py`（FEEDBACK_DIR 常量 / `update_repo` / `install_local_skills` / `_managed_file_contents` / `_project_skill_targets`）、`tools/harness_export_feedback.py`、`assets/scaffold_v2/.workflow/context/roles/harness-manager.md`、`assets/scaffold_v2/.workflow/tools/catalog/harness-export-feedback.md`、`platforms.yaml`
- 数据：主仓 `.harness/feedback.jsonl`（182 行）+ PetMall（28 行）需迁移至 `.workflow/state/feedback/feedback.jsonl`，历史保留

## 路由决策
- `harness regression --confirm` → `testing`（两条都是实现/工程问题，且共享 `update_repo` 代码段，一并修复 + 补 ≥3 个 TDD 用例）

## 衍生发现（不在本 bugfix 范围）
- `bugfix-3` ID 已被历史复用 4 次：分配器未扫 `artifacts/main/archive/bugfixes/`。建议另立独立缺陷。
