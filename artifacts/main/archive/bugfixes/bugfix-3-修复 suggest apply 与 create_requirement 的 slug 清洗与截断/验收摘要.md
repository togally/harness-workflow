# 验收摘要：bugfix-3 修复 suggest apply 与 create_requirement 的 slug 清洗与截断

## AC 核对结果

| AC 编号 | 通过 | 证据 / 备注 |
|---|---|---|
| AC-1 `harness requirement "含/的 标题"` 单级 | [x] | `workflow_helpers.py` L3301-3302 `_path_slug`；`test_slug_paths.py::test_slash_in_title_does_not_create_nested_dir`；E2E 场景 2 `req-02-含-斜杠-的-标题/` 单级 |
| AC-2 `harness suggest --apply sug-08` 单级 + sug 归档 + frontmatter applied | [x] | `workflow_helpers.py` L3116-3131；`test_slug_paths.py::test_apply_sug08_style_long_line_...`；E2E 场景 1 全部断言通过 |
| AC-3 长 title slug ≤ 60，state yaml title 保留原文 | [x] | `_path_slug` L2193 `[:60].strip("-")`；`test_slug_paths.py::test_long_title_slug_is_truncated_but_state_keeps_original`（240 字 → slug ≤60，state 保留 240 字原文） |
| AC-4 `harness bugfix "含/的 标题"` 单级 | [x] | `workflow_helpers.py` L3365-3366；`test_slug_paths.py::test_slash_in_bugfix_title_...`；E2E 场景 3 `bugfix-1-含-路径-的-缺陷/` 单级 |
| AC-5 全量 discover 与基线一致无回归 | [x] | 本轮复跑 `python3 -m unittest discover tests` → 180 tests / 1 pre-existing failure / 36 skipped，零新增回归（failure=`test_human_docs_checklist_for_req29`，与本 bugfix 无关） |
| AC-6 新增 3+ slug 清洗单测全绿 | [x] | 开发者 5（`test_slug_paths.py`）+ 测试工程师 4（`test_slug_paths_extra.py`）= 9 条全绿，超过 3+ 下限 |

Regression 根因修复：4/4 全部到位（`apply_suggestion` 截断、`create_requirement` slug 清洗、`create_bugfix` slug 清洗、sug 文件归档 + frontmatter 翻转）。

## 是否通过

**AI 侧建议：通过**。6/6 AC 全部满足；regression 根因全部修复；零新增回归；状态一致性（runtime.yaml `stage=acceptance` ↔ state/bugfixes/bugfix-3-*.yaml `stage=acceptance`）无漂移。人工最终判定通过后建议动作：`harness next` → `done`。

## 未达项处理建议

无未达 AC。以下为不阻断本次验收、但建议在流转到 done 前/中处理的遗留项：

1. **《回归简报.md》缺失（regression 阶段对人文档契约 3）**：`harness validate --human-docs --bugfix bugfix-3` 显示 2/5（`实施说明.md` / `测试结论.md` 已落；`验收摘要.md` 随本次提交补齐；`交付总结.md` 属 done 产出；`回归简报.md` 为 regression 阶段历史遗留）。建议：主 agent 在推进 done 前临时派发 regression 角色在 `artifacts/main/bugfixes/bugfix-3-.../` 下补产《回归简报.md》即可；无需回退 stage。负责人：主 agent / 诊断师。预期完成：done 阶段前或随 done 一起产出。
2. **`apply_all_suggestions` 同源隐患（workflow_helpers.py L3261 附近）**：仍用旧 `f"{req_id}-{title}"` 拼接，属 bugfix-3 同类缺陷潜在复发点，已在《测试结论.md》记录。建议：done 阶段转 suggest 池新增跟踪 sug，不阻断本次验收。负责人：主 agent（done 阶段回顾）/ 后续开发者。预期完成：下一轮迭代。
