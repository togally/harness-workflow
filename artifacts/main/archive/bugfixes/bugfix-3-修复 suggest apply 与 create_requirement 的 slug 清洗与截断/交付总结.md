# 交付总结：bugfix-3 修复 suggest apply 与 create_requirement 的 slug 清洗与截断

## 需求是什么
- 修复 `harness suggest --apply` / `harness requirement` / `harness bugfix` 在接到含 `/` 或超长 title 时，把 raw title 直接拼进文件路径、造成多级嵌套 + state yaml 污染 + runtime.yaml 错配的 CLI 缺陷。

## 交付了什么
- **代码**：`src/harness_workflow/workflow_helpers.py` 新增 `_path_slug(title, max_len=60)` helper（复用 `slugify_preserve_unicode` + 长度上限 + strip 末尾 dash + 空串 id-only 回退）；改造 `create_requirement` / `create_bugfix` / `apply_suggestion` 三处入口；`apply_suggestion` 成功后 sug 源文件归档到 `flow/suggestions/archive/` + frontmatter `pending → applied`。
- **测试**：新增 `tests/test_slug_paths.py`（5 条 TDD 先红再绿）+ `tests/test_slug_paths_extra.py`（4 条覆盖扩展）；同步适配 `tests/test_cli.py` / `tests/test_suggest_cli.py` / `tests/test_smoke_req28.py` 的行为变更断言。
- **对人文档**：《实施说明.md》《测试结论.md》《验收摘要.md》《回归简报.md》《交付总结.md》五份齐全。

## 结果是什么
- **验收通过**：Validation Criteria 6/6 全 [x]，regression 根因 4/4 修复到位；主仓全量 180 tests / 1 pre-existing failure / 0 errors / 36 skipped，零新增回归（唯一 failure=`test_human_docs_checklist_for_req29`，与 bugfix-3 解耦）。
- **影响面**：修复后 raw title 含 `/`、反斜杠、Windows 非法字符、超长度均统一在入口层清洗为单级安全 slug；state yaml 的 `title` 字段保留原文，UI / 人类可读性不受损。
- **无遗留阻断项**：状态一致性 OK；对人文档 5/5 落盘（done 阶段补齐《回归简报.md》历史缺项）。

## 后续建议
1. 消化 sug-09（`harness next --execute`）/ sug-10（regression 对人文档契约自校验）/ sug-11（`apply_all_suggestions` 同源隐患下沉 `_path_slug`）/ sug-12（`create_suggestion` frontmatter 补齐 title+priority）；建议下一轮合批一个 "CLI 入口鲁棒性补丁" 需求。
2. 执行 `harness archive bugfix-3 [--folder <name>]` 归档本 bugfix，`active_requirements` 清空。
3. 在下一轮新需求开工前跑一次 `harness update` 检查 scaffold_v2 是否需要同步 `_path_slug` helper（避免作者仓历史污染再复发）。
