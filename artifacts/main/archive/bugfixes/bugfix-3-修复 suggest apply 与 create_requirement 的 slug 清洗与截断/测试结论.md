# 测试结论：bugfix-3 修复 suggest apply 与 create_requirement 的 slug 清洗与截断

## 通过/失败统计
- 单测总数 180 / 通过 143 / 失败 1（pre-existing `test_human_docs_checklist_for_req29`，与本 bugfix 无关）/ 错误 0 / 跳过 36。耗时 ~31.7s。
- bugfix-3 专项单测 `tests/test_slug_paths.py`（开发者 5 条）+ `tests/test_slug_paths_extra.py`（testing 补 4 条）共 9 条全绿。
- E2E 4 个真跑场景（apply sug-08 / harness requirement 含斜杠 / 长 title 72 字 / harness bugfix 含斜杠）在 `/tmp/harness-bugfix3-e2e-20260420-115447/` 全部 PASS。

## 关键失败根因
- 无。bugfix.md 的 4 条 Validation Criteria 逐条通过；唯一 failure 是 req-29 已知遗留问题，bugfix-3 不触发、不修复。

## 未覆盖场景
- 跨 `create_change` / `create_regression` 的 slug 清洗没在 bugfix-3 范围内（已由历史链正确调用 `slugify_preserve_unicode`，但未在本轮 E2E 里冒烟）。
- Windows 实机文件系统的大小写折叠（macOS case-insensitive、Linux case-sensitive 跨平台）未验证。
- `apply_all_suggestions` 批量路径未走 bugfix-3 改动，但同类长句打包时仍用旧 `dir_name = f"{req_id}-{title}"`（见 workflow_helpers.py L3261），属**同类缺陷潜在复发点**，未触发本 bugfix 但建议后续 change 跟进。
- `_path_slug(max_len)` 参数外部不可配置，团队若要调宽 > 60 需改源码；未覆盖"超长保留"需求场景。
- apply_suggestion 对已 applied 状态的 sug 二次 apply 的幂等行为（会再搬一次归档）未覆盖。

## 风险评估
可进入 acceptance。bugfix-3 修复精准命中 regression/diagnosis.md 定位的 3 个漏补点（`create_requirement` / `create_bugfix` / `apply_suggestion`）+ 1 个附加需求（sug 归档），9 条单测 + 4 条 E2E 同时守住"单级目录 / slug ≤ 60 / state yaml 保留原 title / sug 归档 + frontmatter 翻转"四条不变式，零新增回归。建议 acceptance 后新开 sug 跟踪 `apply_all_suggestions` 的同类风险。
