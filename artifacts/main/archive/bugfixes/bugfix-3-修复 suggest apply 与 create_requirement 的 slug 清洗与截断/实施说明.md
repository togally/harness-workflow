# 实施说明：bugfix-3 修复 suggest apply 与 create_requirement 的 slug 清洗与截断

## 实际做了什么

- Step 1（helper 提取）：在 `src/harness_workflow/workflow_helpers.py` 新增 `_path_slug(title, max_len=60)`，复用 `slugify_preserve_unicode` 并叠加长度上限 + 末尾 `-` 清理；不改动 `slug.py`。
- Step 2（create_requirement / create_bugfix 改造）：两处 `dir_name` 从 `f"{id}-{raw_title}"` 改为"走 `_path_slug` 清洗后的 slug 段，空则回退 id-only"；state yaml 路径同步用 `dir_name`；模板 `TITLE` 占位符仍用 raw title，保证对人可读。
- Step 3（apply_suggestion 改造）：`title = body.splitlines()[0][:60].strip() or suggest_id`；成功后将源 sug 文件 `target.replace(archive_dir / target.name)`，`archive/` 目录按需创建，随后在归档路径翻转 `status: pending` → `status: applied`；保持原有 `print` 输出。
- Step 4（TDD）：新增 `tests/test_slug_paths.py`（5 条）覆盖含 `/` title、超长 title、bugfix 同源清洗、apply_suggestion 归档 + 截断、边界回退；同步更新 `tests/test_cli.py` / `tests/test_suggest_cli.py` / `tests/test_smoke_req28.py` 中依赖旧行为的 3 条断言。
- 验证：新增 5 条先红后绿；全量 `python3 -m unittest discover tests` 176 tests / 1 failure（pre-existing `test_human_docs_checklist_for_req29`）/ 36 skipped，对齐基线（171 + 5 新增），零新增回归。

## 未做与原因

- 未清理生产仓里历史遗留的 `req-30-清理 .workflow/...` 3 级脏嵌套目录与 `.workflow/state/requirements/req-30-清理 .workflow/`：诊断阶段已备份 + 主 agent 已清理；本 bugfix 只负责源码修复。
- 未动 `slug.py` / `scaffold_v2/` / `runtime.yaml`：briefing 硬约束。
- 未真跑 `harness suggest --apply sug-08` 端到端：全部验证在 tempdir 隔离下跑完，源 sug-08 归档状态由主 agent 后续自行重放。

## 关键文件变更

- `src/harness_workflow/workflow_helpers.py`：新增 `_path_slug`（L2182~2196）；`create_requirement` dir_name 清洗（L3278~3284）；`create_bugfix` dir_name 清洗（L3342~3346）；`apply_suggestion` title 截断 + 源文件归档 + frontmatter 翻转（L3101~3121）。
- `tests/test_slug_paths.py`（新增）：5 条 TDD 用例（req slash / req 长 title / req 空回退 / bugfix slash / apply sug-08 风格）。
- `tests/test_cli.py` L1052 / L1059：bugfix 目录断言从 raw title 更新为 kebab-case slug（`bugfix-1-login-form-validation-fails`）。
- `tests/test_suggest_cli.py` L138~155：apply 成功后源文件应被归档，新增 archive 路径断言。
- `tests/test_smoke_req28.py` L305~311：同上，apply 成功后 sug 应移入 archive/。

## 已知限制

- `_path_slug` 默认上限 60 字符，与仓库既有 `bugfix-N-…` 惯例一致；若后续要求更长（例如 80），只需改 helper 默认参数，但需同步 testing 阶段 smoke 预期。
- `apply_suggestion` 归档后 frontmatter 翻转是字符串替换（`"status: pending"` → `"status: applied"`），对无 frontmatter 的历史 sug 仍保持原文不变；不做额外回填，与 req-28 / chg-01 的 frontmatter 契约一致。
