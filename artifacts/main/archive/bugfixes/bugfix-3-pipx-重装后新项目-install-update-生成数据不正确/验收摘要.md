# 验收摘要：bugfix-3 pipx 重装后新项目 install/update 生成数据不正确

## AC 核对结果

| AC 编号（对应 bugfix.md Validation Criteria） | 通过 | 证据 / 备注 |
|---|---|---|
| #1 `pytest tests/test_workflow_helpers_update_idempotent.py -v` → 2 passed | 通过 | 独立复跑：`2 passed in 0.75s`；两条红用例均转绿 |
| #2 全量 pytest 零新增回归（容忍 pre-existing） | 通过 | `146 passed / 50 skipped / 1 failed`；唯一 failure `test_smoke_req29::HumanDocsChecklistTest` 经 `git stash` 回到 HEAD baseline 独立复跑同样 FAIL，确认 pre-existing（根因：req-29 归档 slug 被清洗但该测试硬编码旧中文 slug） |
| #3 烟测：不再出现 `skipped modified .../context/roles/*.md`、不再 `archived legacy .../experience/index.md`、`legacy-cleanup/.../experience/` 下不再新增 `index.md-N` | 通过 | `/tmp/petmall-bugfix3-acceptance-smoke/` 副本连续两次 `harness update`：Run1 32 adopted / 0 skipped modified / 0 archived legacy；Run2 0 adopted / 0 archived legacy / 3 skipped modified（均非 roles 文件，是 executing 衍生问题 D1 范围）；副本初始就有的 `index.md-2` 未递增为 `index.md-3`，循环归档已终止 |
| #4 对人文档 `changes/实施说明.md` 已产出 | 通过（内容）/ 契约偏 | 文件存在且字段齐全；但 validator 期望落 bugfix 根目录 `实施说明.md`。列入下方未达项 |
| #5 session-memory.md 追加 executing 阶段条目 | 通过 | L142-L207 为 executing 阶段条目，context_chain 延长 + 步骤 ✅ + 经验候选 2 条 ≥ 1 |
| #6 `.workflow/state/action-log.md` 顶部追加 executing 阶段条目 | 通过 | 已核查顶部存在对应条目 |

**辅助核查**：
- `harness validate --human-docs --bugfix bugfix-3` → **0/5**（硬门禁违规，见未达项）
- 状态漂移：runtime.yaml `stage=acceptance` vs bugfix-3 state `stage=acceptance` 一致 ✓
- 代码变更范围：仅 `src/harness_workflow/workflow_helpers.py`（+16/-1），符合 bugfix.md Fix Scope ✓
- 本 subagent 未改代码 / 测试 / scaffold / 目标项目 / runtime.yaml stage，未 commit/push

## 是否通过

**conditional pass**——bugfix.md 6 项 Validation Criteria 实质全部通过，代码修复独立烟测有效，但对人文档路径与 `validate_human_docs.py#BUGFIX_LEVEL_DOCS` 契约不一致（3 份对人文档落在子目录，testing 阶段文件名错成"测试简报"），validate 0/5，违反 acceptance.md Step 1 的"落盘硬门禁"。**建议先回 executing/testing 补齐路径再推进 done**。

## 未达项处理建议

| 未达项 | 责任角色 | 建议动作 | 预期完成时间 |
|-------|---------|---------|------------|
| 对人文档路径不符 BUGFIX_LEVEL_DOCS 契约：<br>`regression/回归简报.md` / `changes/实施说明.md` / `testing/测试简报.md` | executing（或 testing 兜底） | `mv regression/回归简报.md ./回归简报.md`；`mv changes/实施说明.md ./实施说明.md`；`mv testing/测试简报.md ./测试结论.md`（同时文件改名）；保留原子目录下 agent 过程产物（`diagnosis.md`、`required-inputs.md`、`test-evidence.md`、`session-memory.md` 等）不动 | 进入 done 之前 |
| 重跑 `harness validate --human-docs --bugfix bugfix-3` | executing / acceptance 复核 | 预期 4/5（`交付总结.md` 待 done 产出） | done 阶段前 |
| 第二次 update 仍有 3 条 `skipped modified`（D1，pre-existing 多生成器共享文件竞争） | 新开 sug 跟踪 | done 阶段主 agent 起 sug，不在本 bugfix 扩大范围 | done 阶段 |
| pre-existing `test_smoke_req29` FAIL（D4） | 独立追踪（可能已有 sug） | 非本 bugfix 范围 | 独立 |

**衍生问题 D2**（`adopt` 判据对用户自建同路径自定义文件的误覆盖风险）当前概率极低，留待反例出现。
