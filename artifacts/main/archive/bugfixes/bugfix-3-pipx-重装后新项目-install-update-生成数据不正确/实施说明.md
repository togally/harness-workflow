# 实施说明：bugfix-3 pipx 重装后新项目 install/update 生成数据不正确

## 实际做了什么

- 步骤 1（根因 B）：`src/harness_workflow/workflow_helpers.py` 的 `LEGACY_CLEANUP_TARGETS`（L71-L89）移除 `Path(".workflow") / "context" / "experience" / "index.md"`，并加注释说明理由（活跃再生成产物不得列入 legacy）。
- 步骤 2（根因 A）：同文件 `_sync_requirement_workflow_managed_files`（L2572-L2600）在"文件不等于模板且未走 updated / overwrote 分支"之后、兜底 `skipped modified` 之前，新增 adopt-as-managed 分支——判据 `relative not in managed_state`（managed-files.json 完全没有该文件 hash 记录）视为漏登记，直接用 scaffold_v2 模板覆盖 + 写 hash；保留"已登记但 hash 不匹配 = 用户真改过"的 `skipped modified` 语义不变。
- 步骤 3：`PYTHONPATH=src python3 -m pytest tests/test_workflow_helpers_update_idempotent.py -v` → **2 passed in 1.01s**（两条红用例 `test_unregistered_stale_scaffold_file_is_adopted_by_update` / `test_experience_index_md_not_cycled_into_legacy_cleanup` 全绿）。
- 步骤 4：`PYTHONPATH=src python3 -m pytest --no-header -q` → **146 passed / 50 skipped / 1 failed**；唯一 failure `tests/test_smoke_req29.py::test_human_docs_checklist_for_req29` 经 `git stash` 对比验证为 pre-existing（HEAD 基线下也 FAIL），零新增回归。
- 步骤 5：临时副本 `/tmp/petmall-bugfix3-smoke` 上跑 `harness update` 烟测——第一次 32 条 `adopted`、0 条 `skipped modified`、无 `archived legacy .workflow/context/experience/index.md`、`legacy-cleanup/experience/` 无新增 `index.md-N`；对比修复前（HEAD）baseline 的 32 条 `skipped modified` + 循环归档，修复效果验证通过。

## 未做与原因

- 未清理目标项目 `/Users/jiazhiwei/claudeProject/PetMallPlatform` 下已堆积的 `legacy-cleanup/.workflow/context/experience/index.md*` 历史副本——bugfix.md#Fix Scope 明确标注不在本轮范围。
- 未修"第二次 update 仍有 3 条 `skipped modified`"（`experience/index.md` / `runtime.yaml` / `.codex/skills/harness/SKILL.md`）——烟测发现的衍生现象，属于"多生成器共享文件语义冲突"，不在根因 A/B 覆盖范围；按 bugfix.md 执行规则"不得扩大修复范围"。已记入 session-memory 衍生问题 + 本说明"已知限制"。
- 未改 `tests/test_workflow_helpers_update_idempotent.py` / scaffold_v2 模板 / 目标项目任何文件 / runtime.yaml stage（executing 角色不推进）。未 git commit / push。

## 关键文件变更

- `src/harness_workflow/workflow_helpers.py` — 2 处改动：常量 `LEGACY_CLEANUP_TARGETS` 移除 `experience/index.md`（+3/-1 含注释）；`_sync_requirement_workflow_managed_files` 新增 adopt-as-managed 分支（+13 行含注释）。
- `artifacts/main/bugfixes/bugfix-3-pipx-重装后新项目-install-update-生成数据不正确/bugfix.md` — 填写五节 Problem / Root Cause / Fix Scope / Fix Plan（5 步）/ Validation Criteria（6 项）。
- `artifacts/main/bugfixes/bugfix-3-.../session-memory.md` — 追加 executing 阶段条目（context_chain 延长 + 步骤 ✅ + 经验候选）。
- `artifacts/main/bugfixes/bugfix-3-.../changes/实施说明.md` — 本对人文档（新建）。
- `.workflow/state/action-log.md` — 顶部追加 executing 阶段条目。
- `.workflow/tools/index/missing-log.yaml` — 追加 executing "python AST 编辑 / dict 读写 / hash 计算"未命中查询（硬门禁一副作用）。

## 已知限制

- 第二次 update 仍会出现 3 条 `skipped modified`（`experience/index.md` / `runtime.yaml` / `.codex/skills/harness/SKILL.md`），根因是 `_sync_...` 写完 hash 后，同一次 update 调用里 `_refresh_experience_index` / `save_requirement_runtime` / `install_local_skills` 又会修改这些文件。属于 pre-existing 多生成器共享文件语义冲突，不在 bugfix-3 范围。建议新开 sug 跟踪。
- `adopt` 判据假设"漏登记 = 可信任覆盖"，对"用户手动创建了一个与 scaffold 同名但从未登记的自定义文件"会误覆盖；目前 scaffold_v2 下暴露的文件都是受管模板，用户自建同路径场景在真实使用中概率极低，暂不新增白名单；若未来出现反例再扩展判据。
