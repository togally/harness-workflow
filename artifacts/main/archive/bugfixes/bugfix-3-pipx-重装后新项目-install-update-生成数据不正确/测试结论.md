# 测试简报：bugfix-3 pipx 重装后新项目 install/update 生成数据不正确

> 阶段：testing（TDD 红阶段，只补用例、不修代码）。次阶段由 executing 完成绿阶段。

## 通过/失败统计

- 新增用例：2（皆为集成/回归用例，直接调 helper 层 `init_repo` / `update_repo`）
- 红阶段执行：`PYTHONPATH=src python3 -m pytest tests/test_workflow_helpers_update_idempotent.py -v`
- 结果：**2 failed / 0 passed / 0 skipped**（符合 TDD 红阶段预期）
- 既有全量回归：本阶段未跑，由 executing 绿阶段一并验证

## 关键失败根因

- **用例 1（对应诊断根因 A）**：`test_unregistered_stale_scaffold_file_is_adopted_by_update` FAIL。
  - 触发点：`_sync_requirement_workflow_managed_files`（`workflow_helpers.py:2541`）在"文件存在但 `managed-files.json` 未登记 hash + 内容与模板不等"时走 `skipped modified` 分支；`_refresh_managed_state`（:2140）又只在"当前文件 == 模板"才写 hash。
  - 证据：`update_repo` 输出含 `- skipped modified .workflow/context/roles/executing.md`；断言 diff 显示文件内容仍停留在 stale 版本。
- **用例 2（对应诊断根因 B）**：`test_experience_index_md_not_cycled_into_legacy_cleanup` FAIL。
  - 触发点：`LEGACY_CLEANUP_TARGETS`（:71）包含 `.workflow/context/experience/index.md`，该文件在 `update_repo` 结尾又由 `_refresh_experience_index`（:3585）重建，形成"搬家 → 重建 → 再搬家"循环；`_unique_backup_destination`（:2606）产生 `-2/-3/...` 递增副本。
  - 证据：第二次 update 输出 `- archived legacy .workflow/context/experience/index.md -> .workflow/context/backup/legacy-cleanup/.workflow/context/experience/index.md-2`。

## 未覆盖场景

- `--force-managed` 分支的端到端行为（现有其他测试与本次根因无关，故未追加）。
- 用户自定义文件（如 `development-standards.md`）的 skip-on-modified 行为；executing 修复根因 A 时若采用"scaffold 全量默认 overwrite"策略，需额外用例保证用户文件不被吞掉——本次未预埋，建议 executing 同步补一条。
- 目标项目历史堆积的 `legacy-cleanup/.workflow/context/experience/index.md*` 一次性清理；诊断文提过可选，不在用例 2 断言范围。
- 真 CLI 子进程路径（`subprocess` 调 `harness update`）——本次走 helper 层直调，覆盖等价。
- 多语言（cn 模板）下的同一根因；scaffold_v2 结构一致，行为应等价，但未写专门的语言变种用例。

## 风险评估

**需改进**（红阶段完成，尚未可上线）。根因 A 会导致所有"在 scaffold 演进前已 install 过的历史项目"`harness update` 后永远拿不到新增章节，属于**用户可感知的数据回归**；根因 B 会持续制造磁盘垃圾和"对人困惑"。两条用例已精准锁住根因路径，executing 绿阶段完成后重跑本文件两条用例 PASS 即可解除阻塞；同时应跑一次全量 `pytest` 确认对 scaffold 落盘逻辑的改动未引起既有 20 个测试文件回归。
