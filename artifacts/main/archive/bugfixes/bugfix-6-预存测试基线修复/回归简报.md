# 回归简报：bugfix-6 预存测试基线修复

## 问题

- `python3 -m unittest discover tests` 出现 3 failures + 3 errors（86 跑 / 44 过 / 36 跳），属 req-26 之前已存在的预存基线，使测试套件失去"红即真问题"的信号质量。

## 根因

- 3 条 fail + 2 条 error 是**测试漂移**：install 模板中英文案、bugfix 制品路径（`.workflow/flow/` → `artifacts/{branch}/`）、`.qoder`/`.claude` skill 默认启用开关均已迭代，断言未同步；1 条 error 是**模块引用漂移**（`harness_workflow.core` 不存在，实际在 `harness_workflow.tools.harness_cycle_detector`）。

## 影响

- 生产行为全部正确：install / bugfix / update / cycle-detector 相关 CLI 实际均可用，线上用户不受影响。
- 仅影响测试层面的绿灯信号；间接影响：每次 req/bugfix 验收需反复声明"已知基线失败"。
- CI 目前以"整体 fail"着地，需在本 bugfix 后回到 `fail=0 / error=0`。

## 路由决策

- **--confirm**：确认是真实问题，进入 bugfix-6 的 planning/executing。
- 范围：全部在 bugfix-6 内可收口（仅改 `tests/test_cli.py`、`tests/test_cycle_detection.py`），无需转 change / 新需求。
- 无需人工输入（`regression/required-inputs.md` 全 no）。
