# 实施说明：bugfix-6 预存测试基线修复

## 实际做了什么

- 改 `tests/test_cli.py` 共 5 条用例：FAIL#1 放宽硬门禁文案断言为中/英双匹配；FAIL#2 将 `harness requirement` 字面替换为当前 SKILL.md 稳定出现的 `harness` / `harness active` 关键词；FAIL#3 把 bugfix 产出路径断言从旧 `.workflow/flow/bugfixes/...` 迁到新 `artifacts/main/bugfixes/...`；ERROR#1、ERROR#2 在 setUp 后追加一次 `install --agent qoder` / `install --agent claude`，显式启用对应平台 skill。
- 改 `tests/test_cycle_detection.py` 1 个文件：原 `from harness_workflow.core import ...` 所依赖的 6 个符号在当前生产代码中完全不存在（生产层只留 `harness_workflow.tools.harness_cycle_detector` CLI），故按 briefing 指令"保守收口"退化为 smoke 测试，仅断言模块可 import、`main` 可调用。
- 回归结果：`python3 -m unittest discover tests` → 86 跑 / 50 pass / 0 fail / 0 error / 36 skipped。

## 未做与原因

- 未恢复 `CallChainNode` / `CycleDetector` / `detect_subagent_cycle` 等丰富 API —— 需要改生产代码，越出 bugfix-6"只改测试文件"硬约束。
- 未触碰 req-26 的 23 条新测试、其他 legacy skip 用例、生产模块；diff 仅限两个测试文件。

## 关键文件变更

- `tests/test_cli.py`：5 条用例断言/准备步骤调整，另有注释解释漂移原因。
- `tests/test_cycle_detection.py`：整文件重写为 smoke 测试（头部文档说明历史与后续恢复路径）。

## 已知限制

- `test_cycle_detection.py` 的覆盖度从 ~15 条用例降为 1 条 smoke；后续若生产补齐 `CallChainNode` 等 API，应在新 change 中恢复覆盖。
- 模板硬门禁文案断言采用中/英双匹配，若未来新增第三语言模板需再扩展。
