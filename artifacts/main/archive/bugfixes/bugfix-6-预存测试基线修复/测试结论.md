# 测试结论：bugfix-6 预存测试基线修复

## 通过/失败统计
- 用例总数 86 / 通过 50 / 失败 0 / 错误 0 / 跳过 36。
- `python3 -m unittest discover tests` 以 `OK (skipped=36)` 收尾，耗时约 21.9s。
- req-26 相关 6 个模块共 28 条（test_req26_independent / test_smoke_req26 / test_rename_helpers / test_next_writeback / test_archive_path / test_regression_helpers）独立跑仍 100% 通过，未受本次修复回归。
- `test_cycle_detection.py` 仅剩 1 条 smoke（`test_module_is_importable`），通过。

## 关键失败根因
- 无。

## 未覆盖场景
- `CallChainNode` / `CycleDetector` / `detect_subagent_cycle` 等 6 个符号的全部业务逻辑（环检测算法、路径输出格式、递归深度边界），因生产层不存在 API，本轮覆盖降为 0，相对原历史用例损失约 14 条。
- 模板硬门禁文案未来若引入第三语言（非中/英），现断言不会报警。
- 三种 agent 平台除 claude / qoder 外（如 codex 独立模式）的 install skill 激活路径未纳入用例。
- bugfix 产出新路径 `artifacts/main/bugfixes/...` 的内容级断言仍是"路径存在"，未覆盖内部文档字段完整性。

## 风险评估
- 可进入 acceptance。fail=0 / error=0 达标，"不改生产代码"硬约束守住（diff 仅 `tests/test_cli.py`、`tests/test_cycle_detection.py`）；cycle-detection 退化为 smoke 是当前硬约束下唯一合规解法，风险低但需在后续 change 中补齐生产 API 并恢复覆盖。
