# 验收摘要：bugfix-6 预存测试基线修复

## AC 核对结果

| AC 编号 | 通过 | 证据 / 备注 |
|--------|------|-------------|
| AC-1（主）：回归后套件基线 `fail=0 / error=0` | [x] | testing 独立复核：`python3 -m unittest discover tests` → 86 跑 / 50 pass / 0 fail / 0 error / 36 skipped；耗时 ~21.9s |
| AC-2：范围硬约束"只改测试文件" | [x] | `git diff --stat HEAD` 源码侧仅动 `tests/test_cli.py`（+22/-11）与 `tests/test_cycle_detection.py`（+30/-380）；`src/harness_workflow/*` 生产层 0 改动 |
| AC-3：未回归 req-26 新测试 | [x] | req-26 相关 6 个测试模块独立跑 28/28 全绿，未被本 bugfix 污染 |
| AC-4：对人文档齐备（回归/实施/测试/验收） | [x] | 前三份已在本 bugfix 根目录落盘，本摘要补齐第四份 |

## 是否通过

**有条件通过**——主 AC（fail=0 / error=0）已达成且范围守约完整，建议主 agent 执行 `harness next` 归档 bugfix-6；但 `test_cycle_detection.py` 覆盖从 ~15 条退化为 1 条 smoke，需作为遗留沉淀，不阻塞本次归档。

## 范围守约核查

- 源码仅动 `tests/test_cli.py` + `tests/test_cycle_detection.py`，满足"bugfix 只改测试文件"硬约束。
- 其余改动：`.workflow/state/runtime.yaml`（工作流状态）、`.harness/feedback.jsonl`（反馈流水）、本 bugfix artifacts 目录下 4 份对人文档 + session-memory，均为流程产物，不计入代码改动。

## 未达项处理建议

- **cycle-detection API 覆盖退化**（非 AC 未达，属已知限制）：生产层 `harness_workflow` 包缺 `CallChainNode / CycleDetector / CycleDetectionResult / detect_subagent_cycle / report_cycle_detection / get_cycle_detector / reset_cycle_detector` 共 6 个符号（现仅有 CLI `harness_cycle_detector.main`），测试被迫退化为 1 条 smoke，相对历史约损失 14 条业务用例。
- **建议动作**：新开 **sug-13**（cycle-detection 生产侧 API 补全），由后续独立 change 或 bugfix 承接，恢复用例覆盖；责任人：后续 change owner，期望 1-2 个迭代内收口。
- **并入 sug-12**：`operation_type` 字段在 `save` 后不持久化的二次问题（手工补的两行会被清掉）建议补充到 sug-12 的"修复建议"与"验证方式"节，责任人：sug-12 owner。
