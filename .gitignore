# === 通用忽略 ===
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
dist/
build/

# === 外部工具与 IDE ===
/.claude/
/.codex/
/.idea/
/.qoder/
/.kimi/

# === 文档 / 产物 / worktrees ===
/docs/
/artifacts/
.worktrees/

# === macOS 元数据（req-28 / chg-01 新增）===
.DS_Store

# === harness workflow 目录（req-57 / 1.0.1 反转）===
# 1.0.1 起 .workflow/ 整体本地化：state/ 是个人运行时；context/{flow,tools,...}/
# 是 framework-level 模板（由 harness install 从 scaffold_v2 重建）；
# 团队级经验在 artifacts/project/experience/（git 管理）。
# 工具自仓 dogfooding：用 /.workflow/ 仅匹配根级（不含 src/.../scaffold_v2/.workflow/ 模板路径）。
/.workflow/
