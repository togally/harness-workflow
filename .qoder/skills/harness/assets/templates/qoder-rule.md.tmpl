# Harness Workflow

本项目通过新的 Harness workflow 管理需求、阶段与验收流转。

## Hard Gate

未读取 `WORKFLOW.md`、`.workflow/context/index.md`、`.workflow/state/runtime.yaml` 前，立即停止，不执行任何动作。

如果这三个文件任一缺失、冲突或无法解析，立即停止，不允许回退旧入口。

## Qoder 路由规则

1. 先读取根目录 `WORKFLOW.md`
2. 再读取 `.workflow/context/index.md`
3. 再读取 `.workflow/state/runtime.yaml`
4. 按 `.workflow/context/index.md` 的路由继续读取当前 stage 对应的角色、经验和约束文件
5. 如果 `conversation_mode: harness` 或存在锁定 requirement/stage，后续回复不得漂移出当前节点
6. 如果 `runtime.yaml` 缺失、`current_requirement` 为空、`stage` 缺失或无法识别，立即停止并要求先修复状态
7. 在进入新节点、切换子模块或旧上下文不再影响下一步时，先做 context maintenance 判断：无关上下文优先 `/clear`，仍有用但可压缩的上下文优先 `/compact`

## 共享入口

- 根目录 `AGENTS.md` 作为跨 Agent 通用入口
- `WORKFLOW.md -> .workflow/context/index.md -> .workflow/state/runtime.yaml` 是唯一主入口
- `.workflow/flow/requirements/<requirement>/` 是工作文档区
- `.workflow/state/` 是运行状态区

## 质量门禁

- `testing` 与 `acceptance` 都不能跳过
- 如果人类对结果不满意，先走 `harness regression "<issue>"`
- 编译失败、启动失败或仍需外部信息时，先进入 regression 再继续
