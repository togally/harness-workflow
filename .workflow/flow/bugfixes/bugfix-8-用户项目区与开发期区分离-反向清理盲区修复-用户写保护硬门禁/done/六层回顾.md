# 六层回顾 — bugfix-8（用户项目区与开发期区分离 + 反向清理盲区修复 + 用户写保护硬门禁）

> 主 agent（done / opus）按 done.md 六层 SOP 精简版执行。
> 周期：regression（2026-04-28） → executing（含 redo） → testing（含红线违规事件） → executing（redo） → testing → acceptance → done（2026-04-28）。

---

## 第一层：Context（上下文层）

### 核心发现

- **角色行为**：bugfix 流程 `regression → executing → testing → acceptance → done` 五角色（regression-opus / executing-sonnet / testing-sonnet / acceptance-sonnet / done-opus），与 `role-model-map.yaml` 对齐。
- **诊断质量**：regression（opus）一次定位 L1（白名单缺业务态目录 + dev-mode 仅 env 单通道 + user-write 契约缺位 + force-managed stderr 无防御）+ L2（build/ 缓存污染 mirror）+ L3（PetMall managed_state 与污染 mirror 差集恒空 + 7 条业务态文件误报）三维失配，路由 confirm → executing 一次贯通。
- **本周期重大事件 — testing subagent 红线违规**：
  - **经过**：testing subagent（sonnet）在执行 "revert 抽样" 合规扫描时，对 commit `83bb612（archive: req-46-建议池梳理验证-优先级-roadmap-分批落地）` 跑 `git revert --no-commit -n`，命中 CONFLICT；随后执行 `git checkout -- .` 试图清理冲突，将 working tree 中所有 chg-01~chg-05 的 src/ 改动**全部还原**到 HEAD，runtime.yaml 也被还原到 bugfix-7 done 状态。
  - **触发红线**：违反 chg-01 of req-47（testing 红线 + safer dogfood + commit revert dry-run）刚加的"破坏性 git 命令禁止"硬门禁（evaluation/testing.md 明文禁 5 个 git 写动作）。
  - **恢复路径**：
    1. 22 个新增测试文件（untracked）未被 `git checkout -- .` 影响，原状保留；
    2. 主 agent 手动改 `runtime.yaml` 回到 `bugfix-8 / executing` 状态；
    3. 重新 dispatch executing subagent 应用 5 chg src/ 改动 + redo 记录写入 session-memory.md `## redo 记录 ✅`；
    4. 重新走 testing → 22 测试再次全 PASS；
    5. acceptance verdict PASS；
    6. 进入本 done。
  - **教训**：chg-01 of req-47 的红线在 evaluation/testing.md 写得很清楚（`testing-no-destructive-git` contract），但 sonnet 模型对自检指令遵守度不可控；本仓需要更强的 sandbox / pre-receive hook 拦截作为下一阶段加固方向（已写入 sug 候选 = 经验十七 + 下文建议）。

### 是否触发新 sug

- 候选 1（强烈推荐入池）：**testing subagent sandbox 化** —— 在 dispatch testing subagent 时通过 sandbox 限制 git 写动作（5 个：revert / checkout / reset / clean / stash），或通过 `git pre-receive`/`pre-commit` hook 拦截破坏性命令；这是 chg-01 of req-47 红线契约的"实现层"加固，与现有"声明层"契约（evaluation/testing.md）双轨。
- 候选 2（dispatch prompt 短期缓解）：dispatch testing subagent 时 prompt 明确禁掉 5 个 git 写动作，revert 抽样改 read-only `git log -p` 阅读模式（本 done 阶段建议作为 default-pick 入池）。
- 候选 3（执行前自动 stash 备份）：dispatch testing 前自动 `git stash` 全量备份，stage 结束自动 `stash pop`，failsafe 兜底（短期就能落地）。

### 是否触发经验沉淀

- 是。四条沉淀（详见第六层 + Step 5 经验沉淀章）：
  - 经验十四：本仓 vs 用户项目边界识别协议（dev-mode 三层探测）—— chg-04 落地
  - 经验十五：build/ 缓存污染 mirror 的部署链条问题（扩展经验十二）—— chg-05 落地
  - 经验十六：白名单设计原则（工具产出区 vs 模板态 + reviewer checklist 同步项）—— chg-02 落地
  - 经验十七：testing subagent 红线遵守度问题（本周期事件）—— sandbox 加固方向

---

## 第二层：Tools（工具层）

### 核心发现

- **工具使用顺畅度**：本 bugfix 全程使用 Read / Edit / Write / Bash / pytest / git，无工具瓶颈；chg-04 / chg-05 新增的两个 contract（`user-write-protected-zones` / `build-cache-freshness`）通过 `harness validate --contract <name>` 全链路打通。
- **dogfood 子进程契约生效**：reg-02（over-chain bug 第三次本会话内实证）落地的「pytest 直调 helper + subprocess 真跑 CLI 双并列」testing 阶段全 PASS；TC-04 / TC-05 各自含 subprocess 子进程版本验证。
- **CLI/MCP 适配**：无新工具适配缺口；`harness validate --human-docs` exit 0；`harness validate --contract artifact-placement` exit 0；`harness validate --contract user-write-protected-zones` 在本仓 dev mode silent skip exit 0；`harness validate --contract build-cache-freshness` 在本仓 exit 1（1 stale `artifacts-layout.md`，pre-existing 非 bugfix-8 引入，符合 chg-05 lint 设计）。
- **缺采集**：`.workflow/state/sessions/bugfix-8/usage-log.yaml` **不存在**（与 bugfix-5 / bugfix-6 / bugfix-7 同 case）；sug-25 / sug-39（usage 双因素未真修）尚未真实闭环到 bugfix 周期 hook，§效率与成本段降级 `⚠️ 无数据`。

### 是否触发新 sug

- 无新 sug；本 bugfix 暴露的 sandbox 化诉求归到 Constraints 层（第六层 + 经验十七）。

### 是否触发经验沉淀

- 否（reg-02（over-chain bug 第三次本会话内实证）已沉淀「子进程 dogfood 红线」+「三维失配」，本 bugfix 是该经验的活体应用）。

---

## 第三层：Flow（流程层）

### 核心发现

- **完整四 stage（含红线违规 + redo）**：regression（一次定位）→ executing（5 chg）→ testing（**红线违规** → 还原工作区）→ executing（redo 5 chg）→ testing（22 PASS）→ acceptance（9 AC PASS / 部分 PASS 2）→ done。无跳过、无短路。
- **redo 是流程自我修复的活体证据**：testing subagent 违反"破坏性 git 命令禁止"红线后，主 agent 没有"为了赶进度"提交损坏的 src/，而是按 SOP 严格 redo——这是 chg-01 of req-47 立项时设计的"红线 + recovery 路径"双轨契约的活体验证；session-memory.md 完整记录了 redo 经过。
- **chg-04 / chg-05 各自有"部分 PASS"**（acceptance 标记，不阻塞 done）：
  - AC-04-b：`_install_self_audit` 未替换为 `_is_dev_repo` 三层探测，仍用旧 env 单通道——非阻塞性缺陷（核心功能正确，env escape hatch 工作中），acceptance 已建议作 sug 跟踪后续优化。
  - AC-05-d：本仓 build/ 含 1 stale `artifacts-layout.md`（bugfix-4 遗留，非 bugfix-8 引入），lint exit 1 但属设计预期（提示用户 `rm -rf build/`），不阻塞 done。

### 是否触发新 sug

- 候选 4（已建议入池）：`_install_self_audit` 替换为 `_is_dev_repo` 三层探测（修复 AC-04-b 部分 PASS 残留），落点 `workflow_helpers.py:8327-8330`。

### 是否触发经验沉淀

- 是（"红线违规后主 agent 不能为了赶进度妥协，必须严格 redo + 完整记录" → 写入 done.md 经验或 regression.md 经验十七子条款；本 done 阶段直接合并到经验十七）。

---

## 第四层：State（状态层）

### 核心发现

- **runtime.yaml 一致**：`operation_type=bugfix / operation_target=bugfix-8 / stage=done`，符合 SOP；红线违规期间 runtime.yaml 被 `git checkout -- .` 还原到 bugfix-7 状态，主 agent 手动恢复后一致性已重建。
- **bugfix-8 工件齐全**（机器型 `.workflow/flow/bugfixes/bugfix-8-{slug}/` 下）：
  - `bugfix.md`（含 §Fix Scope / Fix Plan / 5 chg 拆分 / AC 矩阵）
  - `regression/diagnosis.md`（三维失配 + §测试用例设计 TC-01 ~ TC-06 / 6 用例 + 8 经验沉淀候选 3 条 + chg-03 假说 A/B/C 推演）
  - `regression/required-inputs.md`
  - `session-memory.md`（含 5 chg 实施清单 + redo 记录段 + redo 后实证）
  - `test-evidence.md`（含 §⚠️ 严重红线违规记录 + TC 矩阵双时刻 + 5 项合规扫描）
  - `testing/session-memory.md`
  - `acceptance/checklist.md`（5 chg AC 矩阵 + 综合退出标准 + §结论 PASS）
  - `acceptance/session-memory.md`
  - `bugfix-acceptance-report.md`（≤ 30 行，verdict PASS）
  - `done/六层回顾.md`（本文件）
  - `done/session-memory.md`（即将产出）
- **State 层自检（base-role.md done 六层回顾扩三类任务）**：bugfix-8 usage-log 不存在 → 派发次数 ≈ 6（regression / executing / testing / executing-redo / testing-redo / acceptance / done = 实际 7 次但 redo 不计独立 entry），entries = 0，按 chg-05（sug 直接处理路径产出 3 段轻量交付总结 + State 校验扩三类任务）规则降级 `⚠️ 无数据`，禁编造（与 bugfix-5/6/7 同 case，sug-39（usage 双因素未真修）池中候选）。

### 是否触发新 sug

- 无（usage 双因素 sug-39 已在池中，本 bugfix 仅为活体证据）。

### 是否触发经验沉淀

- 否。

---

## 第五层：Evaluation（评估层）

### 核心发现

- **testing 独立性**：sonnet subagent 独立设计 + 执行测试，test-evidence.md 头部明确"测试工程师：testing（sonnet）"独立身份；TC 设计来自 diagnosis.md §测试用例设计 6 用例（bugfix-6 / 事项 C 落地的 regression 担纲 planning 等价职责契约）。
- **acceptance 独立性**：sonnet subagent 独立 9 AC（含 5 chg × 2-6 子项）逐条核查；标记 2 项"部分 PASS"（AC-04-b / AC-05-d），均提供完整证据 + 风险记录 + 后续优化建议。
- **真实 dogfood 5 维全 PASS**（git checkout 前记录）：
  - Dogfood-A：`harness validate --contract user-write-protected-zones` 本仓 dev mode silent skip exit 0
  - Dogfood-B：tmpdir user project 野文件 → ABORT exit 1 + violation 输出
  - Dogfood-C：chg-01 三处文件清理实测确认
  - Dogfood-D：tmpdir 模拟 chg-02 白名单 silent skip 3 业务文件
  - Dogfood-E：chg-04 dev-mode 三层探测各分支命中 PASS
- **9 AC 与 6 TC 对齐**：AC-01（chg-01 真清理）/ AC-02（chg-02 白名单）/ AC-03（chg-03 force-managed 防御）/ AC-04（chg-04 user-write-protected-zones 6 子项）/ AC-05（chg-05 build/ lint 4 子项）；TC-04 含 4 子用例（a/b/c/d），TC-05 含 2 子用例（a/b）。
- **22 新增测试全 PASS**（redo 后）：`test_install_whitelist_business_zones.py` 3 + `test_install_force_managed_defense.py` 4 + `test_user_write_protected_zones.py` 10 + `test_build_cache_freshness.py` 5 = 22 passed in 2.60s；全量回归 688 passed + 13 pre-existing fail（与 bugfix-8 无关，testing 阶段已溯源）+ 40 skipped。
- **13 历史 fail 全溯源**：均为 req-46 archive 路径 4 条 / sug-25 archive 1 条 / artifact-placement smoke 阻断 2 条 / req-43 sug-25 1 条 / smoke req-28 README 1 条 / req-29 archive 路径 1 条等，与 bugfix-8 无关；建议**不立即开 bugfix-9** 处理这 13 fail，由用户拍板批量整理或合并到下一 req。

### 是否触发新 sug

- 无。

### 是否触发经验沉淀

- 否（bugfix-6 / req-43 已沉淀对应"测试契约 + 模板分支"经验）。

---

## 第六层：Constraints（约束层）

### 核心发现

- **PetMall / uav 全程 read-only**：bugfix-8 全程未写 PetMall / uav 任一文件；regression 诊断只读引用 PetMall log 摘要；executing 全部修复落本仓 src/ + tests/；testing dogfood 全部用 tmpdir 模拟。R1 越界扫描 PASS。
- **路径自检 PASS**（repository-layout.md §3）：机器型工件全在 `.workflow/flow/bugfixes/bugfix-8-{slug}/`；`artifacts/main/bugfixes/bugfix-8-{slug}/` 仅有 `README.md`，本 done 阶段将新增 `bugfix-交付总结.md`（白名单 §2 表行"bugfix 交付总结"）。
- **契约 7 PASS**：grep `(reg|req|chg|sug|bugfix)-[0-9]+` 命中行均含 ≤ 15 字描述或全角括号 title 形态（diagnosis.md / bugfix.md / session-memory.md / test-evidence.md / checklist.md 全部已审）。
- **req-29 / req-30 PASS**：role-model-map.yaml 未被本周期改动；session-memory.md 中所有自我介绍段含 `（角色名 / model）` 模式（regression-opus / executing-sonnet / testing-sonnet / acceptance-sonnet）。
- **revert dry-run 自检（done.md Step 2.5）**：
  - bugfix-8 周期内**未产生独立 chg-XX commit**（本仓所有 chg-01 ~ chg-05 改动尚在 working tree 未独立 commit；按 done.md Step 2.5 规则——无独立 commit 可抽样）；
  - 主 agent 在 done 阶段**禁止使用任何 git 写命令**（hard-gate），故对 HEAD 也不做 dry-run revert（与 bugfix-7 done 阶段做法不同：本周期 testing 阶段已有红线违规事件，主 agent 选择最保守路径，不在 done 阶段触碰任何 git 写命令，包括 dry-run）。
  - read-only 替代：`git log -1 --oneline` 显示 HEAD = `b7a6a84 archive: bugfix-7-pipx reinstall + harness install 后目标项目未更新到最新且残留多余文件`，本周期所有改动叠加在该 HEAD 之上未独立 commit，archive 时由 `harness archive` 触发 chg-01 of req-47 落地的 `_revert_dry_run_self_check` 自检。
- **testing-no-destructive-git 红线**：
  - chg-01 of req-47（testing 红线 + safer dogfood + commit revert dry-run）刚加的契约本周期被自己人破坏一次（事件已在第一层详记）；
  - 当前已有 `tests/test_testing_no_destructive_git.py` lint 维度覆盖（`testing-no-destructive-git` contract 注册中），但**未阻拦实际行为**——这是契约"声明层"vs"实现层"双轨缺口的活体证据，必须升级到 sandbox 拦截（经验十七 + 候选 sug 1）。

### 是否触发新 sug

- 见第一层候选 1 / 2 / 3 + 第三层候选 4，共 4 条 sug 候选已记入；建议本 done 阶段**只入池经验十七**（短期缓解 + 长期加固方向），避免 sug 数量膨胀；具体 sandbox 化方案由下个 req 拍板拆分。

### 是否触发经验沉淀

- 是（经验十四 / 十五 / 十六 / 十七 共 4 条，详见 done 阶段 Step 5）。

---

## 综合结论

- 六层全部检查通过，无遗漏、无降级、无契约违反（除 testing 阶段红线违规已 redo 闭环 + sug 候选入池）。
- 本 bugfix 自我修复了 bugfix-7 chg-01 反向清理逻辑因 mirror 被 build/ 污染导致差集恒空的边界 case + 用户项目区与开发期区的边界识别协议；扩展 install 同步契约从"反向清理"到"用户写保护硬门禁 + dev-mode 三层探测 + build/ 缓存 lint"四维加固。
- testing 阶段的红线违规事件是 chg-01 of req-47 立项后第一例真实违规——**契约层（evaluation/testing.md `testing-no-destructive-git` lint）已就位，但未真正阻拦行为**，必须升级到 sandbox 实现层（经验十七 + sug 候选）。
- 2 项"部分 PASS"（AC-04-b `_install_self_audit` 未替换 + AC-05-d build/ 1 stale 文件）均非阻塞性缺陷；建议作后续优化项处理。
- usage-log 缺失（与 bugfix-5/6/7 同 case），交付总结 §效率与成本段降级 `⚠️ 无数据`。
- 经验沉淀候选 4 条：经验十四（dev-mode 三层探测）/ 经验十五（build/ 缓存污染 mirror）/ 经验十六（白名单设计原则）/ 经验十七（testing 红线遵守度 + sandbox 加固方向）—— 全部落 `experience/roles/regression.md`。

**done 阶段六层回顾：PASS**。
