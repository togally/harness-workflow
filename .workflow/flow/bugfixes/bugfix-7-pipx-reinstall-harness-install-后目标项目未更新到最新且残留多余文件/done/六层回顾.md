# 六层回顾 — bugfix-7（pipx reinstall + harness install 后目标项目未更新到最新且残留多余文件）

> 主 agent（done / opus）按 done.md 六层 SOP 精简版执行。
> 周期：regression（2026-04-28） → executing → testing → acceptance → done（2026-04-28）。

---

## 第一层：Context（上下文层）

### 核心发现

- **角色行为**：bugfix 流程 `regression → executing → testing → acceptance → done` 全程用 5 角色（regression-opus / executing-sonnet / testing-sonnet / acceptance-sonnet / done-opus），与 `role-model-map.yaml` 对齐。
- **诊断质量**：regression（opus）一次定位 L1+L2+L3 三维失配（源码反向清理缺位 + pipx 远程 commit 滞后 + 用户项目混合状态），未误判，路由 confirm → executing 一次贯通。
- **executing 顺手做 chg-07**：本应是 reg-02（部署同步契约 + 子进程 dogfood）落地的 `_is_stage_work_done` executing gate 严格化，遗漏了 bugfix 模式（无 `changes/` 目录、用 `session-memory.md` ✅ 标注）；executing subagent 自检发现后**就地新增 chg-07** 修复 + TC-09/10，避免阻塞自身推进——是同根因复发的早识别样本。
- **testing 顺手做 testing 分支**：testing subagent 推进 `harness next` 时再次发现 `_is_stage_work_done(testing)` 仅认 `test-report.md`，bugfix 用的是 `test-evidence.md`；就地按 chg-07 同模式给 testing 分路扩展（session-memory.md 第 153-160 行有完整代码段记录）。

### 是否触发新 sug

- 建议候选（已记入 diagnosis.md §9）：sug 候选 1：`harness install --check` 默认输出 venv vs HEAD diff hint（chg-01 部分覆盖）；sug 候选 2：`pyproject.toml::version` 自动 bump 机制（chg-02 解决了一次，但仍是手工）；sug 候选 3：scaffold_v2 文件删除时 reviewer lint 强制写 `LEGACY_CLEANUP_TARGETS`（已被 chg-01 自动派生方案部分覆盖，可降级）。
- 经验沉淀触发：见第六层。

### 是否触发经验沉淀

- 是。三条沉淀候选（详见第六层 + Step 5 经验沉淀章）。

---

## 第二层：Tools（工具层）

### 核心发现

- **工具使用顺畅**：本 bugfix 全程使用 Read / Edit / Write / Bash / pytest，无工具瓶颈。
- **dogfood 子进程契约生效**：reg-02 落地的「pytest 直调 helper + subprocess 真跑 CLI 双并列」testing 阶段全 5 维 PASS，验证了 chg-02（over-chain bug 真修 + deploy 契约 + 子进程 dogfood）落地的红线契约。
- **CLI/MCP 适配**：无新工具适配缺口；`harness validate --human-docs` 在 bugfix 周期 exit 1（缺 done 阶段 `bugfix-交付总结.md`）属预期，按 D-11=B 留痕放行。
- **缺采集**：`.workflow/state/sessions/bugfix-7/usage-log.yaml` **不存在**（与 bugfix-5/6 同 case）；sug-25（record_subagent_usage 派发链路真实接通）/ sug-39（usage 双因素未真修）尚未真实闭环到 bugfix 周期 hook，§效率与成本段降级 `⚠️ 无数据`。

### 是否触发新 sug

- 无新 sug；`harness install --check` 默认输出 venv vs HEAD（已被 chg-01 sug-55 部分覆盖，本 bugfix 用 CLI 子进程 fallback 强化）。

### 是否触发经验沉淀

- 否（reg-02 已沉淀「三维失配」+「子进程 dogfood 红线」，本 bugfix 是该经验的活体应用而非新增）。

---

## 第三层：Flow（流程层）

### 核心发现

- **完整四 stage**：regression（一次定位）→ executing（5 chg + 紧急 chg-07）→ testing（9 PASS / 1 N/A + gate 修）→ acceptance（9 AC PASS + 部署同步检查通过）→ done。无跳过、无短路。
- **executing 中途插 chg-07 + testing 中途插 testing gate**：均属"同 reg-02 根因复发的早识别"——reg-02 严格化 `_is_stage_work_done` 时仅考虑 requirement 模式（`changes/` + `test-report.md`），遗漏 bugfix 模式（无 `changes/`，用 `session-memory.md` ✅ + `test-evidence.md`）；本 bugfix 通过自身 dogfood 暴露这一漏洞并就地修复，是流程自我修复的样本。
- **chg-06 contingency 不触发**：testing 5 维 dogfood 全 PASS，无"模糊状态"边界 case，按 default-pick 转 sug 候选不阻塞。

### 是否触发新 sug

- 无。

### 是否触发经验沉淀

- 是（"模式差异化"：凡是新增 gate / lint / 契约都需考虑 BUGFIX_SEQUENCE / SUGGESTION_SEQUENCE 三模式覆盖，是 reg-02 严格化的盲区扩展，详见 Step 5）。

---

## 第四层：State（状态层）

### 核心发现

- **runtime.yaml 一致**：`operation_type=bugfix / operation_target=bugfix-7 / stage=done`，符合 SOP。
- **bugfix-7 工件齐全**（机器型 `.workflow/flow/bugfixes/bugfix-7-{slug}/` 下）：
  - `bugfix.md`（含 §Fix Scope / Fix Plan / AC 矩阵）
  - `regression/diagnosis.md`（含三维失配 + §测试用例设计 7 TC）
  - `session-memory.md`（含 chg-07 ✅ + testing 阶段附加段）
  - `test-evidence.md`（9 TC PASS / 1 N/A + 13 历史 fail 溯源 + 5 项合规扫描）
  - `acceptance/checklist.md`（9 AC ✅ + §结论 PASS）
  - `acceptance/session-memory.md`
  - `bugfix-acceptance-report.md`（≤ 30 行）
- **State 层自检（base-role.md done 六层回顾扩三类任务）**：bugfix-7 usage-log 不存在 → 派发次数 = 5（regression / executing / testing / acceptance / done），entries = 0，按 chg-05（sug 直接处理路径产出 3 段轻量交付总结 + State 校验扩三类任务）规则降级 `⚠️ 无数据`，禁编造（与 bugfix-5/6 同 case）。

### 是否触发新 sug

- 无（usage 双因素 sug-39 已在池中，本 bugfix 仅为活体证据）。

### 是否触发经验沉淀

- 否。

---

## 第五层：Evaluation（评估层）

### 核心发现

- **testing 独立性**：sonnet subagent 独立设计 + 执行测试，test-evidence.md 头部明确"测试工程师：testing（sonnet）"独立身份；TC 设计来自 diagnosis.md §测试用例设计（bugfix-6 / 事项 C 落地的 regression 担纲 planning 等价职责契约），符合 bugfix 流程契约。
- **acceptance 独立性**：sonnet subagent 独立 9 AC 逐条核查，all PASS；含部署同步检查（_is_stage_work_done import 成功 + venv mtime ≥ HEAD commit ts，符合 reg-02 落地的部署契约）。
- **真实 dogfood 5 维全 PASS**：tmpdir 完整模拟 PetMall 状态——反向清理 / managed sync / check stdout / tool_version mismatch / active_list 跳 prompt，无遗漏。
- **9 AC 与 7 TC 对齐**：AC-01/02/03/05（chg-01）/ AC-06（chg-02）/ AC-04（chg-04）/ AC-08（chg-05）/ AC-09（chg-07）；chg-06 contingency 不触发，记入 sug 候选。
- **13 历史 fail 全溯源**：均为 req-46 / sug-25 / smoke 测试预存，与 bugfix-7 无关。

### 是否触发新 sug

- 无。

### 是否触发经验沉淀

- 否（bugfix-6 / req-43 已沉淀对应"测试契约 + 模板分支"经验）。

---

## 第六层：Constraints（约束层）

### 核心发现

- **PetMall / uav 全程 read-only**：bugfix-7 全程未写 PetMall / uav 任一文件，git diff 仅命中本仓库 src/ + tests/ + .workflow/ + README.md + pyproject.toml。R1 越界扫描 PASS。
- **路径自检 PASS**（repository-layout.md §3）：机器型工件全在 `.workflow/flow/bugfixes/bugfix-7-{slug}/`；`artifacts/main/bugfixes/bugfix-7-{slug}/` 仅有 `README.md`，本 done 阶段将新增 `bugfix-交付总结.md`（白名单）。
- **契约 7 PASS**：grep `(reg|req|chg|sug|bugfix)-[0-9]+` 命中行均含 ≤15 字描述或全角括号 title 形态。
- **req-29 / req-30 PASS**：role-model-map.yaml 未被本周期改动；action-log.md 自我介绍段含 `（角色名 / model）` 模式。
- **revert dry-run 自检**：本周期 bugfix-7 自身的 src 改动尚在 working tree 未独立 commit，按 done.md Step 2.5 规则——无独立 commit 可抽样 → 改对 HEAD（`83bb612 archive: req-46`）跑 `git revert --no-commit -n HEAD` dry-run，**exit 0 无 conflict**，PASS；与 testing 阶段对 `5e5bb18 acceptance: req-46` 抽样的 pre-existing rename 冲突独立（属 req-46 archive rename/delete 模式 → 本 bugfix 无关）。

### 是否触发新 sug

- 无。

### 是否触发经验沉淀

- 是（"pipx 远程安装源 vs 本地 HEAD 部署链条断裂"诊断模板 + "install 反向清理盲区"诊断模板，二者扩展 regression.md 经验十"三维失配诊断模板"，详见 Step 5）。

---

## 综合结论

- 六层全部检查通过，无遗漏、无降级、无契约违反。
- 本 bugfix 自我修复了 reg-02 严格化 gate 的 bugfix 模式盲区（chg-07 + testing 顺手扩展），是流程自检的活体证据。
- chg-06 LLM 兜底 contingency 转 sug 候选，不阻塞 done。
- usage-log 缺失（与 bugfix-5/6 同 case），交付总结 §效率与成本段降级 `⚠️ 无数据`。
- 经验沉淀候选 3 条（详见 done 阶段 Step 5 章节，落到 `experience/roles/regression.md` 经验十一 + `experience/stage/executing.md` 模式差异化或同上经验十扩展子条款）。

**done 阶段六层回顾：PASS**。
