# Plan: chg-05

## 执行摘要

对 `base-role.md` 第 9 行进行单点文本替换，将"启动 toolsManager subagent 查询可用工具"的自查语义改为"委派 toolsManager subagent，由其匹配并推荐工具"的委派语义。

## 目标文件

`.workflow/context/roles/base-role.md`

## 具体修改

### 修改位置

**章节**：`## 硬门禁一：工具优先`（第 7-11 行）

**当前原文（第 9 行）**：

```
在执行任何实质性操作前，必须先启动 `toolsManager` subagent 查询可用工具。详细查询流程、匹配规则和返回值格式见 `.workflow/context/roles/tools-manager.md`。
```

**替换为**：

```
在执行任何实质性操作前，必须先**委派** `toolsManager` subagent，由其匹配并推荐适合当前任务的工具；收到推荐后，优先使用匹配的工具执行操作。详细委派流程、匹配规则和返回值格式见 `.workflow/context/roles/tools-manager.md`。
```

### 修改说明

| 原表述 | 新表述 | 语义变化 |
|--------|--------|---------|
| "启动 toolsManager 查询可用工具" | "委派 toolsManager，由其匹配并推荐适合当前任务的工具" | 从主动查询 → 委派推荐 |
| "详细查询流程" | "详细委派流程" | 保持引用，修正动词 |
| （无） | "收到推荐后，优先使用匹配的工具执行操作" | 明确角色的使用职责 |

第 11 行的核心原则（"有匹配工具时优先使用工具，无匹配时才允许由模型自行判断"）保持不变，无需修改。

## 执行步骤

1. 读取 `.workflow/context/roles/base-role.md` 确认第 9 行原文
2. 使用 Edit 工具执行精确替换
3. 重新读取文件，验证替换结果正确，未影响其他内容

## 验证方法

替换后检查：
- 硬门禁一章节首段中不含"查询可用工具"字样
- 新表述中包含"委派"和"匹配并推荐"关键词
- 第 11 行核心原则文本未改变
- 文件其余内容（硬门禁二、硬门禁三、经验沉淀规则、上下文维护规则）均未被影响

## 预计影响范围

仅修改 `base-role.md` 一处文本，不新增/删除行，不影响文件结构。chg-06 依赖本变更完成后的正确表述。
