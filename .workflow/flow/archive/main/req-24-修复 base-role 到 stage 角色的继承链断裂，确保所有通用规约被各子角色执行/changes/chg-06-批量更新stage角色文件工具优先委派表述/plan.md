# Plan: chg-06

## 执行摘要

分 2 步批量更新 9 个文件的工具优先表述为委派语义。所有替换均为精确文本替换，不新增行、不改变文件结构。参考基准为 chg-05 修正后的 `base-role.md` 表述。

**前置条件**：chg-05 必须已执行完成。

## 委派语义参考基准

来自 chg-05 修正后的 `base-role.md`：

> 在执行任何实质性操作前，必须先**委派** `toolsManager` subagent，由其匹配并推荐适合当前任务的工具；收到推荐后，优先使用匹配的工具执行操作。

---

## Step 1：更新 `stage-role.md`（继承清单 + SOP 模板）

**文件**：`.workflow/context/roles/stage-role.md`

### 修改点 1：继承清单（第 24 行）

**当前原文**：
```
| 1 | **硬门禁一：工具优先** | 在执行业务步骤前，必须先启动 toolsManager 查询可用工具；有匹配工具时优先使用 | SOP 的"执行"部分 |
```

**替换为**：
```
| 1 | **硬门禁一：工具优先** | 在执行业务步骤前，必须先**委派** toolsManager subagent，由其匹配并推荐适合当前任务的工具；收到推荐后，优先使用匹配工具执行操作 | SOP 的"执行"部分 |
```

### 修改点 2：SOP 模板（第 46 行）

**当前原文**：
```
- 每一步实质性操作前，执行 toolsManager 查询（工具优先）
```

**替换为**：
```
- 每一步实质性操作前，委派 toolsManager subagent 匹配并推荐工具（工具优先），收到推荐后优先使用匹配工具
```

---

## Step 2：批量更新 8 个子角色文件

### 2-1：`executing.md`（第 18 行）

**章节**：`### Step 2: 工具优先查询`

**当前原文**：
```
- 对 plan.md 中的每一步，执行实质性操作前先启动 toolsManager 查询可用工具
```

**替换为**：
```
- 对 plan.md 中的每一步，执行实质性操作前先**委派** toolsManager subagent，由其匹配并推荐适合当前步骤的工具；收到推荐后，优先使用匹配工具
```

---

### 2-2：`testing.md`（第 21 行）

**当前原文**：
```
- 设计过程中如需查找代码或运行命令，先启动 toolsManager 查询可用工具
```

**替换为**：
```
- 设计过程中如需查找代码或运行命令，先**委派** toolsManager subagent，由其匹配并推荐适合当前操作的工具；收到推荐后，优先使用匹配工具
```

---

### 2-3：`planning.md`（第 20 行）

**当前原文**：
```
- 拆分过程中如需查询工具或读取文件，先启动 toolsManager 查询可用工具
```

**替换为**：
```
- 拆分过程中如需查询工具或读取文件，先**委派** toolsManager subagent，由其匹配并推荐适合当前操作的工具；收到推荐后，优先使用匹配工具
```

---

### 2-4：`acceptance.md`（第 20 行）

**当前原文**：
```
- 核查过程中如需查找代码或文件，先启动 toolsManager 查询可用工具
```

**替换为**：
```
- 核查过程中如需查找代码或文件，先**委派** toolsManager subagent，由其匹配并推荐适合当前操作的工具；收到推荐后，优先使用匹配工具
```

---

### 2-5：`regression.md`（第 16 行）

**当前原文**：
```
- 分析过程中如需查询代码或运行只读命令，先启动 toolsManager 查询可用工具
```

**替换为**：
```
- 分析过程中如需查询代码或运行只读命令，先**委派** toolsManager subagent，由其匹配并推荐适合当前操作的工具；收到推荐后，优先使用匹配工具
```

---

### 2-6：`requirement-review.md`（第 22 行）

**当前原文**：
```
- 讨论过程中如需查询文件或工具，先启动 toolsManager 查询可用工具
```

**替换为**：
```
- 讨论过程中如需查询文件或工具，先**委派** toolsManager subagent，由其匹配并推荐适合当前操作的工具；收到推荐后，优先使用匹配工具
```

---

### 2-7：`done.md`（第 26 行）

**章节**：`### Step 2: 六层回顾检查`

**当前原文**：
```
- 回顾过程中如需读取文件或生成报告，优先使用合适工具
```

**替换为**：
```
- 回顾过程中如需读取文件或生成报告，先**委派** toolsManager subagent，由其匹配并推荐适合当前操作的工具；收到推荐后，优先使用匹配工具
```

---

### 2-8：`technical-director.md`（第 135 行）

**章节**：`### Step 3: 六层回顾`（或对应回顾步骤）

**当前原文**：
```
- 回顾过程中如需读取文件或生成报告，先启动 toolsManager 查询可用工具，优先使用合适工具
```

**替换为**：
```
- 回顾过程中如需读取文件或生成报告，先**委派** toolsManager subagent，由其匹配并推荐适合当前操作的工具；收到推荐后，优先使用匹配工具
```

---

## 执行顺序

1. 先完成 Step 1（stage-role.md，共 2 处）
2. 再按顺序完成 Step 2（8 个子角色文件，每个文件 1 处）
3. 每个文件修改完成后立即验证：确认目标行已替换，相邻行未受影响

## 验证方法

所有修改完成后，执行全局搜索验证：
- 在 `.workflow/context/roles/` 目录中搜索"启动 toolsManager 查询可用工具"，期望结果为 0 个匹配（含子目录）
- 搜索"委派.*toolsManager"，期望在 9 个文件中找到对应匹配

## 预计影响范围

- 共修改 9 个文件，10 处文本
- 不新增/删除行，不改变文件结构
- 不影响任何业务逻辑、流转规则或其他硬门禁
