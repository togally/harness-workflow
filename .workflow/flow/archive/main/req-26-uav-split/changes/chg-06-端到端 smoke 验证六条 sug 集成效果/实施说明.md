# 实施说明：chg-06 端到端 smoke 验证六条 sug 集成效果

## 实际做了什么

- 步骤 1-2（新增 smoke 文件）：新建 `tests/test_smoke_req26.py`，4 个用例（1 个端到端 + 3 个静态契约）。
- 步骤 3（端到端串联）：`test_full_lifecycle_smoke` 在 tempdir 里按 `create_requirement → rename_requirement → workflow_next×4 → create_change → rename_change → workflow_next×2 → create_regression → regression_action(--confirm / --testing) → workflow_next×2 → archive_requirement` 走完完整生命周期，每步断言 AC-01/02/03/04/05。
- 步骤 4（AC-06 降级为静态契约）：`DualTrackContractStaticTest` 三个用例分别核查 stage-role.md 的"对人文档输出契约"章节、7 个 stage 角色各自的"对人文档输出"条目、scaffold_v2 镜像与 roles/ 文本一致。
- 步骤 5（回归基线）：`python3 -m unittest discover tests` 跑完 81 测试，4 个 smoke 新增用例全过，3 failures / 3 errors / 36 skipped 与 chg-05 合入后基线一致。

## 未做与原因

- 未跑真 `harness` CLI（briefing 硬约束："不跑真 harness 命令，一切在函数级"）。
- 未产出 `smoke-report.md`（plan.md Step 3）：本"实施说明.md"已承担等价职责；同时避免重复落盘、保持 chg-06 产物目录精简。
- 未在 smoke 里模拟各 stage agent 实际写对人文档（AC-06），该部分属 agent 执行层而非 helper 函数行为，降级为静态契约核查（文本 + 镜像一致性）。

## 关键文件变更

- 新增：`tests/test_smoke_req26.py`——端到端 smoke + AC-06 静态契约核查。
- 新增：`artifacts/main/requirements/req-26-uav-split/changes/chg-06-端到端 smoke 验证六条 sug 集成效果/实施说明.md`（本文件）。
- 更新：`artifacts/main/requirements/req-26-uav-split/changes/chg-06-端到端 smoke 验证六条 sug 集成效果/session-memory.md`——追加 `## 执行记录` 节。

## 已知限制

- AC-06 是静态核查，不直接证明 agent 在真实会话中会按契约写对人文档；仍依赖各角色 SOP 的硬门禁被 agent 遵守。
- smoke 不覆盖 bugfix 与 suggestion 子流程（本 req-26 聚焦 requirement 主干），如需完整覆盖应在后续独立增强。
