# 实施说明：chg-05 各 stage 角色双轨输出对人文档机制（SOP 与模板）

## 实际做了什么

- 对应 plan.md Step 1.2：在 `.workflow/context/roles/stage-role.md` 的"流转规则"之前新增"对人文档输出契约（req-26 / sug-06）"章节，覆盖契约 1~5（双轨不迁移 / 路径同构 / 中文命名 + 阶段粒度 / 硬门禁 / 反例核对）。
- 对应 plan.md Step 1.3 + 1.4：为 `requirement-review.md` / `planning.md` / `executing.md` / `testing.md` / `acceptance.md` / `regression.md` / `done.md` 7 个角色文件各追加一节"对人文档输出（req-26）"，嵌入 1.4 节定稿的最小字段模板，并在原"退出条件"末尾追加一条新退出条目（对人文档 `{文件名}.md` 已产出且字段完整）。
- 对应 plan.md Step 1.5：用 `cp` 将上述 8 个角色文件一比一同步到 `src/harness_workflow/assets/scaffold_v2/.workflow/context/roles/` 下同名文件，`diff -r` 核对为零差异。
- 对应 plan.md Step 1.6：将 `src/harness_workflow/assets/skill/assets/templates/change.md.tmpl` / `change-plan.md.tmpl` 升级为中文完整版（含 Title / Goal / Requirement / Scope / Acceptance / Risks 以及 Development Steps / Verification Steps / 依赖与执行顺序 等必备节，占位符用 `{{…}}` + `<!-- 示例 -->` 明示）；`.en.tmpl` 英文版同步升级为字段结构对称的英文完整版。
- 对应 plan.md Step 1.7 + 1.8：确认 `scaffold_v2/` 下无 change/plan 模板镜像，无需额外同步；git 反例核对确认未触碰 `.workflow/flow/` 与 `artifacts/bugfixes/bugfix-{2,3,4,5}/` 历史脏数据。

## 未做与原因

- plan.md 明确列为"可选"的"为 req-26 自身先手动产出一份 `需求摘要.md` 作为样例"（change.md Scope Included 末段）：本 change 只制定机制不强制先写示例，交由后续 requirement_review 阶段补齐。
- plan.md 验证步骤 2.2 / 2.3（`harness install` 与 `harness change` 集成验证）：本 subagent 不跑 CLI 集成验证，该部分归并到 chg-06 端到端 smoke 执行。

## 关键文件变更

- `.workflow/context/roles/stage-role.md`：新增"对人文档输出契约（req-26 / sug-06）"章节（契约 1~5）。
- `.workflow/context/roles/{requirement-review,planning,executing,testing,acceptance,regression,done}.md`：各新增"对人文档输出（req-26）"节 + 退出条件补丁。
- `src/harness_workflow/assets/scaffold_v2/.workflow/context/roles/{stage-role,requirement-review,planning,executing,testing,acceptance,regression,done}.md`：与本仓库版本文本一致的 scaffold 镜像。
- `src/harness_workflow/assets/skill/assets/templates/change.md.tmpl` / `change-plan.md.tmpl`：中文完整模板（Title / Goal / Requirement / Scope / Acceptance / Risks + Development Steps / Verification Steps / 依赖与执行顺序）。
- `src/harness_workflow/assets/skill/assets/templates/change.md.en.tmpl` / `change-plan.md.en.tmpl`：字段结构对称的英文完整模板。

## 已知限制

- `harness install` 与 `harness change` 的端到端集成验证（plan.md 2.2 / 2.3）未在本 change 执行，留给 chg-06 smoke 验证；若 chg-06 发现模板渲染路径或 scaffold 拷贝路径未覆盖新文件，需回归修补。
- planning 阶段对人文档命名仅保留"文件名固定"硬约束，对表述微调的空间未显式列举边界，若后续出现歧义需补 stage-role.md 的备注。
