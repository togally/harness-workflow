# 测试结论：req-26 uav-split

## 通过/失败统计
- 全量：86 / 通过 47 / 失败 3 / 错误 3 / 跳过 36（含本阶段补测 5 条）。
- req-26 相关：`test_regression_helpers` / `test_rename_helpers` / `test_next_writeback` / `test_archive_path` / `test_smoke_req26` 共 23 条，全部通过；独立补测 `test_req26_independent.py` 5 条全部通过。
- 与基线对比：3 失败 + 3 错误均为 **req-26 之前已存在**（已回滚到 `3f94c30^` 核查确认），非本需求引入；req-26 新增测试 0 失败。

## 关键失败根因
- 无 req-26 新增失败。
- 基线 failure（不阻塞 req-26）：`test_cli.test_install_writes_three_platform_hard_gate_entrypoints` / `test_installed_skill_uses_global_harness_commands` / `test_bugfix_creates_workspace_and_enters_regression` 三条与 install/bugfix 的旧模板断言漂移相关；基线 error：`test_cycle_detection`、两条 `test_update_check_and_apply_*` 依赖未落地的 `harness_workflow.core` 模块。已记为独立后续问题。

## 未覆盖场景
- AC-06 对人文档的**真实产出行为**（7 份中文 md 落盘、≤1 页、字段完整）只做静态契约核查，未做运行时写盘校验——agent 实际是否按模板写需在 acceptance 阶段逐目录核对。
- AC-02 `rename` 未覆盖跨分支（非 `main`）下 state yaml / runtime 同步；`_get_git_branch` 在 smoke 中被 mock，真实多分支场景未验证。
- AC-05 archive 仅覆盖 `main`，未覆盖其他 branch 下路径拼接与"primary/branch-only 两态"切换的组合。
- AC-03 `workflow_next` 推进到 `done` 的 `completed_at` 仅断言非空，未断言 ISO 日期格式；`bugfix` yaml 状态在 `done`/`archived` 阶段尚未有回归用例。
- AC-01 `regression --confirm` 下目录 meta.yaml 的字段顺序 / 其他字段（例如 `confirmed_at`）未核查，只断言 status 文本。

## 风险评估
- **低风险、可进入 acceptance**。6 个 change 覆盖的 4 条命令缺陷均有 helper 层用例 + 端到端 smoke 兜底，AC-06 机制侧静态契约齐全；未覆盖场景均为边界延伸而非主链路，不影响上线。建议 acceptance 阶段重点核对"对人文档真实落盘路径"与"全 7 份文件名/字段顺序"。
