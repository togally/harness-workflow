# 交付总结：req-26 uav-split

## 需求是什么

系统化处理 req-25 遗留的 6 条合并建议：sug-01~05 修复 regression/rename/next/archive 四条命令簇缺陷，sug-06 建立各 stage 角色双轨"对人文档"输出机制（不迁移历史文档，只新增中文精炼产出）。

## 交付了什么

- **chg-01**：修 `harness regression --confirm` 不消费 regression 状态 + 产出目录 kebab-case 带 `reg-NN-` 前缀（AC-01 + AC-04）
- **chg-02**：修 `harness rename` 保留 `{id}-` 前缀 + 同步 state yaml / runtime.yaml（并入 `harness change` 目录命名规范，AC-02）
- **chg-03**：`harness next` 自动写回 stage yaml 的 `stage` / `status`，archive 不再靠人工改 yaml（AC-03）
- **chg-04**：折叠 `harness archive` 归档路径 `archive/{branch}/{branch}/` 双层 branch（AC-05）
- **chg-05**：7 个 stage 角色各自追加"对人文档输出"SOP + 最小字段模板；scaffold_v2 一比一同步；`harness change` 的 change/plan 模板升级为中文完整版（AC-06）
- **chg-06**：端到端 smoke 在 tempdir 走完整生命周期 + AC-06 静态契约核查（AC-07）

## 结果是什么

- **验收结论**：有条件通过。AC-01~AC-05 / AC-07 全部通过；AC-06 机制侧静态契约齐全，运行时落盘在本需求内产出 6 份 `实施说明.md` + `测试结论.md` + `验收摘要.md` + 本 `交付总结.md`，`需求摘要.md` / 6 份 `变更简报.md` 按 chg-05 SOP"对后续阶段生效"豁免回溯。
- **测试结论**：req-26 新增 23 条 helper/smoke 用例 + 独立补测 5 条，共 28 条**全部通过**；全量 86 条中 3 failures + 3 errors 已核验为 req-26 之前的预存基线，非本轮引入。
- **影响面**：无 `.workflow/flow/` 现存文档被触碰，无 `artifacts/bugfixes/` 历史脏数据被触碰，Excluded 反例逐条 OK。

## 后续建议

1. 开 **bugfix-6** 承接 3 fail + 3 error 预存基线（install/update/bugfix CLI 模板漂移 + `harness_workflow.core` 模块缺失），参见 sug-08。
2. 下一个新需求作为对人文档产出链的**首次完整示范**：从 requirement_review 开始产出 `需求摘要.md`、planning 每个 change 产出 `变更简报.md`，形成 7 份完整样例，参见 sug-11。
3. AC-06 机制需要一次"agent 运行时真实落盘审计"补齐运行时闭环，参见 sug-09；`harness change` 模板升级后下游仓库需评估 `harness update` 的刷新覆盖范围，参见 sug-10。
