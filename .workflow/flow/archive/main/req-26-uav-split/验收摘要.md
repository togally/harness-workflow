# 验收摘要：req-26 uav-split

## AC 核对结果

| AC 编号 | 通过 | 证据 / 备注 |
|---------|------|------------|
| AC-01 | 通过 | chg-01 修 `harness regression --confirm` 不消费 runtime；`test_regression_helpers` 9/9 + smoke 覆盖「先 --confirm、后 --testing」。 |
| AC-02 | 通过 | chg-02 保留 `{id}-` 前缀 + 同步 state yaml / runtime.yaml；`test_rename_helpers` 5/5；跨非 main 分支场景属边界延伸（testing 已记录）。 |
| AC-03 | 通过 | chg-03 `_sync_stage_to_state_yaml` 自动写回 stage/status；`test_next_writeback` 3/3 + smoke 覆盖 requirement_review→archive 全链路。 |
| AC-04 | 通过 | chg-01 走 `slug.py` 生成 `reg-NN-<slug>`，去空格与全角标点；helper/smoke 双重核验。 |
| AC-05 | 通过 | chg-04 折叠 `archive/{branch}/{branch}/` 双层段；`test_archive_path` 2/2；对历史双层保持不动。 |
| AC-06 | 条件通过 | 契约 1/2/3 机制侧静态契约齐全（7 份文件名入各 stage.md 与 stage-role.md）；运行时落盘在本需求内仅产出 `测试结论.md` / `实施说明.md`×6 / `验收摘要.md`，`需求摘要.md` 与 6 份 `变更简报.md` 未回溯补产——见「未达项处理建议」。 |
| AC-07 | 通过 | `test_smoke_req26` 4/4 端到端覆盖 AC-01~AC-07 集成行为，无人工改 yaml、无目录命名异常、无双层 branch。 |

## 是否通过

**有条件通过**：六条 sug 的代码/机制层全部落地，端到端 smoke 无回归；建议直接 `harness next` 推进到 done，未达项以"下一需求的示范起点"与独立 bugfix 的形式承接，不驳回本需求。

## 未达项处理建议

- **对人文档未回溯补产**（`需求摘要.md` + 6 份 `变更简报.md`）：chg-05 的 SOP 约定对"后续阶段执行时"生效，本需求 requirement-review / planning 阶段在 chg-05 合入前已完成，按 chg-05 规约不硬性要求回溯。作为下一个需求的第一次完整示范执行，不回填本需求。
- **预存基线 failure（3 fail + 3 error）**：`test_install_writes_three_platform_hard_gate_entrypoints` / `test_installed_skill_uses_global_harness_commands` / `test_bugfix_creates_workspace_and_enters_regression` + 2 条 `test_update_check_and_apply_*` + `test_cycle_detection`，已回滚到 `3f94c30^` 确认为 req-26 之前已存在。建议主 agent 新开 **bugfix-6**（install/update/bugfix CLI 模板漂移 + `harness_workflow.core` 模块缺失），不在本 req-26 内修。

## Excluded 反例核对结果

- `.workflow/flow/**` 未触碰：**OK**（仅删除 `sug-01-regression-confirm-bug.md`，属 chg-01 合规产出）。
- `artifacts/bugfixes/bugfix-2/**` 未触碰：**OK**（commit 3f94c30 stat 无该路径条目）。
- `artifacts/main/bugfixes/bugfix-{3,4,5}/**` 未触碰：**OK**（commit 3f94c30 stat 无该路径条目）。
- `.workflow/flow/archive/**` 未触碰：**OK**（commit 3f94c30 stat 无该路径条目）。

## 遗留建议

1. 新开 **bugfix-6**：修 3 fail + 3 error 预存基线（install/update/bugfix CLI 模板漂移、`harness_workflow.core` 模块缺失）。
2. 下一需求首个 requirement_review / planning 阶段作为对人文档完整示范，不回溯补产 req-26 的 `需求摘要.md` / 6 份 `变更简报.md`。
3. AC-02 跨非 main 分支 rename、AC-05 非 main 分支 archive、AC-03 `completed_at` ISO 格式断言、AC-01 regression meta.yaml 字段顺序——列入下一轮测试补齐清单（非阻塞）。
