# 六层回顾 — req-47（整合清理所有 suggest，先判断当前版本适用性，再将清理后建议打包开发）

> 产出：done 阶段主 agent（done / opus），按 done.md SOP §六层回顾检查执行
> 范围：req-47 全周期（requirement_review → planning → executing → testing → acceptance → done）
> 关联 chg：chg-01（testing 红线 + safer dogfood + commit revert dry-run，5 sug 五合一）
> 关联 sug：sug-31 / sug-51 / sug-52 / sug-55 / sug-58（已 done 阶段 archive）

---

## 第一层：Context（上下文层）

### 角色行为检查

| 角色 | 预期行为 | 实际行为 | 评估 |
|------|---------|---------|------|
| analyst（requirement_review）| 澄清边界 + 列 default-pick | 11 条 D-1~D-11 决策 + requirement.md 5 节完整 | ✅ 符合 |
| analyst（planning Part B）| 沿用 req-46 经验做增量校准 + chg 拆分 + plan.md §4 测试用例 | sug-audit-r2.md 39 条复核 + roadmap-r2.md 8 chg + chg-01 工件 + 10 TC | ✅ 符合 |
| executing | 按 plan.md §1 9 Steps 顺序实现 | Step 1~6,8 全 ✅，Step 7 池清理跳过（硬序约束），Step 9 自身 dogfood 流转后观察 | ✅ 符合 |
| testing | 独立验证 + 溯源 6 历史 fail + 5 项合规扫描 | 9 PASS / 1 N/A（TC-08 P1 留尾）+ 全量 665 pass + 0 新增 fail | ✅ 符合 |
| acceptance | 12 AC 矩阵核查 + verdict | PASS verdict + 4 default-pick 决策记录 | ✅ 符合 |
| done（本角色）| 六层回顾 + 池清理执行 + 交付总结 + 经验沉淀 | 进行中 | ✅ 符合 |

### 经验文件更新

| 经验文件 | 本周期是否更新 | 评估 |
|---------|-----------|------|
| experience/roles/done.md | 已含 chg-03 单表 + chg-04/05 三类任务模板（req-43），本 req 无新可泛化经验 | ✅ 无需更新 |
| experience/roles/testing.md | testing.md 红线（破坏性 git + tmpdir mock + dogfood 标准流程模板）已通过 chg-01 直接落到 evaluation/testing.md（更权威载体）| ✅ 经验下沉到 evaluation 而非 experience，路径更高效 |
| experience/roles/analyst.md | analyst.md Step B2.5 dogfood TC 必填字段已落，是契约层而非经验层 | ✅ 同上 |
| experience/roles/executing.md | 本 req 无新经验值得沉淀（5 sug 同根因合并落地是 chg 拆分粒度问题，不是 executing 行为问题）| ✅ 无需更新 |

**核心发现**：本 req chg-01 把"经验"直接做成了**契约层硬条目**（testing.md 红线段 + lint 工具 + analyst.md TC 必填字段），跳过了 experience/ 软经验路径。这是 chg-01 范围天然属性（testing 红线 = 硬契约不是软经验），不需要再追加 experience/ 沉淀。

### 上下文完整性

- 项目背景、团队规范、契约 7、硬门禁六/七、repository-layout §3 均已被各角色读取（角色加载协议 Step 2~7）。
- 本 req 跨 stage 上下文承接通过 session-memory.md 实现，无丢失。

### 是否触发新 sug

无。

---

## 第二层：Tools（工具层）

### 工具使用顺畅度

| 工具 | 使用情况 | 评估 |
|------|---------|------|
| Edit / Read / Write | 全周期主用 | ✅ 顺畅 |
| Bash（git mv / pytest / harness validate）| 池清理 + 回归 + 自检 | ✅ 顺畅 |
| harness change | CLI 实分配 chg-01（req-47 内首个）+ 落地 changes/chg-01-{slug}/ 骨架 | ✅ 路径自动化 |
| harness validate --human-docs | exit 1（D-11=B 留痕放行，与 req-43~46 同 case）| ⚠️ 工具未做 stage 感知，全 stage 都触发；非本 req 责任 |
| harness validate --contract artifact-placement | exit 0 全周期 PASS | ✅ 顺畅 |
| harness validate --contract testing-no-destructive-git（本 chg-01 新增）| exit 0 PASS | ✅ 落地新工具 |

### CLI 工具适配

- 本 chg-01 新增 `harness validate --contract testing-no-destructive-git` + `harness install --check` + `harness archive --skip-revert-check` 三个 CLI 入口，全部端到端 PASS。
- 工具层无新缺口需要补 CLI / MCP。

### MCP 工具适配

- 本 req 无 MCP 工具相关变更。

### 是否触发新 sug

无。

---

## 第三层：Flow（流程层）

### 阶段流程完整性

| stage | 进入时间 | 流转方式 | 评估 |
|-------|---------|---------|------|
| requirement_review | (合并 planning) | analyst Part A | ✅ |
| planning | 2026-04-28T02:05:23+00:00 | analyst Part B 自决推进（auto exit） | ✅ |
| ready_for_execution | 02:29:21 | 用户拍板首批 chg-01 范围 + harness next --execute | ✅ |
| executing | 02:38:18 | executing subagent 完成 8/9 Steps（Step 7 / 9 跨 stage 留尾）| ✅ |
| testing | 02:52:39 | auto exit（testing 完成转 acceptance）| ✅ |
| acceptance | 03:04:48 | verdict PASS → done | ✅ |
| done | 03:18:35 | 当前 | ✅ |

无阶段跳过 / 短路 / 重复。

### 流程顺畅度

- 各 stage 流转无卡顿；用户介入仅 1 次（planning → ready_for_execution 拍板首批 chg-01 范围），与 stage_policies.planning.exit_decision = user 一致。
- conversation_mode = harness 全周期锁定，未漂移其他 req。

### 重要发现

- **首批 K=1 单 chg 落地节奏证实可控**：req-46 同模式（K=2）外加本 req K=1，节奏可调；首批控制 ≤ 3 是合理上限。
- **chg-01 5 sug 同根因合并粒度合理**：一个 chg 涵盖 testing 红线 + lint + dogfood + revert + dev mode 5 主题，工作量"中"（plan.md 9 Steps），未触发二次拆分，验证 split rules"同根因 sug 必合并到一个 chg"实操性。

### 是否触发新 sug

无。

---

## 第四层：State（状态层）

### runtime.yaml 一致性

| 字段 | 当前值 | 评估 |
|------|-------|------|
| current_requirement | req-47 | ✅ |
| stage | done | ✅ |
| conversation_mode | harness | ✅ 锁定 |
| active_requirements | [req-46, req-47] | ⚠️ req-46 已 done 但仍在 active 列；非本 req 责任，归 sug-13 / sug-26 / sug-37（chg-9 runtime sync）留尾 |

### 需求状态一致性

- `.workflow/state/requirements/req-47-{slug}.yaml`：status: done / completed_at: 2026-04-28 / stage_timestamps 全 6 stage 齐全 ✅
- `.workflow/state/requirements/req-46-...yaml` 等历史档保持原貌 ✅

### 状态记录完整性

- 关键决策（24 default-pick：D-1~D-11 + D-r2-1~D-r2-8 + D-rm-1~D-rm-4 + acceptance/testing 7 条）全部落 session-memory.md / sug-audit-r2.md / roadmap-r2.md。

### **State 层关键发现：usage-log.yaml 物理缺失**（base-role done 六层回顾 State 层自检硬门禁）

执行 base-role 状态校验：

| 检查项 | 结果 |
|--------|------|
| `.workflow/state/sessions/req-47/usage-log.yaml` 存在 | ❌ 不存在（仅 task-context/ 子目录）|
| `.workflow/flow/requirements/req-47-{slug}/usage-log.yaml` 存在 | ❌ 不存在 |
| usage-log entries 数 | 0 |
| 派发次数（session-memory 树估）| ≈ 6（analyst×2 + executing×1 + testing×1 + acceptance×1 + done×1）|
| 容差 | 0（无失败派发 / stub 降级）|
| **断言 entries ≥ 派发次数 - 容差** | **FAIL：0 < 6** |
| 报告 | 本 req **usage 采集不完整**；缺失派发清单：全部 6 次（无 entries 留痕）|

**根因**：sug-39（record_subagent_usage 派发钩子未真接通）+ sug-59（done_efficiency_aggregate 写读路径漂移）双重根因仍未真修；与 req-46 同状态，本 req 比 req-46 更恶化（state/sessions/ 也未生成 entries）。

### 是否触发新 sug

- **建议升级 sug-39 + sug-59 优先级到 P0/数据安全**：本 req 周期实证两根因恶化（req-46 至少 entries 数对 = 9，token total 单字段有效；req-47 全 0 + entries 数为 0），下 req chg-2 落地紧迫性升级。
- 现有 sug-39 / sug-59 frontmatter 已是 priority: high，无需新增 sug，但建议 chg-2 落地时 plan.md §1 step 1 显式 dogfood 实证两根因可复现。

---

## 第五层：Evaluation（评估层）

### testing 独立性

- testing subagent 独立读取 plan.md §4 测试用例设计，独立编写 test-report.md（root 层），不受 executing session-memory 影响（仅读取已完成结论）。
- testing 验证 16 新增用例全绿 + 全量 665 pass + 0 新增 fail 是独立结论，与 executing 自我汇报"已实现"独立交叉验证。
- ✅ 评估独立性达成。

### acceptance 独立性

- acceptance subagent 独立读取 requirement.md / change.md / test-report.md，独立编写 acceptance/checklist.md（12 AC 矩阵）+ acceptance-report.md。
- 4 条 default-pick 决策（req AC-02 ≤ 25 / chg AC-06 时机 / usage-reporter.md / 6 历史 fail）独立做出，未盲目继承 testing 结论。
- ✅ 评估独立性达成。

### 评估标准达成

- 12 AC 全 PASS，无降低标准 / 妥协；TC-08 N/A 留尾是基于 P1 优先级 + executing 明示未实现 + AC-03 其他 3 条已满足的合规判定，不是放水。
- 6 历史 fail 在 testing/acceptance 阶段都做了独立溯源 + 与 chg-01 零交集核对，不是绕过。

### 是否触发新 sug

无。

---

## 第六层：Constraints（约束层）

### 边界约束触发

- 硬门禁四（同阶段不打断 + default-pick 记录）：全周期严格遵守，24 条 default-pick 全部留痕。
- 硬门禁六（对人汇报 ID 必带简短描述）+ 契约 7（id+title 首次引用）：testing 阶段已扫描 chg-01 工件、acceptance/checklist.md / acceptance-report.md 全合规；本 done 阶段交付总结.md 首次引用工作项 id 时全部带 title 或 ≤ 15 字描述。
- 硬门禁七（周转汇报不列选项 + 必报本阶段已结束）：各 stage 汇报段已遵守 Ra/Rb/Rc。
- 硬门禁五（scaffold_v2 mirror 同 commit 同步）：chg-01 涉及的 evaluation/ + roles/ 均已同步（usage-reporter.md 漂移属 sug-56 范围，归 chg-4 留尾）。
- testing 红线（破坏性 git 禁止 + tmpdir mock）：本 chg-01 自身落地的红线在 testing 阶段实测合规（read-only diff 替代实 revert，0 违规）— **dogfood 自证**。

### 风险扫描更新

- testing 红线 = 数据安全 P0 风险已落到 evaluation/testing.md + validate_contract.py lint，known-risks.md 不需要再追加（契约层已覆盖）。
- 6 历史 fail 风险登记到本交付总结 §后续建议，建议另开 bugfix。

### 约束遵守情况

- **路径自检硬门禁**（machine 落 .workflow/flow/ + 对人落 artifacts/）：本 req 周期所有产物全合规。
- 契约 6（sug frontmatter 5 必填字段）：池清理 5 条 sug 翻 status: applied + applied_by_chg + applied_at 后所有字段齐全。
- conversation_mode 锁定：全周期未漂移其他 req。

### 是否触发新 sug

无。

---

## 工具层 / CLI 适配性问题专项

### CLI 工具适配性

- 本 chg-01 新增 3 个 CLI 子命令（validate testing-no-destructive-git / install --check / archive --skip-revert-check）端到端 PASS。
- 工具适配无显著痛点。

### MCP 工具适配性

- 本 req 无 MCP 触点。

---

## 流程完整性检查

| 检查项 | 结果 |
|-------|------|
| requirement_review | ✅ Part A 完成（与 planning Part B 同 analyst 角色）|
| planning | ✅ Part B 完成（sug-audit-r2 + roadmap-r2 + chg-01 工件 + 池清理 5 条）|
| ready_for_execution | ✅ 用户拍板首批 chg-01 范围 |
| executing | ✅ 8/9 Steps 完成（Step 7 / 9 跨 stage 留尾合规）|
| testing | ✅ 10 TC + 全量回归 + 6 fail 溯源 + 5 合规扫描 |
| acceptance | ✅ 12 AC PASS + 4 default-pick |
| done | ✅ 进行中（六层回顾 + 池清理 5 条 + 交付总结）|

无阶段跳过 / 短路 / 重复 / 遗漏。

---

## 改进建议（自动转 sug 池）

| 编号 | 建议 | 来源层 | 优先级建议 |
|------|------|-------|-----------|
| - | usage-log.yaml 全 req 采集失效 | State 层 | 已有 sug-39 / sug-59，**P0 升级**（无需新增 sug，建议 chg-2 plan.md 显式 dogfood）|
| - | active_requirements 列表未清理 req-46 | State 层 | 已有 sug-13 / sug-26 / sug-37（chg-9 runtime sync），无需新增 |
| - | 6 历史 fail 建议另开 bugfix | Flow / Evaluation 层 | 不在 sug 池范围（直接走 harness bugfix 路径），归交付总结 §后续建议 |
| - | TC-08 dogfood TC 字段 lint 留尾 | Evaluation 层 | 已在 testing/acceptance session-memory 标注；建议归 chg-5（契约 lint 套件）；不新增独立 sug（chg-5 范围已含 sug-22 / sug-23 / sug-27 等同类工具骨架）|

**结论**：本 req 周期六层检查均无新 sug 入池触发；现有 sug 池已覆盖所有发现的待办事项；池清理 5 条出池后池容量回到合理 baseline（live 41 / archive 18）。

---

## §结论

**六层回顾 PASS**

- 6 层均无重大异常；24 条 default-pick 决策全留痕；池清理 5 条 sug 出池合规（contract 6 frontmatter + git mv archive/）；usage-log 缺失为已知根因（sug-39 / sug-59，下 req chg-2 修），其他 5 层均通过。
- 经验沉淀：本 req chg-01 把"软经验"直接做成"契约层硬条目"（testing.md 红线 + analyst.md TC 必填字段 + lint 工具），跳过了 experience/ 软经验路径，是契约硬化的高效路径，无需追加 experience/ 沉淀。
- 流程完整 + 评估独立 + 约束严守 + 状态自洽（除已知 usage-log 缺失），符合 done 阶段退出条件。
