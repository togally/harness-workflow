# Session Memory: req-04/chg-01 监控指标与阈值设计

**变更**: chg-01-监控指标与阈值设计
**阶段**: executing (开发者)
**开始时间**: 2026/04/14
**执行者**: 开发者 subagent

## 执行日志

### 步骤 1：调研 Claude Code 上下文限制
- [x] 查阅 Claude Code 文档，确认最大上下文长度（token 数）
  - Claude API 最大上下文长度：102400 tokens（来自 req-03 诊断文件）
- [x] 了解 `/compact`、`/clear` 等命令的工作原理和效果
  - `/compact`：压缩历史消息，保留重要信息但减少细节，适用于历史消息仍有价值但占用空间过多的情况
  - `/clear`：清除历史消息，重新开始，适用于历史消息已无效或干扰新任务的情况
- [x] 记录关键限制：最大 token 数、消息条数限制、文件读取限制等
  - 最大 token 数：102400 tokens
  - 消息条数限制：未找到明确限制，但会累积到 token 限制
  - 文件读取限制：未找到明确限制，但每次读取都会增加上下文负载

### 步骤 2：设计监控指标清单
- [x] 创建 `design.md` 文件，定义指标
  - 已创建 `/Users/jiazhiwei/claudeProject/harness-workflow/.workflow/flow/requirements/req-04-上下文维护机制设计/changes/chg-01-监控指标与阈值设计/design.md`
- [x] 为每个指标定义量化方法和采集时机
  - **Token 估算**：基于消息历史、文件内容、工具输出的估算公式
  - **消息条数**：用户消息 + AI 回复的总条数，实时统计
  - **文件读取次数**：Read 工具调用次数，按文件类型分类
  - **会话时长**：从会话开始到当前的时间，持续计时
  - **工具调用分布**：各类工具调用次数的分布情况

### 步骤 3：设计触发阈值
- [x] 在 `design.md` 中定义三层阈值
  - **预警阈值**（黄色警报）：70-80% 最大上下文（71680-81920 tokens）
  - **强制维护阈值**（橙色警报）：85-90% 最大上下文（87040-92160 tokens）
  - **紧急阈值**（红色警报）：>95% 最大上下文（>97280 tokens）
- [x] 为每个阈值提供具体数值建议和依据
  - 基于最大上下文长度 102400 tokens 计算
  - 考虑安全边际：预警（20-30%）、强制维护（10-15%）、紧急（5%）
  - 提供辅助阈值：消息条数、文件读取次数、会话时长

### 步骤 4：定义指标采集方法
- [x] 设计三种采集方式
  - **主 agent 定期估算**：关键节点主动估算
  - **subagent 任务结束报告**：任务结束时报告消耗量
  - **用户手动检查点**：通过命令随时检查
- [x] 定义采集频率和触发条件
  - **高频采集**（实时）：消息条数、工具调用
  - **中频采集**（关键节点）：Token 估算、文件读取汇总
  - **低频采集**（定期）：完整状态报告（每30分钟）

### 步骤 5：创建验证方法
- [x] 设计验证场景
  - **场景 1**：正常负载下的指标计算（50条消息，30次读取，1小时）
  - **场景 2**：预警阈值触发（120条消息，60次读取，2.5小时）
  - **场景 3**：强制维护阈值触发（180条消息，90次读取，4.5小时）
  - **场景 4**：紧急阈值触发（220条消息，110次读取，6.5小时）
- [x] 定义验收标准
  - **完整性**：包含所有要求的指标、阈值和采集方法
  - **清晰性**：指标定义清晰，量化方法明确
  - **合理性**：阈值设计符合实际使用场景
  - **可行性**：采集方法考虑实现成本和性能影响
  - **一致性**：与后续变更的接口明确

## 待处理捕获问题

## 经验沉淀

### 经验一：基于现有文档的上下文限制调研
**场景**：需要调研Claude Code上下文限制，但无外部文档访问权限。

**经验内容**：
1. 优先搜索项目内部文档（如诊断文件、代码注释）
2. 从错误信息中提取关键限制（如req-03诊断文件中的102400 tokens）
3. 通过代码注释了解工具工作原理（如core.py中的`/compact`、`/clear`说明）
4. 汇总形成完整的限制认知，避免依赖外部不确定信息

**可泛化**：适用于所有需要了解系统限制但无外部文档的场景。

### 经验二：基于硬限制的相对阈值设计
**场景**：需要设计触发阈值，但实际使用场景多样。

**经验内容**：
1. 以硬限制（102400 tokens）为基准设计相对阈值
2. 考虑安全边际：预警（20-30%）、强制维护（10-15%）、紧急（5%）
3. 提供辅助阈值（消息条数、文件读取次数、会话时长）作为补充
4. 阈值设计要留有余地，避免频繁误报

**可泛化**：适用于所有需要基于系统限制设计预警机制的场景。

### 经验三：变更间的接口明确化
**场景**：当前变更（chg-01）为后续变更（chg-02到chg-06）提供基础。

**经验内容**：
1. 在设计文档中明确标注与后续变更的接口
2. 定义清晰的输入输出关系（如：阈值判断 → 动作决策）
3. 避免越界，严格遵守change.md的范围定义
4. 为后续变更提供足够的灵活性和扩展性

**可泛化**：适用于所有有依赖关系的变更序列设计。

## 内部测试结果

### 测试项 1：完整性检查
- [x] 包含所有4个监控指标（Token估算、消息条数、文件读取次数、会话时长）
- [x] 定义了三层触发阈值（预警、强制维护、紧急）
- [x] 定义了三种采集方法（主agent定期估算、subagent报告、用户手动检查）
- [x] 提供了验证方法和验收标准

### 测试项 2：清晰性检查
- [x] 指标定义清晰，有明确的量化方法
- [x] 阈值设计有具体数值和依据
- [x] 采集方法有明确的触发条件和频率
- [x] 文档结构清晰，易于理解

### 测试项 3：合理性检查
- [x] 阈值基于102400 tokens最大限制计算
- [x] 考虑了安全边际（预警20-30%、强制维护10-15%、紧急5%）
- [x] 采集方法考虑实现成本和性能影响
- [x] 与后续变更的接口明确，没有越界

### 测试项 4：一致性检查
- [x] 与change.md要求一致，包含所有要求的内容
- [x] 不包含change.md明确排除的内容
- [x] 为后续变更（chg-02到chg-06）提供了清晰的接口

**测试结论**：通过所有内部测试，design.md符合change.md的验收条件。

## 完成状态
- [x] plan.md 所有步骤已执行
  - 步骤1：调研Claude Code上下文限制 ✅
  - 步骤2：设计监控指标清单 ✅
  - 步骤3：设计触发阈值 ✅
  - 步骤4：定义指标采集方法 ✅
  - 步骤5：创建验证方法 ✅
- [x] 内部测试通过
  - 完整性、清晰性、合理性、一致性检查全部通过
- [x] session-memory 执行日志已更新
  - 所有步骤标记✅，包含详细执行记录