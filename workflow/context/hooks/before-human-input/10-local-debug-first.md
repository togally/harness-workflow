# Local Debug Evidence First

`before-human-input`

## Rules

- Startup logs, compile output, test failures, and runtime errors must be inspected by the AI first.
- Do not ask the human to inspect local logs first.
