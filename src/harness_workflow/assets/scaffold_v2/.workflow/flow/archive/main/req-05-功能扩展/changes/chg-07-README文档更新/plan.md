# 执行计划

## 依赖关系
- **前置依赖**：chg-01（kimicli 核心实现）、chg-02（平台检测注册）、chg-06（制品仓库功能）
- **后置依赖**：无

## 执行步骤

### 步骤 1：阅读现有 README 结构
1. 阅读 `README.md` 和 `README.zh.md`，确认：
   - 平台列表所在章节（通常是 "Supported Platforms" 或安装说明章节）
   - `harness install` 命令说明的位置
   - 是否已有 `artifacts/` 相关内容（检查是否需要新增章节或追加内容）

### 步骤 2：更新 README.md 平台列表
1. 在"受支持平台"列表中，追加 kimicli 条目：
   ```markdown
   - **kimicli** — `.kimi/skills/{command}/SKILL.md`（YAML frontmatter + Markdown 格式）
   ```
2. 若有平台数量描述（如"三个平台"），更新为"四个平台"
3. 在 `harness install` 说明中，更新默认启用的平台列表描述：`codex / qoder / cc / kimi`

### 步骤 3：更新 README.md artifacts/ 说明
1. 在合适位置（建议在"归档"或"archive"相关章节之后）新增：
   ```markdown
   ## Artifacts Repository

   The `artifacts/` directory serves as a knowledge base for completed requirements:

   - **`artifacts/requirements/`** — Auto-generated by `harness archive`. Each archived requirement produces a `{req-id}-{title}.md` summary doc covering business context, goals, scope, acceptance criteria, change list, and key design decisions. Useful for onboarding new team members.
   - Other subdirectories (`sql/`, `api/`, etc.) are managed manually by the team.
   ```

### 步骤 4：更新 README.zh.md 平台列表
1. 同步 README.md 的平台列表更新，使用中文说明：
   ```markdown
   - **kimicli** — `.kimi/skills/{命令}/SKILL.md`（YAML frontmatter + Markdown 格式）
   ```
2. 更新平台数量描述和 install 说明

### 步骤 5：更新 README.zh.md artifacts/ 说明
1. 新增中文版制品仓库说明：
   ```markdown
   ## 制品仓库

   根目录 `artifacts/` 作为已完成需求的知识库：

   - **`artifacts/requirements/`** — 由 `harness archive` 自动生成。每个归档需求会生成一份 `{req-id}-{标题}.md` 摘要文档，包含业务背景、需求目标、交付范围、验收标准、变更列表和关键设计决策，便于新成员快速接手。
   - 其他子目录（`sql/`、`api/` 等）由团队手动维护。
   ```

## 预期产物
1. `README.md` 更新：kimicli 平台条目、artifacts/ 章节
2. `README.zh.md` 更新：同上，中文版

## 验证方法
1. 阅读更新后的 README.md，确认 kimicli 出现在平台列表中，路径为 `.kimi/skills/{command}/SKILL.md`
2. 确认 artifacts/ 章节存在且描述准确
3. 对比 README.md 和 README.zh.md，确认内容结构一致（语言不同，信息对等）
4. 检查无拼写错误和格式破坏（Markdown 标题层级、列表缩进）

## 注意事项
1. 仅修改文档文件（README.md、README.zh.md），不触碰任何代码文件
2. 保持现有文档风格（标题层级、列表符号、代码块语言标注）
3. artifacts/ 说明需强调：`requirements/` 由 harness 自动维护，其余目录用户自管，不要让读者误以为 harness 会管理整个 artifacts/
4. kimicli 说明中需提及 YAML frontmatter 格式，与 codex skill（纯 Markdown）有所区分
5. 若 README 中原有"安装后自动创建"类描述，更新时确保包含 `.kimi/skills/` 目录
