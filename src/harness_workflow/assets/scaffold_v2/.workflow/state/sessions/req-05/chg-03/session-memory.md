# Session Memory: chg-03

## Change
角色文件与约束更新

## Status
✅ 已完成

## Steps
- [x] 读取 `WORKFLOW.md` 并增加 ff 模式协调职责
- [x] 读取并更新 `planning.md` 的 ff 说明
- [x] 读取并更新 `executing.md` 的 ff 说明
- [x] 读取并更新 `testing.md` 的 ff 说明
- [x] 读取并更新 `acceptance.md` 的 ff 说明
- [x] 读取并更新 `requirement-review.md` 的 ff 说明
- [x] 读取并更新 `regression.md` 的 ff 说明
- [x] 读取 `constraints/boundaries.md` 并新增 ff 决策边界条目
- [x] 读取 `constraints/recovery.md` 并新增平台错误恢复和 skill 缺失处理条目
- [x] 检查所有修改的一致性

## Internal Test
- [x] `WORKFLOW.md` 包含 ff 模式协调职责
- [x] 至少 4 个核心角色文件已补充 ff 说明（实际补充了 6 个）
- [x] `constraints/boundaries.md` 新增 ff 决策边界
- [x] `constraints/recovery.md` 新增平台级错误恢复条目
- [x] `constraints/recovery.md` 新增 skill 缺失处理条目

## Notes
所有修改逻辑一致，无冲突。ff 模式下各角色的流转规则与 `stages.md` 中的定义完全对齐。
