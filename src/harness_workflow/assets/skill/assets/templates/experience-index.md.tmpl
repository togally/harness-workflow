# 经验索引

> Agent 在需求分析、方案设计、实施、自检前，以及每个阶段进入具体任务前，都应先读取本索引。
> 本文件保持轻量，只维护关键词、文件路径、加载时机与置信度。

## 使用方式

1. 根据用户意图与仓库上下文匹配下表关键词
2. 仅加载命中的经验文件
3. 当前 change 的 `session-memory.md` 中若存在经验沉淀候选，先结合候选内容判断是否需要补充经验文件
4. `high` 置信度条目必须遵守
5. `low` 置信度条目作为参考，验证后再提升
6. 每个阶段任务完成后，回看本次新增经验，判断是否应写回 `session-memory.md`、经验文件或正式规则

## 经验映射表

| 关键词 | 经验文件 | 加载时机 | 置信度 | 说明 |
|--------|----------|----------|--------|------|
| 架构, 设计, 技术选型 | experience/architecture/common-errors.md | 设计前 | low | 设计前先检查历史方案中的常见错误。 |
| 自检, 评审, 收尾 | experience/review/self-check.md | 自检前 | low | 完成实施后做针对性自检。 |

## 置信度规则

- `low`：新沉淀经验，尚未充分验证
- `medium`：重复问题中至少验证过一次
- `high`：多次验证有效，或属于高风险约束

成熟且高置信度的经验，可逐步上升为 `workflow/context/team/` 或 `workflow/context/rules/` 中的正式规范。
尚未稳定的经验先留在当前 change 的 `session-memory.md` 或候选经验文件中，不要过早提升为硬规则。
已经成熟、且与当前任务直接相关的经验，不只要“读取”，还应在当前 requirement、change、plan、execution 中主动融合使用。
