# 回归所需输入

当编译失败、启动失败，或继续排查前需要用户提供数据时，必须先补齐本文件，再提示用户填写。
如果启动日志、编译输出、测试堆栈等已经能在本机直接获取，应先由 AI 自行采集和分析，不要先向用户要这些本地信息。

## 1. 当前问题

- 问题描述：
- 所属 regression：
- 关联 change：

## 2. 需要用户提供的内容

| 项目 | 是否必填 | 说明 |
| --- | --- | --- |
| 配置项 | 是/否 | 例如环境变量、配置文件字段 |
| 测试数据 | 是/否 | 例如样例请求、数据库记录 |
| 账号信息 | 是/否 | 例如测试账号、权限范围 |
| 外部依赖信息 | 是/否 | 例如服务地址、第三方回调参数 |

## 3. 用户填写区

- 配置项：
- 测试数据：
- 账号信息：
- 外部依赖信息：

## 4. 填写后动作

- 用户填写完成后，重新读取本文件
- 回到当前 regression，继续确认或修复
