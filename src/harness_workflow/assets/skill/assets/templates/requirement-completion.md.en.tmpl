# Requirement Completion

## 1. Requirement

- `{{TITLE}}`

## 2. Mandatory Checks

- [ ] Every linked change has passed its own acceptance checks
- [ ] The project starts successfully and startup validation passes
- [ ] Evidence of successful startup is recorded here or in the linked change documents

## 3. Startup Validation Record

- Startup command:
- Success signal:
- Key output:

## 4. Failure Handling

- If the project does not start or startup validation fails, do not bypass workflow and close the work anyway
- Start `harness regression "<issue>"`
- If user-provided configuration, data, accounts, or external dependency details are required, write the request template into the related change `regression/required-inputs.md` and ask the human to fill it before continuing
