# 版本工作区

## 1. 版本号

{{VERSION_ID}}

## 2. 说明

- 本目录是当前版本的主工作容器。
- requirement、change 与 plan 都应放在本目录下。

## 3. 内容

- `需求/` 或 `requirements/`
- `变更/` 或 `changes/`
- `计划/` 或 `plans/`
- `版本记忆.md` 或 `version-memory.md`
