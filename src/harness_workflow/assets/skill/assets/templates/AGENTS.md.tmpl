# AGENTS.md

本仓库采用面向 Agent 协作的文档工作流。

## 优先阅读

1. 在需求分析、change 拆分、plan 制定或执行前，先读取 `docs/context/rules/workflow-runtime.yaml`
2. 根据 `current_version` 继续读取对应 version 的 `meta.yaml`
3. 再读取 `docs/memory/constitution.md`
4. 在分析、设计、实施、自检前读取 `docs/context/experience/index.md`
5. 在执行任何任务前读取 `docs/context/rules/risk-rules.md`
6. 仅按关键词加载命中的经验文件，不全量读取 `docs/context/experience/`
7. 详细执行流程请读取 `docs/context/rules/agent-workflow.md` 与 `docs/context/rules/development-flow.md`
8. 如需了解项目事实或开发规范，继续读取 `docs/context/project/project-overview.md` 与 `docs/context/team/development-standards.md`

## 关键入口

- `docs/README.md`：文档总入口
- `docs/context/`：团队规范、项目事实、经验索引与执行规则
- `docs/context/rules/workflow-runtime.yaml`：当前工作流运行态
- `docs/versions/active/<version>/`：当前版本下的需求、变更与计划工作区
- `docs/runbooks/`：操作手册

## Harness 命令入口

以下场景优先考虑使用 `$harness`：

- 初始化仓库工作流：`harness init`
- 切换语言：`harness language english|cn`
- 切换或创建版本：`harness version "v1.0.0"`
- 新建需求主题：`harness requirement "需求标题"`
- 拆分具体功能点：`harness change "变更标题"`
- 为 change 制定计划：`harness plan "变更标题"`

## 验证要求

- 若仓库提供文档流程 lint，先执行 lint
- 在声称完成前执行项目现有的测试、构建或校验命令
